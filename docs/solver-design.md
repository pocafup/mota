# solver-design.md — 求解器设计待办

## 优化阶段待办

### FLY-CHEAP: fly魔杖视为廉价动作

玩家实测：频繁用 fly 来回切层优化路线"一定不劣"，且 fly 不消耗 HP。
优化阶段求解器应将 fly 魔杖切层视为近似零代价动作（不扣 HP、只消耗背包的 fly 道具），
不应对 fly 次数施加惩罚权重。

具体含义：若两条路线 HP 相同，仅 fly 次数不同，应视为等价（fly 不限次时）。
若 fly 道具有数量限制，则优化时需跟踪持有量，但单次 fly 代价 = 0 HP。

**来源**：玩家 2026-05-31 指出，待优化阶段实现时参考。

---

## 段间评估必须用 Pareto 前沿，不用 HP 单维（A 段实证，2026-06-05）

### 实证：单层 HP 贪心会牺牲永久属性
MVP A 段（MT8 段，入口 tokens[:680] → 出口 MT8(3,11)）搜索结果：
单层「到出口 HP 最大」找到 HP=253 的走法，比玩家 route 的 203 多 50 HP，
但出口 **ATK/DEF 各低 1**（少拿了一块宝石）。引擎独立重放确认这条走法合法。
- 局部看：+50 HP，更优。
- 接全局看：**必亏**。少拿的攻击/防御宝石是**永久加成**，作用在后面全塔每一场
  战斗的损血上；+50 HP 只是一次性消耗品。本阶段（一区末、剑盾已到）正确策略
  是疯狂拿宝石：攻击宝石对初级卫兵减伤显著，MT10 boss 战防御宝石关键。

### 规则：段出口保留多维 Pareto 前沿
对一个段的出口状态，必须保留 **(HP, ATK, DEF, MDEF, 金, 各类钥匙)** 多维
Pareto 前沿，整条前沿传给下一段继续展开。**绝不**在段出口用「HP 最大」单维取优——
那会丢掉「HP 略低但永久属性更高」的点，而后者往往才是全局最优的前身。
这正是分段推进必须**传前沿而非传单点**的根本原因。

### 领域知识：永久属性是投资，HP 是消耗品
- 永久属性（攻/防宝石、装备）边际价值 ≈ base × ratio × 后续受益的战斗场次。
  越早拿、惠及的战斗越多，边际价值越高。
- HP 是消耗品：当前段省下的 HP 不产生复利，只是一次性余量。
- 推论：在「拿永久属性 vs 省 HP」之间，全局最优通常倾向永久属性——但这必须由
  Pareto 前沿**自然涌现**，**不写死成「先拿宝石」规则**（铁律：优化偏好由搜索算出，
  不预设；见 CLAUDE.md「不要做的事」）。领域知识只用来理解为何前沿不能塌成 HP 单维，
  不用来给搜索预设走法。

**来源**：玩家 2026-06-05 裁定 A 段结果（253 单层更优但接全局必亏）时指出，要求落盘。

---

## 出口价值必含「钥匙 + 地图剩余资源」，HP 高不等于更优（253 复盘，2026-06-05）

### 实证：那条「+50 HP」的 253 路线是「提前吃储备」型，不是更优
A 段把出口判优维度补全后逐项核对（全由代码重放算出）：

| 维度 | route 出口(tok712) | 253 搜索出口 |
|---|---|---|
| HP | 203 | 253 (+50) |
| ATK / DEF | 26 / 24 | 26 / 24（持平，都拿了红/蓝宝石）|
| 持有 yellowKey | **3** | **2**（少 1 把）|
| 段内吃血瓶 | **0** | **1 个 redPotion** |
| 地图剩 redPotion | 3 | 2 |

253 的 +50 HP **全部来自吃掉 route 特意留在 (8,10) 的 1 个 redPotion**，同时**少持 1 把
黄钥匙**。补上「钥匙 + 地图剩余资源」两维后，253 **不再支配 route**——它只是 HP 单维更高，
代价是清掉储备 + 少 1 把硬通货。**HP 高 ≠ 更优**：那是把未来储备一次性兑现的虚高。

### 宗旨（本塔打法，玩家 2026-06-05 裁定，落盘为铁律级领域知识）
1. **血量充足时绝不用道具/吃血瓶换血。** 血瓶留在地上是战略储备（低血时回头吃、配合
   夹击战术）。提前吃 = 储备清零 = 出口 HP 虚高。
2. **钥匙是硬通货，必须进 Pareto 维度。** 少开门省下的血不算赢——钥匙少 1 把可能后面
   某扇门就过不去。`key:*` 维已在 value_map 中，但**判优查询必须真的比较它**（A 段历史
   误判正因查询只比了 ATK/DEF/HP，漏了钥匙）。
3. **「地图剩余未拾取资源」必须进出口价值评估。** 同持有属性下，地图上还留着资源的出口
   **严格优于**把资源拿光/吃光的——留着 = 可后取的未兑现增益 / 可调度的储备。

### 建模粒度：数据驱动区分消耗品/永久/钥匙（玩家 2026-06-05 裁定，已固化）
**历史问题**：第一版把**所有**地图剩余资源不加区分地当 Pareto 正维，让「少拿/不拿」的点
全成非支配点（地图剩得多）——A 段前沿从 8 炸到 **143**，连 HP=22「几乎没拿」都在前沿上。
这与「永久属性是投资、疯狂拿宝石」张力极大，且 B/C 段会指数恶化。
- **张力根源**：宝石（→永久属性）该拿、钥匙是该持有的硬通货、血瓶（→消耗 HP）才是
  「留在地上 ≈ 银行里的 HP」那种可两地等价的储备。把三者一视同仁当「剩着更好」是错的。

**裁定（已实现）**：按 `items.json` 的 pickup 效果**数据驱动**区分（solver/search.py
`_gives_hp_on_pickup` + `_remaining_items`，塔无关、不写死任何 id）：
- **「给 HP 的消耗品」（pickup `type==stat & stat==hp`，或 multi 含 hp op）**：其地图剩余
  计入 `map:*` 正维——留着 = 可后取的 HP 储备。
- **宝石/装备（永久增益 stat==atk/def/mdef）与钥匙（硬通货）**：**不**计入地图剩余，只计
  **持有**（已在 `atk/def/mdef/key:*` 维体现）；拿到才兑现，留在地上不是 Pareto 优点。
- 判定与引擎 `_apply_item_effect` 同口径（读同一份 `_items_db`），换塔自动适配。
- 备选「合成单一『可用 HP』维（持有 HP + 地图可取血瓶治疗量）」**未采纳**——它把喝/留血瓶
  在 HP 维上压成等价（更小前沿），但丢掉血瓶**具体位置**（夹击战术按格）信息；保留 `map:*`
  分维更安全（不丢全局最优前身）。
- 铁律守恒：这是**机制事实**（消耗品 HP 可两地等价 vs 永久属性拿到才兑现）的建模，**非**
  走法预设——仍不预设「先拿宝石」「加到某阈值」，孰优由段间 Pareto 支配自然涌现。

**来源**：玩家 2026-06-05 质疑 253「更优路线」（钥匙没报 / 血瓶是留着的储备 / 地图剩余
资源缺维）时裁定，要求核查并落盘。核查结论：质疑成立，253 非更优。

---

## 搜索会精准钻「引擎未建模的机制缺失」；裁判同意≠真走得通（C 段防回头墙复盘，2026-06-05）

### 实证：6 个「严格支配 route」的高 HP 出口全是假赢
C 段（MT33 闭环，入口 tokens[:2894] HP=956 → 出口 MT33(10,1)）**修复前**搜索报出 11 点
前沿、其中 **6 个**完整向量严格支配 route（HP 586/526/476/336/216/76、ATK 全 154）。
引擎裁判独立重放**全部 [OK] 与搜索宣称一致**——可它们**全是物理上走不通的假赢**。

根因：MT33 (8,10) flower 是**防回头墙/单向阀**（左→右穿过后该格永久封成 yellowWall），
脚本挂在 **block 实例的 event/outEvent 上、不在 floor.events**，被 Playwright 提取**系统性
遗漏**。引擎没建这条机制 → 搜索发现可以**反复来回穿 (8,10)** 多薅格外增益再折返回出口，
凭空造出比 route 高 580 HP 的「更优」点。**搜索不是在解塔，是在精准利用引擎与真值之间的缝隙。**

### 修复后：搜索空间塌缩 16×，6 个假赢全部消失
把单向阀建进 sim（通用 `outEvents` 离开格事件通道，data 驱动、塔无关）后重跑 C 段：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 搜索内 step 生成 | 1,406,144 | **86,728**（16× 少）|
| goal_hits | 14,460 | **612** |
| 耗时 | ~195 s | **~12 s** |
| 严格支配 route 的出口 | **6 个**（HP 76~586，假赢）| **1 个**（HP=6，下） |

唯一存活的支配点：HP=**6** ATK=154 DEF=70 金=**766** kill=**183** yellowKey=**3**（86 步，
裁判 [OK]）。与 route（HP=6 ATK=154 DEF=70 金=656 kill=181 yellowKey=2）**同 HP/ATK/DEF，
仅 +110 金 / +2 kill / +1 钥匙**——是「沿 route 多薅一点、同样 HP=6 收尾」的可信候选，
不再是「回头穿墙」的物理违例。**修复把假赢从 6 个削到 0 个，并留下一个真候选待玩家复核。**

### 铁律：「支配 route」的声称，玩家真实游戏复核通过才算数
1. **引擎裁判同意 ≠ 真走得通。** 裁判（我们的 sim）与搜索若**共用同一套机制盲区**，裁判
   的「[OK] 一致」只证明「搜索和裁判错得一样」，不证明真实引擎也允许。6 个假赢全部通过
   了裁判正是此理。**最终权威是玩家把动作序列丢回真实游戏引擎重放。**
2. **「太好了」是机制审计的触发器，不是胜利。** 当搜索报出远胜 route 的出口（尤其闭环段里
   HP 暴高于 route 出口），**第一反应应是怀疑漏建了机制**，而非庆祝。重点审计路径**反复
   复用的格子**上有无未建模的单向/封死/阻断类机制（防回头墙、踩雷假墙、一次性机关门等）。
3. **诊断手法（已沉淀，全程代码判定不手推）**：把可疑出口的动作序列丢回引擎逐帧重放
   （见 diag_mt33_c_reject.py 模板），定位「撞墙原地不动」的步——若发现路径试图回头穿
   一个**本应已封**的格，即坐实机制缺失。修复后同一序列必须被裁判判为走不通。

### 提取盲区：block 内联 event/outEvent 需全塔审计
h5mota 把**踏上(event/arrive)与离开(outEvent/leave)事件挂在 block 实例上**，与 floor.events
**两套并存**。我们的提取只拉了 floor.events，**整类 block 内联事件被系统性遗漏**。
- 已知同机制待提取：**MT38(2,5)** 同一开关、direction=='down' 的防回头墙。
- 待办：审计所有楼层的 `core.getBlock(x,y).event` 是否含未提取的 event/outEvent。
- 建模口径见 data/games51/floors/MT33.json 的 `_outEvents_comment`（moveHero 被逐 token
  重放吸收为 no-op；只建离开封即可达性等价）。

**来源**：玩家本次会话给出 (8,10) block 的 live event/outEvent 脚本，要求修进 sim（封板
机制缺失修复、非凑绿，过三关：零回归 / 新单测钉死 / C 段#1 被裁判拒）并落盘教训。核查
结论：6 假赢全消、搜索塌缩 16×、新单测 4 条钉死单向阀（tests/test_one_way_valve_mt33.py）。

---

## 地图残留资源有「兑现时机」维度；段内 Pareto 判不了，必须靠段边界带「地图残留态」（C 段唯一候选被否决，2026-06-05）

### 实证：那个「+110 金 / +2 kill」的唯一候选，是把 2× 收益按 1× 提前兑现
C 段单向阀修复后唯一存活的「严格支配 route」候选：HP=6 ATK=154 DEF=70 **金=766 kill=183** yellowKey=3，
比 route（金=656 kill=181）**多杀 2 只零伤怪、多 110 金**。段内 Pareto 看：同 HP/ATK/DEF 下白赚，严格支配。

**玩家终审否决**：那 2 只零伤怪 route **不是漏打**，是**故意留着等拾到 coin（击杀金币 ×2）之后再杀**——
coin 前杀每只 = 1× 金，coin 后杀 = 2× 金。这条「改进」把**未来 2× 的收益提前按 1× 兑现**：段内多 110 金，
全局每只少拿一倍金。**段内赚、全局亏。** → C 段**无真支配点**，route 在 C 段也是 Pareto 前沿点。

### 领域知识：地图残留资源 = 「量」 × 「兑现时机」二维
上一课只补了「量」维度（地图剩多少 HP 消耗品，见 `_remaining_items`）。但**同一份残留资源，兑现时机不同、价值差一倍甚至天差地别**：
- **零伤怪 × coin**：coin（tile53，击杀金币 ×2）拾取前后杀同一只怪，掉金差 2 倍 → 先留后杀（coin 后）严格优。
- **血瓶 × 血线**：高血时吃 = 溢出浪费（甚至超上限截断），低血时吃 = 救命 / 配合夹击 → 同一个血瓶价值天差地别。
- 推论：「kill 数」「金数」「地图剩 N 个血瓶」这些**标量**不足以定全局优劣——**必须知道是「哪几只怪 / 哪几个资源、在什么时机兑现」**。

### 这是「分段传前沿」必须解决的核心，呼应「kill 数相同但杀的哪几只」健全性问号
段内 Pareto **结构上判不了跨段时机**：它只看本段出口的标量向量，看不到「这 2 个 kill 是 coin 前还是 coin 后兑现」。
- 这正是之前留下的健全性问号「**kill 数相同但杀的是哪几只**」的**同一个问题**：段边界传的不能只是属性标量，
  **必须携带或可推导「地图残留态」**——哪些怪还活着、哪些资源还在地上及其位置、已触发的全局乘区开关（如 coin 是否已拾）。
- **设计要求（正式分段框架必须满足）**：段间传的 Pareto 前沿，每个点要绑定**该点对应的「地图残留态」**
  （活怪集合 / 地上资源集合及坐标 / coin 这类时机开关的状态）。「coin 前 1× 杀 vs coin 后 2× 杀」这类取舍
  由**跨段** Pareto 比较自然裁定，**不在段内强判**——段内只如实展开、不做时机价值估算。
- 反例警示：若段边界只带 (HP,ATK,DEF,金,kill,钥匙) 标量，就会把「提前 1× 兑现」误判为段内更优而污染前沿——
  C 段这个候选就是活样本。

**来源**：玩家 2026-06-05 终审 C 段唯一候选时裁定（多杀的 2 只零伤怪是 route 留着等 coin×2 后再杀，提前杀
1× 兑现、段内赚全局亏），要求落盘并定档「C 段无真支配点」。⚠ 与待确认项 **bomb×coin**（mechanics_status）
同源：coin 的 ×2 乘区是跨段时机的关键开关，正式分段框架前必须坐实其作用范围。

---

## MVP 三段（A/B/C）搜索回路验证定档（2026-06-05）
单层段搜索回路（重放建入口 → 通用 BFS+Pareto → 引擎裁判独立重放 → 验收）在 A/B/C 三段全部跑通：
- **route 三段皆在 Pareto 前沿**（无任一段被真正支配）。
- 搜索两次「声称超越 route」均被玩家终审否决，且**根因不同、各暴露一类系统缺陷**：
  1. **A 段 253**：钻「出口价值缺维」（钥匙 / 地图剩余血瓶储备未计）→ 补全维度后非支配。
  2. **C 段 86 步**：钻「引擎未建模机制」（防回头墙）造 6 假赢 → 修进 sim 后全消；残下的唯一候选再钻
     「跨段兑现时机」（coin×2）→ 玩家否决。
- 教训全部落盘（本文件各段 + mechanics_status）。**MVP 阶段目的达成**：回路可信、剪枝安全、
  失败模式已知且有防护（单测 + 真值检查点 + 玩家真实游戏终审）。

---

## 正式分段框架启动：裁定①借 route 层序骨架（选 A）+ 裁定②控宽用数据说话 + 两阶段计划（2026-06-05 玩家拍板）

MVP 三段验证回路可信后进入全塔正式分段。玩家就上一会话抛出的两个裁定点拍板如下，要求落盘并写明两阶段计划。

### 裁定①：分段切法 = 借 route 层序骨架（选 A，非完整分层图搜索）

**选 A 不触「不准模仿 route」红线**——该红线针对的是**打法策略**（剑盾优先 / 加点到某阈值那类死规则，见 CLAUDE.md「不要做的事」）；而 route 的**层序（先去哪层）是脚手架、不是策略**。借层序与 MVP 一个路子：先小闭环验证机制，再扩。

- **段内走法 100% 仍由搜索自由决定**（打谁 / 拿什么 / 怎么绕 / 格子间如何走），借的**只有「先去哪层」的层序骨架**。
- 两方案利弊（留档备查，解释为何选 A）：
  - **A·借 route 层序骨架**：利 = 段边界天然有 route 真值锚点（基准下界 + 同口径对比，MVP 工具链现成）、段规模可控、可逐段验收；弊 = 被 route 层序框住，可能漏 route 没访问的跨层分支，本质是「以 route 为骨架增量改良」。
  - **B·完整分层图搜索**：利 = 不被层序限制、理论覆盖全局最优、能发现 route 之外路线；弊 = 无现成段边界真值锚点、状态爆炸难控（A 段已 207K 状态 / 150s）、「地图残留态进段边界」在自由分段下更难定义传递、裁判 / 验收成本高。

### ⚠ 这是【阶段一】，非终态——两阶段计划（必须落盘，防止被误读为「永远借骨架」）

- **阶段一（当前）**：借 route 层序切段，**跑通机制本身**——段间传 Pareto 前沿 + 地图残留态（活怪集合 / 地上资源及坐标 / coin 等全局乘区时机开关）+ 控宽。目的是让分段推进的**机制**可信、性能有数。
- **阶段二（后续）**：**放开层序**，转向完整分层图搜索（方案 B）。届时已积累阶段一的控宽经验 + 真实性能 / 膨胀数据，完整图搜索才有胜算（否则一上来就爆炸、且无控宽依据）。
- **明确**：借骨架是阶段一的脚手架手段，**不是永久设计**；全局最优的终极保证由阶段二放开层序达成。

### 裁定②：前沿控宽 = 先不预设控法，用数据说话

- **不现在拍控宽策略**。阶段一先跑前几段（线性链），**每段报告三项膨胀指标**：① 本段出口前沿宽度；② 绑定「地图残留态」后的去重指纹数；③ 段间传递后的累积宽度。拿到**真实膨胀曲线**再定控宽策略。
- **唯一现在就定的保底（保活兜底）**：若某段宽度爆到跑不动（量级参考 >万级），**临时启用「HP + 各维加权的软截断」**保活继续跑；但**被截断的点必须落盘记录、绝不静默丢弃**——控宽造成的任何丢点都要**可审计**（呼应铁律：控宽不能偷偷塌掉全局最优前身）。
- **ε-支配 / 残留态分桶 / 维度量化**等候选控法，**等膨胀曲线出来再选**，不现在拍。

### 红线照旧（本阶段全程约束）

- `sim/` **除机制修复外不动**；任何改动须过 **84 单测 + 46 检查点全绿**（零回归）。
- **每段引擎独立裁判**（引擎只当裁判、不进搜索循环）；搜索宣称终态须 == 引擎重放终态。
- **任何「支配 route」的点一律玩家真实游戏终审**——裁判同意 ≠ 真走得通（C 段教训）。

**来源**：玩家 2026-06-05 就上一会话抛出的两裁定点拍板，要求落盘并写明两阶段计划。

---

## 缩点商图（连通块图搜索）：治段内「站位×拾取幂集」爆炸（玩家 2026-06-06 三裁定 + MT3 闭环）

### 动机（实测）：段内搜索爆炸，非段间宽度
阶段一前 5 段闭环跑出膨胀曲线后定位到**真正的墙是段内搜索成本**（幂集枚举），不是段间前沿宽度（合并后 ≤78）。富实体层把「英雄站位 × 地上可消耗实体的拾取幂集」全展开：
- **MT3 seg4（(2,11)→(11,5)）**：朴素 BFS gen=**2,000,003** 撞 2M cap、**~104s 未跑完**；同层另一段 gen=**26,532,079**、**~21 分钟**。

### 方案：把「零损血自由连通区」缩成块
并查集 floodfill 把**零损血、可自由连通、无活事件**的格塌成一个「块」——**块内坐标不进状态、块内免费资源拾取顺序不进状态**，状态只记「英雄当前自由块 = 哪些付代价对象已消除」。位置维 `可达格 → 块数`、可消耗维 `2^(全部) → 2^(付代价)`，MT3 单层理论压缩 ≈ 1.7M×（见 analyze_mt3_quotient.py）。

**与走廊剪枝的本质区别（为何选缩点）**：缩点是**无损等价类合并**（只塌「块内位置 + 自由拾取顺序」这些对价值无影响的维度）；走廊是**有损空间剪枝**（会切掉 centerFly 远跳 / 绕远路）。飞行道具在缩点里天然保留为「耗道具的边」，不被误剪——这是缩点对走廊的正确性优势。

### 裁定①：缩点分类口径（玩家 2026-06-06）
- **钥匙 / 宝石 / 装备 / 道具（拿到即兑现）**：块内自动吸，顺序无关，不进搜索分支。
- **血瓶单独一类**（见裁定②）。
- **付代价合并算子**（分隔块、需付代价）：可击杀怪（`damage<hp` 才可战）、钥匙门（持钥匙）、自开假墙。执行 = 走到对象旁 + 发一步触发，引擎 `step` 做真实合并（扣血 / 消钥匙 / 删墙）。
- **独立节点**（不并入块，保留可达性差异 = 残留指纹）：挂**活**事件格 / 怪（MT33 约束）、当前打不动的怪、NPC / 商人 / 祭坛、特殊门、领域/夹击/阻击伤格（块内须零损血，由 `_zone_blocked` 排除）。

### 裁定②：血瓶第一版从简 —— 块内自动吸，不做 bank 开关（已知简化 + 升级条件）
- **本版**：血瓶也**块内自动吸**，**不做**跨块 bank（喝/留）开关。
- **理由（玩家实测）**：前 20 层血瓶基本不留余量，提前喝 vs 留着差别小，先跑机制效果。
- **⚠ 这是【已知简化】，明记于此不静默**：若后续数据（如与 route 同口径对比）显示**提前喝血瓶丢了全局最优**（高血时喝 = 溢出浪费 / 低血回头吃配合夹击的储备价值丢失，呼应「地图残留资源 = 量 × 兑现时机」一课），则**升级为跨块 bank 决策**（血瓶留在地图作可后取储备，喝/留进 Pareto 时机维）。
- **coin×2 兑现时机开关【保留】**（确定有影响）：杀怪 = 该格并入自由块（残留指纹变）→「现在杀 vs 暂不杀」天然成两个指纹都保留，coin 前 1× / coin 后 2× 的取舍不被段内压掉（呼应 C 段否决教训）。

---

## 缩点商图接入 phase1 + 跨段膨胀实测 + 前沿成分诊断（2026-06-07）

### #13 完成：search_quotient 替换段内朴素搜索
- `phase1._run_block` 改用 `search_quotient(_search_copy(st), (floor, gx, gy), step)`；补 `hit_cap` / `intercept_locs` 上报。
- **seg3 触发格修复**：盗贼 NPC `hide`（无 loc/remove）清实体但不写 `_suppressed_events` → `_live_arrive_event` 仍 True，floodfill 永久挡格。修法在 `_expand_op`：trigger 撞开后再多走一步 `mv` 踩上已清格。验证 found=True，朴素路 `UULLDDD` 吻合。
- **效果**：seg4（MT3）原 23 分钟 cap 墙 → **8s**。

### 阶段一全曲线（10 段线性链，块图+商图）
| 段 | 层 | 自由格→块 | 段内状态 | 段末宽 | 累计gen | 段耗时 |
|---|---|---|---|---|---|---|
| 0 | MT1 | 59→10 | 5 | 4 | — | 5ms |
| 1 | MT2 | 69→8 | 1 | 0 | — | 4ms |
| 2 | MT3 | 65→10 | 1 | 1 | — | 1ms |
| 3 | MT2 | 69→8 | 2 | 1 | — | 2ms |
| 4 | MT3 | 67→9 | 1529 | 10 | 5,080 | 8.1s |
| 5 | MT4 | 60→14 | 3008 | 128 | 9,848 | 9.3s |
| 6 | MT5 | 59→15 | 13951 | 1285 | 108,469 | 209s |
| 7 | MT4 | 64→11 | 236567 | 9378 | 956,325 | 1371s |
| 8 | MT5 | 64→10 | 242598 | 12881 | 650,809 | 1568s |
| 9 | MT4 | 64→11 | 968459 | 11607 | 3,460,159 | 5081s |

- **总耗时 8246s（2.3h）；累计裁判不一致=0、hit_cap=0、软截断=0；末态前沿全局重放裁判 3/3 一致。**
- 宽度曲线：4→0→1→1→10→**128(seg5)→1285→9378(seg7)→12881→11607**，在 ~1.2 万 **饱和**（地图残留组合数封顶，非无限爆）。
- (b) **首个商人**：seg5 MT4 (6,1) blueShop → 商图正确识别为拦截事件格，记 `intercept_locs=[(6,1)]` **未强解**（决策放开依据已留）。道具 `item:fly`/`item:I333` 全程 distinct=1（统一拾取，forced 照搬无分叉）。
- (c) seg4 最佳出口 = route 完全一致 HP=976，**无严格支配**；深段「支配 route」动作序列待控宽裁定后再抽（1.2 万宽前沿上抽序列无意义）。

### 前沿成分诊断（玩家裁定「先诊断成分」；diag_frontier_composition.py，N=6 width=128）
**分层分解 5 → 69 → 128**：
- 仅 (hp,atk,def,mdef) 真阈值维 Pareto = **5** 点。
- 加记账维 gold/key/kill → value-Pareto = **69**（+64；去掉全记账维塌缩 92.8%，去掉 gold 单维塌 44.9%、key 42%、kill 10.1%）。
- 全部前沿 = **128**（+59 value-被支配、靠**地图残留指纹**存活）。宽度 = 真阈值数的 **25.6×**。
- 撑宽**唯一**主成分 = `floors`（全层残留）；逐层拆只有 **MT3(entities=10/terrain=8) + MT4(entities=31/terrain=12)** 在变，MT1/MT2/MT5 全常量。即宽度 = 「MT3/MT4 哪些可选怪/门/道具的不同组合」。

### ⚠ 关键修正：宽度是合法载荷，**不是虚胖**（floor-schedule 数据证伪「死层折叠」假设）
diag_floor_schedule.py 算出 route 完整层序（**225 个 floor-segment / 6354 token**）：
- **首个「死层」（不再回切）是 MT3 @ seg72**；MT4 回切到 **seg247**、MT5 到 seg245。**seg72 之前无任何死层。**
- 推论：当前推进深度（seg9）下，MT3/MT4 残留都会被反复回踏 → 「留只怪到更强时再杀（coin×2 / 后杀更优）」对它们**真实成立**。所以 128/1.2 万宽是**route 重度回切的真实组合代价**，是「身份 vs 价值」设计（frontier.py:16-18）在干正事，**没有可廉价无损剪掉的胖**。
- 「折叠不再回切层的残留指纹」是唯一可证无损的杠杆，但 **seg72 前零收益**（届时才开始有死层；endgame seg240+ 大批层死亡时才显著）。

### 待裁定（控宽方向，反映上述数据；玩家拍板，勿预设硬编码）
exact Pareto 到全深 225 段（×~1.2 万宽 × 每点段内搜索，seg9 已 85min/段）**不可行**。控宽因此**必然有损 OR 需算法重构**：
- **① 死层残留折叠**：无损，但 seg72 前无收益；值得为 endgame 实现，救不了近端 seg6-9 墙。需把剩余 forced 层表传进 merge_frontier。
- **② 记账维 gold/key/kill 分箱粗化**：能砍到接近 5，但 gold 刚遇商人（买属性）、key 开门，粗化有丢解风险；分箱阈值是塔特有偏好（碰「不硬编码」红线）。
- **③ 软上限 -c N 截宽**：保活兜底（裁定②已授权），丢最优性保证，被截点须落盘可审计。
- **④ per-point 段内加速**：`_copy_state` 占 ~48%（A 段实测），优化只给常数因子，不解组合增长。

**来源**：本会话 2026-06-07 接入 #13 + 跑通 10 段 + 成分诊断 + floor-schedule 证伪。待裁定项未决，勿预设控法。

### 裁定③：验收硬标准 —— 输出必须是完整动作级、可直接照着走
搜索时**不存块内坐标可以**，但**最终输出必须是完整的动作级通关线路**：`_expand_op` 把每个算子展开为 `_bfs_moves` 块内寻路 + 触发步，每步 U/D/L/R 合法；拼接成整条线路丢回**封板引擎逐字段一致**、格式可直接照着走。`search_quotient` 输出契约对齐 `search_segment`（found / goal_frontier / goal_frontier_actions），phase1._run_block 照旧 `solver.verify.replay` 独立裁判。

### 实现：solver/quotient.py（塔无关，无楼层/怪/道具/阈值硬编码）
`_zone_blocked`（领域/阻击伤格）、`_live_arrive_event`（到达事件门控，见下）、`_is_free_tile` / `_free_cells`（floodfill 英雄当前自由块）、`_bfs_moves`（块内寻路出动作）、`_killable`（`damage<hp` 才可战）、`_boundary_ops`（边界付代价算子）、`_expand_op`（展开为动作序列 + 引擎 `step` 推进）、`_absorb`（吸块内道具到不动点，装备涨 atk 扩边界则重跑）、`_qfp`（商图指纹 = floor + frozenset 自由格 + flags + 全局开关）、`search_quotient`。可通行 / 可击杀 / 零伤 / 领域全由注入 state 的通用字段 + sim 判定函数读出。第一版**单层内**（离层算子裁掉，跨层 / 飞行算子留待后续）。

### 关键修正（本会话教训）：缩点可达判定必须与引擎到达事件门控**同口径**
**Bug**：`_is_free_tile` / `_killable` 原本「只要 `loc in floor.events`（或 after/before_battle）就当独立节点」，**漏看 `_suppressed_events` / `enable`**。MT3 入口 7 格块的唯一非墙出口是 **(5,9)**——那是 seg2 已触发过的伏击格，已落入 `_suppressed_events`（现可通行，route 正穿它上 (11,5)）。结果块被误封死 → 算子=0 / found=False。
- **修正**：抽 `_live_arrive_event(floor,x,y)` 助手**复刻引擎 `_fire_events` 门控**（simulator.py:1415-1424）——到达事件仅当 `loc in events ∧ loc not in _suppressed_events ∧ enable!=False` 才会真正触发；已 fire 的一次性事件 / 禁用事件视作空操作（该格可自由通行）。`before/after_battle` 是战斗钩子（仅 `_fight_monster` / `_use_bomb` 消费，simulator.py:662/1207），空地到达不触发，故空地分支不判它们；怪格的战斗事件仍在 `_killable` 排除（真·事件怪 = 独立节点）。
- **教训**：缩点的「自由格 / 独立节点」判定**必须与引擎的可通行 + 事件触发语义逐条对齐**。把「已消事件格」误判成独立节点 = 封死块出口 = 丢解（且 found=False 时静默，最易漏）。可达性是「当前 state 的纯函数」这条铁律，**包含事件抑制态**。

### MT3 验收实测（入口 tokens[:82]，(2,11)→(11,5)，即撞 2M cap 那个 run）
入口态 HP=400 ATK=10 DEF=10；整层自由格 66 → **连通块数 9**；入口边界付代价算子 **2**（kill(7,5) / kill(3,5)）。

| 指标 | 块图（缩点） | 朴素 BFS |
|---|---|---|
| found | True | True（cap=300k 被截）|
| 状态数（指纹）| **253** | 92,149 |
| 展开 / 生成 | 413 / **1,097** | — / 300,001（撞 cap）|
| 出口前沿宽 | **20** | 16（被截）|
| 峰值块 | 74 | — |
| hit_cap | **False（跑完）** | True |
| 耗时 | **~1.7s** | ~15s 仍未完（真值 2M ≈ 104s）|
| 出口峰值 HP | **976** | 976（吻合）|

- **① 秒级**：1.7s 跑完（vs 朴素真值 ~104s 未完）。
- **② 块图前沿（持有维）⊇ 朴素被截前沿：通过**（宽 20 ⊇ 16）。
- **③ replay 逐字段一致**：段内落点 (11,5)@MT3 HP=976；全局 111-token 线路（route 前缀 tokens[:82] + 块图动作）落点 / 属性吻合，**可直接照着走**。样例 29 步（块内零损血，步数非最优只求合法）。
- **⚠ 诚实标注对比局限**：朴素被 cap 截断（非完整真值，2M 真值跑不完正是本方案要治的问题），故 ② 只能对比**被截**前沿；但**块图本身 hit_cap=False（完整跑完）**、且双方峰值 HP 一致，是「不丢解」的强证据——非完全证明（完整朴素永不终止，无法做绝对 ⊇ 对拍）。

### 红线（本会话）
`sim/` **一字未动**（git 确认改动仅 `solver/quotient.py` 等新文件 + 验收脚本，引擎清白）；**93 单测全绿**（注：阶段一裁定段落记的「84 单测」为 2026-06-05 数，随测试增补现为 93）。

**来源**：玩家 2026-06-06 三裁定（分类口径 / 血瓶 v1 从简 + 升级条件 / 验收硬标准），要求按实施计划开工并落盘。MT3 验收脚本 verify_mt3_quotient.py、规模分析 analyze_mt3_quotient.py。

---

## 缩点商图接入 phase1 + 跨段膨胀实测（2026-06-07）

### trigger 算子：撞触发型事件格（小偷暗道型）
`_boundary_ops` 新增第 4 类算子 **trigger**：边界上挂【会触发的到达事件】的**非怪**格（NPC / 踩格机关）。撞它会改地图（开门 / 移怪 / 显隐）= 改变可达性的算子（动态连通性铁律）。
- **修法（seg3 MT2 小偷暗道实证）**：(3,7) 小偷 NPC（entity 123），撞一下触发 `event[3,7]`（openDoor[2,7] + show/move/hide thief），引擎对 noPass NPC 是**停原格、不移入**。其 `hide`（无 loc / 无 remove，simulator.py:1586-1589）只清实体不写 `_suppressed_events` → 事件到达会**无害重放**，但 floodfill 把「未抑制的活事件格」永久当墙 → 撞开后仍过不去。
- `_expand_op` 对 trigger 补「**撞开后再走入一步**」：撞一下触发事件后，若该格实体已清且未陷拦截态，再发同向一步真正踏入——与钥匙门 / 假墙的「开了再走入」两步**完全一致**。门已开 / 实体仍在 / 陷拦截 → 不补步。修后 seg3 块图 found=True。
- **choices 型拦截**：撞商人 / 祭坛 / 老人会 set `floor._event_intercepting=True` 等 CHOICE token；块图无此口径 → `search_quotient` 检测到拦截态即**跳过 + 记录到 `intercept_locs`**（不强解），phase1 在 block 行 + 曲线末尾汇总，作**放开决策依据**。事件**怪**仍 = 独立节点（MT33，`_killable` 排除），不列 trigger。

### #13 接入完成
`phase1._run_block` 用 `search_quotient` 替朴素 `search_segment`；补 `hit_cap` 上报（裁定②被截不静默）+ `intercept_locs` 上报。前 5 段红线全绿：**93 单测**、`sim/` 一字未动、每段 replay 裁判、**seg4（旧 23min 撞 2M cap 的墙）→ 8s 完整跑完、0 hit_cap**。

### ⚠ 跨段前沿膨胀实测曲线（前 10 段，玩家观察(a)）—— 新的墙
| 段 | 层 | 自由格→块 | 段末前沿宽 | 段耗时 | 累计gen |
|---|---|---|---|---|---|
| 4 | MT3 | 67→9 | 10 | 8s | 5,080 |
| 5 | MT4 | 60→14 | 128 | 9s | 9,848 |
| 6 | MT5 | 59→15 | 1,285 | 209s | 108,469 |
| 7 | MT4 | 64→11 | 9,378 | 23min | 956,325 |
| 8 | MT5 | 64→10 | 12,881 | 26min | 650,809 |
| 9 | MT4 | 64→11 | 11,607 | **85min** | 3,460,159 |

- 全程 **裁判 3/3 一致、0 不一致、0 hit_cap、0 软截断** —— 搜索**正确**，纯粹前沿太宽。
- **宽度 1→10→128→1285→9378→12881→11607**：前段约 **×10/段**，到 seg8-9 在 **~12k 饱和**（**非无界指数**，撞到该层不同配置上限）。但 12k 仍太宽：每段要从上万 Pareto 点**逐个重搜**大层 → seg9 单段 85min，10 段共 **2.3h**。瓶颈 = MT4↔MT5 往返穿梭。
- merge 把 seg9 的 117,077 raw 压到 11,607 —— 即 **1.1 万个点在当前价值维（hp/atk/def/mdef/gold/kill + 各 key: / item:）下真·Pareto 不可比**。疑点：很多点只差 1 金 / 1 钥匙却互不支配，**价值维可能过细**导致前沿虚胖。

### 观察(b)：飞行道具 / 商人 / 祭坛在块图下的表现（放开决策依据）
- **飞行道具**：seg4 MT3 起即作 **in-block 拾取自动吸**（顺序无关），value 向量见 `item:fly:1` —— 拾取无碍。（fly **使用**=跨层算子，第一版单层内未放开。）
- **第一个商人**：**seg5 MT4 (6,1) blueShop（商店 commonEvent，entity 131）**。块图撞其拦截态 → **记录 `intercept_locs=[(6,1)]` 不强解**，搜索照常找到目标（宽 128）。当前块图对商店 = **死路节点**（不能做买/不买 choice）→ shopping 价值**不被搜索探索**，只有 route forced CHOICE 时才捕获。放开 = `search_quotient` 在拦截点对 CHOICE 选项分叉（买/不买 × 每件）→ 进一步**乘宽**。
- 祭坛同理（choices 型，未在前 10 段出现）。

### 观察(c)：route 支配检查
seg4 块图最优 HP=976 = **route 持平**（未严格超越，与既往「route 在 Pareto 前沿、两次声称超越被玩家终审否决」一致）。**背景**：route 在 seg2→seg3 间有剧情**属性清零**（HP 1000→400、攻防 100→10，小偷「越狱」线）—— 前 3 段是序章，真正弱英雄博弈从 seg3 起。后续段因前沿太宽未逐点比对 route，且历史教训在先，不擅自宣称超越。

### ⚠⚠ 待裁定（核心）：跨段控宽策略
裁定②原文「控宽先不预设、用数据说话，仅宽度爆时按保底软截断」。**数据现在说话了**：宽度在 ~12k 饱和、每段重搜 20-85min，10/98 段已 2.3h，**当前规模推不到底**。候选（待玩家定，铁律：不擅自把控宽偏好写死）：
- **(A) 启用软截断 cap**（`-c N`，被截点已落盘不静默）—— N 取多少？风险：截掉的点可能是后段最优前身。
- **(B) 价值维粗化** —— 1.1 万前沿多因 gold / 单 key / 单 item 细维互不支配虚胖。合并 / 分桶某些维（如 gold 按阈值分桶）能砍宽，但**阈值非线性**铁律下有丢解风险，需谨慎。
- **(C) 段间更激进合并 / 识别「穿梭往返」段的等价性**（MT4↔MT5 反复穿梭，残留态差异可能多为冗余）。
