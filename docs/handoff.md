# 跨层模拟器开发交接文档

**写于**：2026-05-31（机制侦察阶段封板）  
**目的**：新会话快速上手，不重复上一阶段已确认的结论。

---

## 📌 2026-06-13 收尾·S1 pull_big 独立旋钮 α_big + S2 α_big 扫描（剑盾线；**不**取代下方红钥 Stage-2 主线）

> 本 session 两小步、产品码+守卫已 commit（只读扫描、未改任何逻辑、未碰 door_pull）。

- **S1**：`pull_big` 装独立衰减旋钮 `--alpha-big`，距离衰减 `/(1+dist) → /(1+dist)^α_big`，与 door_pull 的 α **解耦**（根因：一个 α 背"愿意去远"与"目标价值排序"两冲突角色，见 [[project-alpha-dual-role]]）。**默认 α_big=1 字节零回归**、**147 pytest 绿**（守卫：①α_big=1 零回归 函数级+@slow 搜索级 ②满额 G=β·ΔRP₀ 对任意 α_big∈(0,1] 仍是守着引导上界 ③拒 α_big≤0）。
- **S2 扫 α_big∈{1,0.7,0.5,0.3} 关键发现**（`analyze_priorities.py` 重放真引擎读拾取时序；玩家网站回放终审）：
  1. **旧"剑盾"诊断里"盾前扫光 6 个 MT1-5 宝石"前提已失效**——door_pull 落地后基线演化，现基线 α_big=1 盾在 **step422**、盾前只 **3 个**（MT3/MT3/MT4）。（下方 §A line40-41 的 step432/6宝石/576-25-25 是**旧快照·已被本条取代**。）
  2. **α_big=0.7 唯一无异常**：盾 step417、盾前 2 个、HP**583**（略优基线 571/26/24）。
  3. **α_big≤0.5 崩**：硬闯远盾 HP→67（step288）；α_big=0.3 截断（670 步只吃 5 件、终 21/22）。
  4. **四条同深 MT10 入口、无红钥**：MT10 卡死＝α 无关的红钥/门老问题、本次不涉及；**无崩爬**。
- **下个方向**：(a) ~~α_big∈[0.5,1.0] 细扫 × 预算 overnight~~ **✅已做→见下 §S3**；(b) 随机化/遗传搜索（目标层编码 + sim 当 decoder + 真 HP 当 fitness，待独立设计；**fitness 须带"潜力"项、不能只用终 HP，见 §S3.7**）；(c) future：核 scoring 是否处处用 ΔRP（阶梯感知）而非线性近似。
- **S2 构建产物**（ab0.7/0.5/0.3 floorbest jsonl + 4 条 .h5route）按惯例不入仓。

### §S3（本 session·2026-06-13）α_big 细扫 [0.5–1.0] × 双预算【即上方"下个方向(a)"】
> 只扫参、**未改任何产品码**（git status 除 route/ 产物外全 clean）。所有数 `analyze_priorities.py` **重放真引擎**核实（落盘 `route/analyze_recheck_*.txt`，非记忆）。范围 α_big∈{0.5,0.6,0.7,0.8,0.9,1.0} × 预算 {K200/cap120k, K500/cap480k}＝12 格。剑（MT5(11,11)+atk10）**全 12 格 step81**。

终态（最深层·HP 最高态；重放核实）：

| α_big | 120k 终 HP·ATK·DEF | 480k 终 HP·ATK·DEF |
|---|---|---|
| 0.5 | 269·26·25 | 636·25·24 |
| 0.6 | 150·21·22 | 612·26·26 |
| 0.7 | 583·26·25 | 689·26·25 |
| 0.8 | 595·26·24 | 517·26·27（MT10(11,11)） |
| 0.9 | 546·26·25 | 598·26·27 |
| **1.0** | 571·26·24 | **718·26·26** |

1. **零回归锚**（重放核实）：α_big=1/120k ＝ MT10(1,10) **571/26/24**、1169 步、剑 step81、盾 step422——与上方 S2 基线特征逐项一致。
2. **全 12 格止步 MT10**（重放核实）：0.8/480k 落 MT10(11,11)、余 MT10(1,10)；红钥/门老问题，4× 预算未推过（α 无关）。
3. **盾前掉血崩＝结构性**（重放核实·对照盾步）：α_big≤0.6 两预算全部盾前 HP→67（0.5/120k step288<盾309；0.6/120k step300<321；0.5/480k step300<321；0.6/480k step300<321）——4× 预算只把终态拉回、**治不了崩本身**，印证"撑不住≠预算不够"。
4. **α_big∈{0.7,0.9,1.0} 两预算盾前干净无崩**（重放核实；0.7 盾后取 MT8 掉到 53 属正常代价、非盾前崩）。
5. **0.8 非单调**（重放核实）：120k=595 干净；480k=517 且盾前 step312 HP→54 轻坑、终点换 MT10(11,11)——**加预算反而 HP↓**（beam 拓宽选到别的深点），别默认"更大预算=更好"。
6. **终 HP 最高＝α_big=1.0/480k＝718**（重放核实，全局最高）：**预算越大、基线 α_big=1 优势越明显**；与共享 α 扫描同向（软化净负）、别再重扫（见 [[project-alpha-big-sweep]]，一次 cap480k 6 格≈3.4h）。
7. **玩家判定：α_big=0.7 路线骨架最优**——终 HP 虽非最高，但**余血瓶/余钥匙/攻防底子潜力最高、最接近人手、已能不靠商人过一区**。→ **下一步 GA fitness 必须带"潜力"项、不能只用终 HP**（否则会选 718 那条把资源耗尽的）。
- 产物：12 条 floorbest/h5route 按 gitignore（`*.jsonl`/`*.h5route`/`*.txt`）不入仓；**扫描 runner `route/_sweep_runner.ps1` 入仓**（参数化 beam/cap/ctag/alphas，可复用）。

### §S4（本 session·2026-06-13）GA 实现首棒：navigate_to 定向导航器【已落地】
> 严格分步进入 GA 实现，本棒**只**实现 navigate_to + 独立验证，**未碰 GA 种群/fitness/交叉变异/主循环**。已 commit（产品码 `extract/ga_navigate.py` + 验证 `tests/test_ga_navigate.py`，全套 152 pytest 绿）。

- **契约**：`navigate_to(state, goal_cell, zone, step_fn) -> (final_state, moves, reached)`——定向走到单 goal，够到返真态+合法动作串+True，够不到原样返回入口态+False（**原子失败零副作用**）。`extract/ga_navigate.py`。
- **★算法纠偏（重要，记牢免得下次重踩）**：设计稿钉死点2.2 原方案"乐观损血距离贪心单算子"被实测**证伪**——乐观 toll 启发沿途清怪塌缩成平台→退化盲目 BFS，跨层钥匙-门逆梯度绕行被局部界剪掉。改用：**结构 hop 距离**（反向 BFS 建场，门怪当通、纯几何不塌缩）做 GBFS 主键 + **真损血 g_dmg 次键** + **资源向量支配剪枝**。
- **独立验证**（`tests/test_ga_navigate.py`）：3 个人手已知目标对（拿剑 MT5 / 拿盾 MT9 / 小宝石 MT1），全走到、损血**优于 α_big=0.7 人手基准**（拿剑达理论最优 246）。损血上界用 0.7 那条真 route 当参照（乐观下界低估跨层夺钥代价：盾下界 304 但人手实走 1202）。
- **⚠ 已知瓶颈（下一棒必处理）**：navigate_to **慢**——深目标(盾)~2249 pops/14s。它是 GA 内循环、将被调千百次→**GA 主循环前必须先优化导航器性能（缓存/分层）**，否则 GA 跑不动。
- **红线守住**：navigate_to **不读 fitness**；本 session 未碰 GA 种群/fitness/交叉变异/主循环。

### §S5（本 session·2026-06-13）GA 第二棒：navigate_to 缓存提速【已落地】
> 本棒**只**给 navigate_to 加缓存，**未碰 GBFS 算法 / GA 种群/fitness/交叉变异/主循环**。已 commit（`extract/ga_navigate.py` + `tests/test_ga_navigate.py`，全套 154 pytest 绿）。

- **缓存 = 外层 memoization，GBFS 算法零改动**；5 个原导航验证**原封全绿** = 行为未变的硬证明。
- **cache key = `_nav_key`**：按 `_copy_state` 深拷清单取**全部可变态字段**（英雄属性/钥匙/items/gold/各层 entities 门怪道具/假墙/事件触发态/current_floor/dead-won 等）。★**特意不用 `_qfp`**（只编码当前块局部、不含持有资源和远层状态 → 跨层会误命中）；★**纳 gold/items/kill_count**（不影响可达但影响 final_state，不纳会误命中返错 final）。
- **加速**：命中近免费——盾 563044×、浅目标 3000× 左右。但 ★**单次首算不变**：盾深目标仍 16s。
- **⚠ 关键结论：缓存必要不充分**——只消除"重复"、治不了"单次慢"。一区最小 GA 估 ≈ 1.4h（命中 90%）~ 7h（命中 50%）。
- **方向决定（玩家拍板）**：**不再继续优化导航器**——它已证对且够快跑最小验证（7h overnight）。下一棒**搭最小 GA 主循环验范式本身**，导航器优化推迟到"范式验证后、带真实瓶颈数据"再做。

### §S6（本 session·2026-06-13）K500/cap480k 卡死诊断＝结构边界 B【只读诊断·未改码/未跑扫描】
> 诊断 §S3 那条 K500/cap480k（α_big=1）"停在 MT10、终态还剩钥匙和血"是 (A)预算耗尽 / (B)结构边界 / (C)属性墙。**纯只读**：读搜索日志 `route/log_ab1_cap480k.txt`、重放核实 `route/analyze_recheck_*`、源码 `solver/quotient.py`、红钥位置 `data/games51/floors/MT8.json`；**未改任何代码、未跑扫描**。

- **结论：B 结构性边界封死**，排除 A(预算)、排除 C(属性墙)。→ **加算力(960k)无效、不跑**：搜索 480k 已自己提前停、当前预算都没用完，给更多预算治不了"带红钥穿谷开红门的态在 beam 里生不出/被剪"。
- **证据1（A 证伪·日志坐实）**：log `hit_cap=False`、生成 **477,777 < cap 480,000**＝预算没用完（hit_cap 唯一触发＝`states_generated>=max_states`，`quotient.py:565`）；wave 宽度从 wave183 起平滑衰减到 0（…8→1→0，`while wave` 前沿空自然退出，`quotient.py:498`），**非高位戛然截断**＝前沿自然枯竭、搜索空间穷尽，不是被预算叫停。
- **证据2（日志坐实）**：终态 **MT10(1,10) HP718/26/26**（重放核实）；MT10 探了 **169 去重态**（非只摸入口一格）；但已自然收敛＝该探的探完了、**非"还在推进"**。
- **证据3（B 机制·坐实+推断）**：红钥在 **MT8(10,2)**（一区可达层、**非够不到**，`MT8.json:72`），红门 **MT10(6,9)** 后即 boss（win=95,659，log:26）。但**全程前沿 maxHP 态最大总钥匙=1、全是 yellowKey、无一带红钥、更无开红门态**（log 钥匙留存表 MT8/9/10 行）。机制＝拿红钥要绕去 MT8 降 HP ＋ 开红门在 value_vector 上纯损一把钥匙、门后 boss 巨值未吸时被"不开门兄弟"Pareto 支配剪掉＝**延迟满足边界**（Stage-1 fact3/4 老问题：门后价值未吸不进 V、keys==0 门是死边界）。
- **证据4（重放核实）**：α_big **12 条路线 11 条卡 MT10(1,10) 同一点**（仅 ab0.8/480k 落 MT10(11,11)）、4× 预算未推过＝结构边界非预算。
- **★对 GA 的意义**：这坐实 **beam 结构上过不了红钥长臂＝GA 存在的理由**。是 `ga_design.md` 钉死点4 第二阶段（`pickup_key` 红钥 ＋ `open_door` boss 门接进目标集）要验的**正主**。door_pull 软引导已证"短臂(可达门 MT7)治好、长臂(红钥过 boss)没治好"；**GA 终评**（整条跑完看终局 win 巨值、中途绕路降 HP 不进剪枝）才是长臂正解。

### §S7（本 session·2026-06-13）GA 第三棒：fitness 终局局面价值【已落地】
> 本棒**只**实现 fitness + 双权重标定 + 单测，**未碰 GA 种群/选择/交叉变异/主循环**。已 commit（`solver/fitness.py` + `tests/test_fitness.py` + `analysis/calibrate_fitness.py` + `analysis/probe_key_reachability.py`，全套 **164 pytest 绿**）。

- **契约（`ga_design.md` 钉死点3·κ=1 守死）**：fitness **只对一条已跑完死路线的终态评一次分**，绝不参与中途逐步决策、绝不反馈 decoder/navigate_to。潜力项都是这条路线**真实留下的既成事实**（地上血瓶/地上钥匙），不是"守着没拿的期权"→ 结构上不构成 κ=1。
- **公式**：`fitness(final) = 主干 equiv_hp_over_roster(一区 roster) + w_potion·一区地上剩余血瓶 + 钥匙家底 + win_bonus(通关)`；死亡 → `DEATH_FLOOR(−1e9) + 深度`（恒 ≪ 任何活态，给"虽死但更深"爬坡梯度）。**钥匙家底 = w_key·手里余钥匙(已兑现满权重) + Σ_地上够得到钥匙 max(0, w_key−守怪损血)**。
- **钥匙口径（A类不碰 door_value B）**：地上钥匙=这条死路线终态客观还剩的家底（对齐血瓶），按守怪损血**扣血成本**防 κ=1 裸计数（"13 血净赚 1 把"经济学）。**可达=预算门控**：楼梯出发能走过【终态手里有该色钥匙的门】才算够得到（乐观近似：1 把当整色门可过，因钥匙便宜·门→钥匙链自给）；没钥匙的门当墙、守怪打不动断边。可达绑**终态手里钥匙预算**=既成事实(type-A)，**不是**把钥匙按未来开门折算(type-B 第二阶段红钥长臂，第一版不碰)。数据坐实见 `analysis/probe_key_reachability.py`。
- **标定（真对照 cap480k 718耗尽 vs 689高潜力）**：`w_potion=1.5 ∈[1.0,2.0]`、`w_key=39 ∈[26,52]`，**都取稳健区间中值**（非 689 险胜临界：w_potion 反超临界 0.40、w_key break-even 13，都远低于选定值）。分项对账：主干 689 输 **−210**（更瘦→压制弱）、血瓶项 **+375**（地上多 250 血瓶）、钥匙地上项 **+95**（MT4 三把 13 血黄门钥匙·718 已吃掉/689 留地上）→ **净 +260 反超**。**689 反超靠血瓶+钥匙双潜力一起补主干劣势**，缺一不可、每项物理意义干净。
- **单测**（`tests/test_fitness.py` 10 个）：fitness(689)>fitness(718)、死亡态≪任何活态、更深死>更浅死、通关恒+win_bonus、双潜力反超、血瓶线性、钥匙分解、便宜算/贵归 0、`test_key_budget_gating`（清空手里钥匙→黄门当墙→够得到钥匙严格变少＝可达绑预算的硬证）、total 对账。
- **红线守住**：只动 fitness，**未碰 GA 主循环**；fitness 只终评不进搜索剪枝。
- **⚠ 待办（落盘别做·GA 主循环验证后的校准项）**：玩家通关存档（根目录·首次到 MT10 在 **tok788**）可作 fitness 的**第二道独立校准**——但需先**对齐截断口径**（玩家 tok788=刚进 MT10 vs beam 718/689=MT10 内自然收敛，进度不同**不可直接比**）。最佳时机=最小 GA 跑出一区路线后，**三方对照**（GA 路线 / 689 / 玩家 tok788）看 fitness 排序是否合理、GA 是否逼近人类打法。

### §S8（本 session·2026-06-13）GA 第四棒：decode 最小闭环【已落地】+ navigate_to 吸收语义缺陷·路2 决策
> 本棒**只**实现 decode 闭环 + 单测 + 诊断脚本，**未碰 GA 种群/选择/交叉变异/主循环**。已 commit（`extract/ga_decode.py` + `tests/test_ga_decode.py` + 诊断 `extract/ga_decode_diag.py`，全套 **174 pytest 绿**）。

- **闭环成果**：`decode(chromosome, start) -> (tokens, final)` 串 navigate_to（依次定向导航各目标、够不到跳过、死亡/通关停）+ fitness 终评。闭环门 **A[剑,盾] ≥ B[剑,小宝石,盾]** 复现 fitness 对 **689>718**「留潜力 > 耗资源」机制（A 靠血瓶+HP 潜力赢、B 属性高但 HP 低）。
- **⚠ 诊断发现 C[盾,剑]==A 非良性（撤回"符合预期"）**：`extract/ga_decode_diag.py` dump 坐实——`navigate_to(start, 盾)` 这条腿步数=241、终态 ATK 10→21（顺路抓了剑）、与 A 完整 241 步逐 token 相同；物理顺序也是先剑(第81步)后盾(第240步)。排除缓存误命中（cache=None 与带 cache 逐字段同）、排除 decode 重排（leg0=盾 leg1=剑 忠实按序）、排除静默跳过（leg0 reached=True）。
- **缺陷根因（dump 坐实非推断）**：navigate_to **进块即贪婪 `_absorb` 吸光所有零损血道具**。去盾的最优路顺路经过剑格 → 吸剑 → `[盾,剑]` 基因物理上变成 `[剑,盾]`、与 A 逐 token 相同。**decode 没错、navigate_to 符合自身契约**，错在【基因表示与贪婪吸收的接缝】：navigate_to 在"导航"名义下替 GA 做了"拿不拿"的策略决策、**越权**。
- **为什么必须修（玩家游戏知识坐实）**：MT4 那 3 把钥匙很优秀、在"开局→盾"主干路影响范围内（玩家最优解**盾前**取 MT4、而 689/718 是**盾后**取——"何时取 MT4"是真实策略维度、正是 GA 该搜的）。navigate_to 顺路把钥匙吸了 → GA **永远无法表达**"先拿盾、留 MT4 钥匙当潜力"这 689 式策略 → 在 GA 最该发挥处失去控制权。**冲突本质：navigate_to 无差别贪婪吸收 ⊥ fitness"留潜力"理念**。
- **决策＝路2（已和玩家定）**：navigate_to 吸收语义改为【**只吸非潜力标的，潜力标的留下**】。
  - **非潜力标的**＝攻防宝石/剑盾等纯增益（即时增益、跨阈值减伤、无"留着"价值，玩家确认这版塔顺路吸必然优秀）→ **顺路照常吸**。
  - **潜力标的**＝血瓶 + 钥匙（＝fitness 潜力项标的）→ navigate_to **路过不吸**，只能由基因**显式 pickup 目标**触发 → GA 才能控制"何时取/留不留"。
  - **二分判断复用 fitness 已有塔无关口径**（`_remaining_items` 判血瓶、`DOOR_KEY_MAP` 判钥匙），**零新硬编码**、不写死物品 id。
- **★分步纪律**：navigate_to 是**地基件**，先单独把吸收语义改对+重验（**5 个人手验证重绿、缓存没坏**）→ 证对后**下棒再**动 decode/基因支持显式 `pickup_key`/`pickup_potion` 目标。**两步别混**。
- **验证门重估**：**A vs B 仍有效**（两条真不同路线、留潜力>耗资源方向对）；**A vs C 作废**（同路径道具顺序被贪婪吸收架空）。

### §S9 钥匙目标线：detect_key_targets已实现(44候选/三分全中/689式验证门走通/185绿)（2026-06-13）
> **【2026-06-13 已实现·commit cc73094】** detect_key_targets 落地、44 候选三分全中、689 式验证门走通、185 绿零回归。下方【三分口径】保留作口径事实来源、【已实现】块附验证 chromosome 解码结果作下棒参照。**GA 主循环/种群仍未写（分步纪律）。**

**【根因回顾】** 上一棒(路2只改navigate_to吸收)暴露：钥匙是路径必需资源(要钥匙开门才到得了目标)，禁吸会让navigate_to对所有门后目标失能。绕一圈后定论：钥匙不该禁吸——顺路钥匙照吸(最优)，真问题是"代价型钥匙(要打守怪取)进不了基因目标池"，这正是ga_design.md钉死点1早写好、一直没补的pickup_key一等目标。

**【读码坐实：三个地基件都不用改】** navigate_to本就能以钥匙格为目标、用kill算子打守怪绕过去取(剑/盾深目标能达即证)；decode本就能串任意(fid,x,y)goal；fitness的zone_key_potential/zone_ground_key_costs已算钥匙潜力。唯一缺口=detect_big_items只产大件+宝石、钥匙格不在候选池→GA选不到。所以只需新增独立钥匙涌现器detect_key_targets。

**【三分口径(已定死)】** 一区钥匙全集59把，用【真实key-chain afford闭包】(零钥起步、把一区door-wise真拿得到的钥匙色滚到不动点={黄,蓝,红}、铁钥不在内)+门拓扑，一次自然产出三档(非加规则)：
- ①顺路12把(MT3:6/MT8:3/MT9:2/MT6:1)：零损血floodfill白捡+零血算子可推到→navigate_to顺手吸=非候选。
- ②候选44把(含MT4六钥(2,1)(3,2)(3,11)(5,10)(5,11)(9,2))：afford门内但必须打守怪付血才到=有"何时取(等属性高减伤少)"时机价值→进候选池、GA决策。玩家游戏知识坐实"44把都是黄门全开也必须打怪才能到的"、不缩池。
- ③够不到3把(MT2(3,4)(3,5)(4,4))：黄钥但被铁门锁死、铁钥一区拿不到→door-wise每条路都断→不进池。MT2自然落③是修了"FULL afford乐观假装铁门能开"那个bug的结果，非"MT2特判"。
- 判据"门色∉afford闭包"全数据滚出、零硬编码、换塔自动重算=塔无关。③只锁门(铁钥拿不到=硬结构、与属性无关)，守怪可行性留runtime(navigate_to到不了=reached False/decode跳过)，不预判。

**【已实现(2026-06-13·commit cc73094·185绿零回归)】**
1. ✅ detect_key_targets(extract/key_targets.py·独立新函数·塔无关·真实afford闭包·产候选池②=44·只产候选不决定取舍)。实测三分全中：afford={黄,蓝,红}(铁钥不在)、59=顺路12+候选44+够不到3、MT4六钥全在候选、MT2三钥全在够不到、顺路∩候选=∅、三分无交叠且并==全集、与probe_key_targets.triage四项(候选/顺路/够不到/afford)全一致。
2. ✅ 验证门chromosome=[('MT9',9,7)盾, MT4六钥(2,1)(3,2)(3,11)(5,10)(5,11)(9,2), ('MT5',11,11)剑]，8 goal全reached=True。**decode终态 MT5(11,11) HP=212 ATK=23 DEF=21 keys={yellow:2,blue:1} tokens=810、钥匙净增3**。四铁律过：引擎可重放(重放终态==decode终态)、物理先盾后钥匙(盾step240<首MT4钥step445)、≥3钥真取到、用钥开后门(DEF10→20@step240 + 多处KEYS减少开门事件)。早期step10-19在MT3捡6钥是①顺路白捡(非MT4候选)、印证三分区分成立。
3. ✅ beam零影响(detect_big_items/navigate_to/decode/fitness一字未动·git确认无追踪文件改动)。配套tests/test_key_targets.py 11测(7三分+4 gate)、analysis/ga_keytargets_diag.py 诊断器(三分对账+验证门解码 dump)。

**【下个session起点】** pickup_key 候选池已就位但 **GA 主循环/种群还没写**(分步纪律)。下棒：动 decode/基因支持显式 pickup_key 目标 + 写 GA 主循环，让基因从 `detect_big_items ∪ detect_key_targets` 的并池选目标。上面那条验证 chromosome 可直接当下棒的参照样本(已知合法可重放·先盾后钥匙·净增3钥)。

**【其余状态】** 血瓶潜力控制还没做(血瓶非路径必需、不阻塞，留更后面)；navigate_to的skip_potential参数上一棒撤回了禁吸、钥匙恢复照吸=健康基线。A vs B验证门有效、A vs C作废(吸收语义缺陷暴露)。

### §S10 最小GA跑通+不拿盾诊断（2026-06-13）
- · 最小GA(pop12/gen6/单点变异/锦标赛/精英)跑通，爬坡+872单调不降非震荡、未早熟，机器真在进化。201绿、beam零影响。
- · ★最优个体不拿盾(HP469/DEF11/到MT5)，诊断=根因B非A：fitness(含盾)=-1488 > fitness(不拿盾)=-3729，fitness给"拿盾"高分2241、排序689(-725)>含盾(-1488)>不拿盾(-3729)【玩家游戏知识确认此排序正确】。盾减伤价值fitness算对(含盾主干净赚3364即"一区拿盾能回本")。fitness指南针对、一个字不用动；是pop12/gen6/单点变异最弱配置没跨到含盾峰(凑"盾在前+多钥匙"有序子集概率低)。
- · ★玩家真引擎重放确认：GA不拿盾route属实(剑→MT4→宝石→HP469→MT5)；fitness三方排序符合游戏判断。
- · ⚠观察待查(不现在查、别开线)：玩家看GA走MT4那几把钥匙的具体走法"奇怪/偏劣"。可能是GA没训练好(加搜索力度会改善)，也可能是navigate_to对"一串相邻钥匙目标"导航得劣(若是这个，会持续污染每个含MT4基因的fitness、加种群治不了)。等下棒加搜索力度、GA出更像样解后，若MT4段仍劣再单独查navigate_to多钥匙导航。
- · 下棒方向(根因B修法·搜索侧)：加交叉(OX/PMX) + ★"初始种群注入一个含盾个体"诊断——注入后GA留住并改进它=纯变异够不到(加力度能解)；注入后又淘汰回不拿盾=选择/fitness有更深洞(矛盾、要查)。比盲目放大pop/gen信息量大。fitness不动。

### §S11 GA三组对照结论 + ★序列有效性根问题(待修)（2026-06-14）

**【三组对照结论】**
- · B注入含盾→留住并改进到-1452(≈含盾锚-1488)、8代不丢；A光交叉(-3647)≈C纯变异(-3650)、都漂回无盾。
- · ★真杠杆是注入、不是交叉(A≈C说明光加交叉没用)。
- · ★病因=延迟满足在GA选择层现形：含盾在排序未优化时fitness -4601~-9940<无盾-3650→贪心选择在它被打磨翻盘前就淘汰它。注入+精英(elite2)护种子越谷(gen4翻-1452)。A/C的随机含盾种子被打磨前就被无盾淘汰。
- · B末代最优GA把注入的"盾first"自己进化成"盾排第3(先2钥再下深盾)"=689式雏形。
- · 成本：B冷交叉585s，全规模pop25/gen15冷≈38min(解释原40min卡死)。修法(下阶段)=持久化decode_cache跨run复用让深盾全局只冷算一次。

**【★序列有效性根问题(玩家发现·下一棒优先做·进化前必须解决)】**
- · 现象：B序列说"剑第7个目标拿"，但剑实际在第2-3目标间就被navigate_to顺路吸了。根因="拿到"定义只是navigate_to走到该格、没查"走到时那格是否还有东西/是否第一次踏上"。
- · 严重性：这是最初C==A缺陷(navigate_to顺路吸剑致[盾,剑]变[剑,盾])在GA层的复发。GA以为在搜"剑排第几"的顺序，实际剑总被顺路吸、排第几都一样=GA在该维度空转、产生自欺序列。
- · 玩家定义的有效性（修正版·去掉"第一次踏上"）：一个目标在某一步"真正拿到"⟺ navigate_to走到该目标格那一刻、那个道具【确实还在该格上】且【被这一步拿走】。【不要求第一次踏上】——因为后期有隐藏物品需触发开关才出现，拿到它时未必是首次踏该格；判定锚点是"拾取那刻道具在不在/被没被拿"，不是时序。反面(无效)：该道具在更早某步已被顺路吸走、这步走到时格子已空=假拿到=自欺位置。
- · ★影响：自欺序列让GA的fitness评估建立在假顺序上→继续进化=在有bug的评估基础上加力度、搜越狠离真相越远。所以【序列有效性必须在继续进化前修】，B含盾结论待此修复后复核。
- · 可能的解(下棒想清楚)：(i)落地有效性定义(目标"这步真拿到"=navigate_to走到时那格道具还在且被这步拿走)；(ii)更根本——把"顺路必吸"的目标(剑/顺路宝石)像顺路钥匙那样剔出基因池(它们位置物理决定、非策略决定、GA控制不了顺序、放进去只造自欺序列+浪费维度)。这俩可能是同一解两面：剔除顺路必吸目标→自欺序列从根消失。呼应玩家早拍的"剑盾顺序不该GA搜、物理决定"。
- · 这个修正让"剔出顺路必吸目标"方案更优：与其运行时逐目标判"这步拿到时还在不在"(要处理隐藏/开关/时序、复杂)，不如从源头把"顺路必吸"目标(剑/顺路宝石)剔出基因池→自欺序列没机会产生、也不用纠结时序。两者可并存：剔出基因池=根治顺路必吸目标；有效性判据"那格当时有东西"=对保留目标(盾/代价钥匙)的校验护栏。

**【两笔债(下棒一起修)】**
- · ①注入个体fitness自检没生效(注入的-9940不在含盾区间却没拦)；②交叉vs变异精炼贡献没隔离(B是注入+交叉、没单独跑注入+纯变异)。

**【下一棒方向】**
- · 优先修序列有效性(地基)，再谈继续进化(加力度/隔离交叉/成本优化)。

---

### §S12 剔剑撤回 + 解码后规整方案（2026-06-14 晚·验证铁证）

- · ★剔剑方向撤回(玩家质疑+铁证推翻前提)：前面"剑反正被吸、排序无效"只在含盾解成立。组1无盾铁证：剑早(fit-4792/HP652)vs剑晚(-21618/HP30)Δ+16826——剑排序在无盾解有真实价值、fitness算对剑减伤。剔剑会杀掉这+16826=剥夺GA学"无盾路线剑该早拿"。组2含盾：[剑,盾]≡[盾,剑]终态全同Δ0=§S11自欺(去盾顺路吸剑)。
- · 结论：剑留池(不剔)、不写死剑顺序(违反偏好涌现红线)。fitness指南对(给剑早+16826)→GA没搜出"先拿剑"是搜索力问题(同含盾解过早淘汰)、非fitness洞。
- · ★§S11真解=解码后规整(非剔除)：decode逐目标时若目标格已空(被前腿顺路吸)→就地规整成"第j腿已兑现"、产normalized_order(真实完成序)。含盾[盾,剑]与[剑,盾]折叠成同一normalized(去重)、无盾[剑,5钥]与[5钥,剑]不折叠(有效序存活)。规整不改fitness值、只让等价基因评估去重、把搜索力省给有梯度的维度。封板件(decode/navigate_to/fitness/detect_*)一字不改、新增规整包装层。
- · ✅【规整必做层已实现】(2026-06-14 commit d49a58d·216绿/两护栏过)：eval层(ga_loop.py make_decode_fitness_eval)产 normalized_order(真实进包先后序)当 fitness 缓存键去重；新增 _taken/_decode_with_order(复刻封板 decode 目标串+逐腿进包追踪)、封板件 decode/navigate_to/fitness/detect_* 一字未改、beam 字节零回归(4守卫过)。护栏铁证：★无盾[剑,5钥]≠[5钥,剑] norm 不同(剑早 fit-4792/HP652 vs 剑晚 -21618/HP30·Δ+16826 信号存活·有效序【不被误折叠】=生死线过)、含盾[盾,剑]≡[剑,盾] norm 全同(去盾顺路吸剑→规整把剑摆回盾前·自欺折叠·终态全同 Δ0·fitness 不变)、一致性测试 4 基因 _decode_with_order 终态==封板 decode。可选层(种群规范化、动变异交叉算子)仍留后续。
- · ⚠成本墙仍在(独立问题)：含盾+交叉全规模pop25/gen15冷算38min/卡死(深盾反复冷算26s)。规整减重复评估但不治"单条含盾冷算贵"。跑全规模含盾训练前要先解决(持久化decode_cache跨run复用让深盾全局冷算一次)。
- · 三待拍板已定并落地：范围=必做层(normalized当缓存键去重)、位置=eval层(make_decode_fitness_eval内)、护栏=含盾折叠/无盾不折叠两单测。
- · ★方法论教训(记忆已存)：差点剔剑、被玩家"GA产出过次优不先拿剑路线"的铁证拦下。代码推"该剔"、玩家游戏知识+GA自己的次优输出证"该留"。真实游戏验证>代码推断又一次兑现。

---

### §S13 小规模验方向(B) + 成本墙实测 + 拆墙【✅已完成】（2026-06-14·验方向7c2ddc2 → 拆墙6c1de0f）

- · 🎯【拆墙✅已完成 2026-06-14·commit 6c1de0f·231绿】`PersistentNavCache`(dict-like注入navigate_to·内存→磁盘→冷算·原子写tmp+os.replace·版本桶=sha256(格式‖塔数据指纹‖导航代码10文件import闭包))。**实测拆墙：暖vs冷63×**(off70.4s/cold72.1s/warm1.1s·深目标盾26s全局只冷算一次·跨run磁盘读回62条0miss)、开/关GAResult逐字段相同(缓存不改结果)、改sim走新桶不返旧值(版本失效焊死·真改文件try/finally铁revert+字节核对)、默认persistent=False自建{}字节等价。grep坐实全项目扣血6处全在`sim/simulator`(地形毒735+领域973+3战斗1195/1228/1322+事件脚本2076)、战斗值算`sim/combat`、指纹零遗漏。封板件decode/navigate_to/fitness/detect_*不改、beam4守卫字节零回归。接入=`build_harness(persistent=)`+`ga_loop.py --persistent`开关；缓存桶`cache/nav/<tag>/`已入.gitignore。**下一步=路线图②上规模跑一区好解(追平/超689·拉tok788三方对照)；跑前先 `python extract/ga_loop.py --persistent` 暖桶(首次冷算一次落盘、之后调参热启)。**
- · ★B验方向三结论（pop12×gen8/seed20260614·注入含盾种子+交叉0.3·规整开/关对照·216绿零回归·诊断钩子normalize+stats默认字节等价）：
- · ① 规整去重生效**但必做层不改进化轨迹**：规整开真fitness调用68 vs 关79=省11/79(14%等价基因折叠)；★GAResult逐字段相同(gen_best/best_individual/n_unique全=True)=实证「必做层只省评估开销、不改爬坡」。**想用规整让爬坡更快/更好→必做层做不到、须上『种群规范化』可选层(population层去重·§S12留的后续)；本棒没做(方向没定型先别优化·同昨天选B不选A)。**
- · ② 含盾稳定涌现：爬坡-4939→-1444单调不降(Δ+3495)、末代最优含盾(MT4终态HP293/24/22·fit-1444略超含盾人工锚-1488)、注入种子+精英2保住不淘汰；pop8×gen3探针也涌现→小规模稳。
- · ③ 无盾解剑早占优(★验2b正确判法=用无盾解·不用含盾解)：79去重解中无盾21、剑真进包10个；剑早(位≤2)mean-6976 vs 剑晚(位≥4)mean-15279、阶梯断层(第2-3约-6.7~-7k vs 第4+约-15~-16k)；人工锚X1=[剑]+5钥-4792 vs Y1=5钥+[剑]-21618·Δ+16826复现§S12铁证。fitness指南对(奖励剑早)、GA小规模搜得出。
- · ★成本墙实测(独立问题·规整治不了)：pop12×gen8规整开(冷·填decode_cache)646s、gen0单代159s(深盾MT9(9,7)冷算26s/态反复触发)、cache饱和284条后末代降4-32s；规整关(热cache·同seed)仅0.4s。**实证:规整省fitness调用(68vs79)、不省导航冷算(热cache才省)。** 全规模含盾pop25/gen15冷38min墙仍在(§S12)。
- · 🎯★下一步=【拆成本墙】(已和玩家定)：**持久化 decode_cache 跨 run 复用**(深目标navigate_to全局只冷算一次·后续run热启)。理由(i)方向已验对+一区目标集稳定(剑盾钥宝石detect涌现固定)→现在建cache不白费(昨天"方向没定先优化会白费"顾虑已消除)；(ii)它是后面一切前置——不拆墙没法反复跑反复调。
- · 拆墙后路线图(顺序铁律·别跳)：①拆墙(持久化cache)**✅已完成(commit 6c1de0f·63×·231绿)**→②上规模跑一区好解(追平/超689、拉tok788三方对照)**【←下一步】**→③一区站稳后才专门打红钥长臂(§S10二阶段主验证·需注入含红钥种子对抗延迟满足·★非"加序列尾端")。**★红钥不是现在做**：下方旧🎯钥匙价值/红钥段降级为"③阶段输入"、勿当下个session主攻。

---

### §S14 验尺：fitness 在「先拿下面(B) vs MT9拿盾冲MT10(A)」上判得准吗？【✅尺准·只验未改】（2026-06-14）

> 上规模跑②前玩家先逼问的根本前提：三方对照要"比潜力"，而潜力依赖 fitness 能判对一个没验过的决策——【先拿下面属性(已兑现) vs MT9拿盾就冲MT10(下面没拿·留满地潜力)】谁优。怕"到MT10门槛"奖励"拿盾冲上楼靠满地没拿的东西刷虚高潜力"的猥琐解(κ=1 影子变体)。本棒**只验 fitness 准不准、不上规模/不跑大GA/不改 fitness**；标尺=人类已知"先拿下面(B型)更优"(689/tok788 都是 B型)。脚本 `analysis/verify_fitness_rush_vs_collect.py`(未 commit·一次性)、权重用 GA 真实尺 w_potion=1.5/w_key=39。

- · **★结论=尺准**：6 组 A/B 配对【全部 B>A】。fitness 给"先拿下面"高分 → 三方对照可用 fitness、GA 会被正确引导(奖励兑现属性、不被满地潜力骗)。
- · **分项对账(total·主干equiv_hp·血瓶项·钥匙项)**：
  - B型(先拿下面)：**tok789(玩家·首进MT10)=-215**(HP265/26/24·主干-4067·血瓶3675·钥177) > **689=-725**(HP689/26/25·主干-3327·血瓶2400·钥202) > **718=-985**(HP718/26/26·主干-3117·血瓶2025·钥107)。【玩家路线 fitness 最高=人类最优·尺方向对】。
  - A型(拿盾冲上楼·decode 从 build_start)：**A1[盾]=-5180**(HP82/**21/20**·主干**-9380**·血瓶**4200**·钥**0**)；A2[盾,MT10]=-5180(=A1)。
  - 同起点对照 B'(decode整池·排除起点不一致)：**-5072**(HP64/24/21·主干-8717·血瓶3600·钥45)。
- · **★关键机制=B 赢在【已兑现属性】、A 输在【满地潜力虚高但不顶用】**：每组 Δ主干都大正(689 vs A1=**+6053**、tok789 vs A1=+5313、B' vs A1=+663)；而 **A 的血瓶项是全场最高(4200)**、Δ血瓶在所有组都负(A>B·-525~-1800)=A 确实在刷地上潜力——**但被主干压倒 3~9 倍**。正是玩家担心的猥琐解结构，fitness 没被骗。
- · **κ=1 防线生效(钥匙项)**：A 弱攻(21)→地上够得到钥匙守怪损血≥w_key=39→ground 全折扣到 0(够得到仅3把·key_ground=0)、手里 0 钥→钥匙项=0。弱属性自动拿不到便宜地上钥匙=防"留着不拿刷计数"。
- · **⚠ 唯一软点=血瓶项(留作③输入·本棒不改)**：血瓶**不按可达/守怪成本 gating**(不像钥匙)、纯名义计数→冲上楼解的血瓶项被如实抬高(A 最高)。当前被主干碾压故不翻盘；但同起点 B' vs A 仅 +108(主干+663+钥45 抵 血瓶-600)、对 w_potion 敏感(w_potion>~1.77 该薄组会翻)——真·优化解(689/tok788)主干领先 5~6 千、任何区间内 w_potion 都不翻。**若日后属性不再决定性、血瓶项是最先暴露的口子**。
- · **🔎附带铁证(强化结论)**：A2=[盾, MT10入口] 解码**到不了 MT10**(navigate_to reached=False·终态仍卡 MT9(9,7))——拿盾(0钥/HP82/21-20)连 MT9→MT10 都过不去。"拿盾就冲上楼留满地"猥琐解**物理上自败**：不先拿下面资源根本活不到 boss。fitness 对活态无"推进度"项→A 卡 MT9 与到 MT10 同分、对照仍成立。

- · **🎯🎯 玩家定的北极星(记牢·潜力对照的终极口径)**：**一区最标准的潜力对照 = 【打完 boss(过 MT10)后比属性和潜力】**——都过同一关、终点线共同、才真可比。当下"卡 boss 前比潜力 + 验 fitness"是**过渡**(因算法暂时构造不出打败 boss 的解)。**等 GA 能过 boss 后，潜力对照升级成"过 boss 后比"**。本棒"到 MT10 门槛"验尺只是过渡期权宜。
- · **下一步不变**：尺已验准 → 回到路线图②【上规模跑一区好解·追平/超689·拉 tok788 三方对照】，跑前先 `python extract/ga_loop.py --persistent` 暖桶。

---

### §S15 序列有效性根治：禁区寻路(否决调换方案)（2026-06-14）

> 本 session 只落盘方向决定、未实现禁区寻路、未改 navigate_to(玩家红线)。下个 session 做这个大工程。

- · **【问题】** GA基因=目标序列，但navigate_to去目标X时会顺路吸"排X后面的目标Y"(如[盾,剑]去盾顺路吸剑)。§S12规整在评分层把normalized改成真实进包序(剑摆第1)，但这只是"调换顺序"。
- · **【★玩家否决"调换"、定"禁区寻路+判无效"(核心决定)】**
  - 调换方案的致命缺陷(玩家论点)：调换默认navigate_to走的是最优路，但navigate_to只是【贪心认为优】、非客观最优。剑【不在盾必经路上】，navigate_to因剑减伤大、损血贪心【主动选了经过剑的路】。把[盾,剑]调换成[剑,盾]=承认这个贪心选择、并【永久掩盖"先拿盾走一条不碰剑的更优路"这种可能】——若那条路更优，GA和玩家永远发现不了，序列顺序变得毫无参考性。
  - 正确做法：navigate_to去目标X时【强制不准踏入任何排X后面的目标格】。找不到"不碰任何后续目标格"的路到X→序列【判无效/淘汰】，【绝不调换】。这样"先盾不碰剑"的更优路若存在就能被探索；若不存在则序列本就无效该淘汰。只有这样GA搜出的序列才严格有效、不被navigate_to的bias掩盖更优解。
- · **【★§S12护栏已实测确认安全(玩家游戏知识确认)】** 玩家确认：【去MT4那5把钥匙的路不碰剑格MT5(11,11)】。所以无盐解[5钥,剑]去拿钥匙时本就不经过剑格→带禁区(剑格设禁区)约束自然满足→剑老实排最后被拿→[5钥,剑]【不被新规则误伤、保留、+16826信号完整存活】。新规则只判死真正谎报序列(如[盾,剑]去盾必经剑)、不误伤无盐解有效顺序。护栏边界已钉死、下个session不必再纠结无盐解误伤问题。
- · **【★这是大工程：给navigate_to(封板件)加"带禁区寻路"能力】** (去X时把所有排X后的目标格当禁止踏入)，动封板件核心寻路、非"跳过空走"小手术。
- · **【判序列无效怎么落地(下个session定)】** navigate_to带禁区找不到路→reached=False→这条基因整条淘汰(极差fitness)还是该目标fail？对GA选择/进化的影响要想清。
- · **【下个session起步】** ①读码评估navigate_to加禁区寻路的真实规模+风险(GBFS能否加禁区约束/改动多深/会否影响复用的quotient原语/缓存键/导致大量序列fail) ②"判无效"怎么落地 ③出方案给玩家确认再实现。护栏已实测安全、专注工程实现即可。先别实现。

---

### §S16 GA目标层级重构：块为目标(取代单物品+规整)（2026-06-14）

> 本 session 只落盘根本性方向决定、未实现、未改产品码、未动禁区/规整(玩家红线)。下个 session 先坐实能否落地、要动多深，再定走不走这条根本重构。归档两份只读诊断证据：`analysis/ga_forbidden_failrate_diag.py`(禁区 fail 率·0红/长基因100%黄)、`analysis/ga_block_granularity_diag.py`(连通块粒度·实证 MT4 五钥 atk10 静态分4块/atk26 清怪后并1块)。

**【背景：绕了好几轮的症状，根因是目标层级错了】** 从§S9起，GA目标池是【单个物品格(fid,x,y)】(detect_key_targets钥/detect_big_items大件)。围绕"单物品目标"打了一长串补丁都在治症状：§S11-S12规整(剑顺路吸致序列谎报)、§S15禁区寻路(navigate_to去X顺路吸排X后的目标)、判无效+可区分给分、连通块打包候选钥。诊断还发现长基因100%黄腿(五钥互碰绕路)。

**【★玩家的根本纠正(本section核心、推翻单物品目标层级)】**
- 之前做过【缩点】：整座塔缩成若干"内部零损血连通(块内移动不扣血)"的连通块；门和怪物是【连接块的边】，边权=过边代价(打怪损血/耗钥匙)。这是一张图：节点=零损血连通块，边=门/怪。
- ★GA要搜的序列【不该是单物品顺序，而是"连入连通块的顺序"】：不是"剑→盾"，是"剑所在的块→盾所在的块"。进一个块=块内所有零损血可达物品(钥/宝石/剑)【自然一起到手、无顺序问题】(块内不扣血、先拿哪个都一样)。
- ★单物品不该是GA目标单元，【目标单元=块】。物品是块的附属。
- 这从根上消除之前所有症状：①五钥互碰→块内无顺序、块间顺序就是要搜的东西；②剑顺路吸谎报→剑是剑块附属，连盾块若必经剑块=图上剑块是盾块前驱=【拓扑事实非navigate_to bias】；③navigate_to路线bias→退化成"算两块之间那条边的代价"。
- ~~★禁区/规整在块层级【不再需要】——它们是"单物品目标"这个层级错误的补丁。提到块层级，根没了。~~ **【更正 2026-06-14：本条"禁区不再需要"是顾问误置的推断、非玩家主张——玩家从未说禁区不需要，只给了"连通块"概念。实测后更正：规整(块内假序)块为目标后退役✓；★禁区(跨块顺路吸)一直需要、不被取代——只要 navigate_to 是 cell 级自由选路，跨块顺路吸就不可避免、与块为目标无关。详见 §S17。】**

**【下个session评估(玩家纠正后我给的方向，先坐实不实现)】**
1. ★缩点现状：quotient.py的_free_cells/_qfp商空间是不是就是这个缩点？现在【缩成"块为节点、门/怪为边"的全塔图】了吗，还是只缩单层/局部给beam用？有没有现成全塔块图，还是要组装？dump坐实。
2. ★命门=边权依赖属性(动态)：块间边是门/怪，边权随英雄属性变(打同怪atk10 vs atk26损血天差地别→已见MT4 atk10时4块/atk26时1块)。"连块顺序"搜索的边权是动态的(每连一块属性变、后面块分不分都变)。坐实现有缩点是静态还是能随属性动态重算；这动态性怎么和GA搜块顺序结合(它恰是时机价值来源：属性高了边权低)。
3. 改目标层级爆炸半径：动detect_key_targets(§S9)/detect_big_items/ga_decode/ga_loop(基因=块序列)/navigate_to(退化成算块间边代价?)/fitness。是重做目标定义层、非小改。
4. 上块为目标后禁区/规整是否被取代(玩家主张：是)。
5. beam零影响(块图改动在extract/、beam用自己的quotient)。

**【★MT3/MT4五钥楼层存疑(玩家提出、未解决、下个session一并查)】** 报告反复说"MT4五钥"，但玩家记得那五把是【MT3】的。§S9候选②标的"MT4六钥(2,1)(3,2)(3,11)(5,10)(5,11)(9,2)"楼层id对应的真是游戏MT4吗？会不会§S9涌现就把楼层标错(候选池楼层错=严重)？下个session dump每把钥fid+坐标+楼层地图特征，玩家对游戏认。

**【玩家判据(落盘备用)】** 拿钥匙的时机有意义⟺路上有怪/地形/门拦路(=不能免费到达、不在同一零损血连通块)；同一零损血连通块内先后顺序完全不影响。←这正是"块为目标"的理论基础：块内无序、块间(隔门/怪)才有时机价值。

**【路线】** 下个session先坐实(缩点现状+边权动态性+爆炸半径+五钥楼层)，玩家看清能不能落地、要动多深，再定走不走"块为目标"这条根本重构。禁区本体先不实现(是块为目标之后的独立下一棒、不被块为目标取代——§S17 已坐实纠正)。

---

### §S17 块为目标坐实通过 + 实现待办(用CC) + 禁区独立待办（2026-06-14）

> 本 session 只读码坐实+评估、未实现、未改产品码。新建一次性只读诊断 `analysis/ga_block_initial_model_diag.py`(复刻 quotient 引擎口径·三测全绿)归档 analysis/。下个 session 做块为目标(先出方案、玩家确认再实现)。

**【坐实结论(实跑铁证)】**
- 断言1单向吸纳✅：块划分(`_is_free_tile`+floodfill)【零读勇者属性】、是 entities/门/事件纯函数→块身份天然跨属性恒定(玩家"块定死"对、上 session 判"无稳定身份"错)。属性动态全在"边"(`_killable` 只在 `_boundary_ops`)。重放 route 77 次并块全是勇者块吞邻块、零反例(单向性成立)。
- 断言2初始块可计算✅：初始态 CC 一区 144 块、min(cell) 稳定 id。MT4 五钥+宝石 6 格→4 初始块(两对相邻钥各同块、块内一起拿无序)。

**【★决策(玩家拍)】**
- 用 CC(连通分量)不用 BCC：零损血连通=一块。BCC 会用割点切碎块(MT4 宝石块切三段)、坏"零代价一起拿"。CC 是 quotient 现成。
- ★块为目标【不是禁区银弹】(报告诚实提醒、玩家认)：解决块内假序(五钥互碰、规整退役)；【跨块顺路吸/navigate 选路 bias(剑被顺路吸根本)依然存在、禁区仍独立需要】。先做块为目标(结构清理铺干净地基)、再块层级做禁区。块为目标≠取代禁区。

**【块为目标实现待办(下个 session、先出方案玩家确认再实现)】**
1. 块涌现层：初始态 CC 算全塔静态块集合+稳定 id(min cell)、复用 quotient 现成 CC/`count_floor_blocks`、cell 目标映射到所属初始块。
2. 基因表示：cell 有序子集→初始块 id 有序子集，build_min_pool/pool/meta 改块 id，变异交叉在块 id 层。
3. ★护栏：detect_big_items/detect_key_targets 仍吐 cell(块层叠其上、不改 detect)→beam pull_big 零影响(关键护栏守住)。
4. 空块目标：基因引用的块运行时已被勇者块吸纳→decode 优雅跳过(像跳已拿的)、是真实涌现非自欺。
5. MT10/MT33 类"远端改图(关门=块分裂)<10 特殊事件"：断言1 只测了合并没测分裂。评估一区范围要不要现在 census 特判(MT10 boss 一区边界、MT33 三区)还是碰到再特判。
6. 规整必做层退役(块内假序根除后)：删还是留着无害、护栏单测怎么改。

**【禁区独立待办(块为目标之后)】** §S15 禁区方案(navigate_to 带禁区寻路、B' 不碰 quotient、判无效给可区分差分 INVALID_BASE+进度、护栏:去 MT4 五钥不碰剑格已确认安全)仍有效、是块为目标之后的独立下一棒。治跨块顺路吸。

**【五钥楼层】** §S9 没标错=游戏第 04 层(title 铁证)、候选池正确。玩家记忆可能与 MT3 第 03 层那片顺路钥(①MT3:6)记混、建议游戏里对一下两层左上钥群。

---

### §S18 块为目标实现方案【已确认方向·三岔路已拍·待实现·玩家终审】（2026-06-14）

> 接 §S17：本 session 只出实现方案 + 玩家拍三岔路、**未实现、未改产品码**。顺手清了 §S16 标题与【路线】行两处"禁区被取代"残留（与 §S17 一致：块为目标取代单物品+规整、**不**取代禁区）。下个 session 先坐实岔路 A 的 (a)、再实现（玩家终审每步）。**本方向优先于下方 🎯②上规模跑——块为目标是②的前置结构清理（§S16 根因：目标层级错了）。**

**【方案六条·对应 §S17 待办】**
1. ·块涌现层：`solver/quotient.py` 新增 `partition_floor_blocks(state)->list[frozenset]`（与 `_is_free_tile`/`_DELTAS` 同口径），`count_floor_blocks`（现只返计数·不返块集）改调它＝复用口径不另造。全局稳定块 id=`(fid, min(cell))`（cell 不含 fid·跨层 min 会撞·必带 fid）。extract/ 新增 `block_targets.py`（驱动层）建 `{(fid,x,y):块id}` 映射 + dump（一区 144 块 + 10 目标 cell 各属哪块·预期 pool 缩到 <10 块 id＝块内假序消失的直证）。诊断脚本 `_partition`/`_stable_blocks` 逻辑已验证、可提升为产品函数。
2. ·基因表示：`build_min_pool` 内把 detect 吐的 cell 经映射折成块 id 去重、`meta` 标块 id 来源；`pool`=块 id 列表；`run_ga` 的 _mutate/_crossover/_random_individual **逻辑零改**（元素 cell→块 id）。**decode 改一处**：块 id→代表 cell(=min(cell))→`navigate_to`（封板件**不动**·进块即 `_absorb` 吸光整块·ga_navigate.py:209；到达判定对道具格/空地都成立）。
3. ·★护栏（关键·守 beam 零影响）：`detect_big_items`/`detect_key_targets` 签名返回值不变（**仍吐 cell**）；块涌现叠在 detect 输出**之上**（build_min_pool 内）、detect 函数体一字不改 → beam 的 `pull_big`/`pickup_bonus`（消费 detect 的 cell·走自己的 search_quotient·不经块层）零影响。硬验收 = beam 4 守卫字节零回归全绿。
4. ·空块跳过：基因块运行时已被勇者块吸纳 → `navigate_to` 立即 reached=True/无新货 → 优雅跳过（像跳 _taken）。块内维度=真实涌现；**跨块顺路吸残留=禁区治、本棒不声称解决（§S17）**。
5. ·分裂特判：见岔路 C。
6. ·规整退役：见岔路 B。★§S12 +16826 铁证块为目标后**更稳固**——剑 MT5/钥 MT4 不同层必不同块→[剑块,5钥块]≠[5钥块,剑块]**天然不折叠**（从"规整不能误折叠"升级为"结构上不可能折叠"）。

**【三岔路确认（玩家拍·2026-06-14）】**
- ·A 初始态来源 = **先坐实 (a) 可行性再定**：(a)=从 data/floors/*.json 构造 `_is_free_tile` 可用的干净 floor、摆脱 route 依赖（但 `_zone_floor_cells` 的 is_wall 口径≠`_is_free_tile`·需新写"静态 JSON→自由块视图"·可行性未坐实）；(b)=沿用诊断脚本重放 route 首踏快照（已验证 144/4 块·但挂一条 route 文件依赖）。**下个 session 第一步=读 simulator 层加载能力、坐实 (a) 能否干净实现，再在 (a)/(b) 间定。**
- ·B 规整处置 = **保留+最小适配**：`_decode_with_order` 进包追踪 cell→块、normalized 改块 id 序、护栏单测改块版；规整无害（§S13 实证只省评估不改爬坡轨迹），等禁区棒根治跨块顺路吸后连同护栏一起删。
- ·C 分裂特判 = **暂不特判+补一次性分裂验证**：MT33 在三区不管；MT10 不产采集目标块（全在 MT1-MT9）、navigate_to 实时算块对分裂免疫；只补一次性验证（扩展诊断 test_B 检测 MT1-MT9"1 旧块裂≥2 新块且勇者不在"反例·跑标尺 route 确认零反例→一区安全）。MT10/二三区分裂特判留过 boss/出一区再做。

**【护栏单测改法（tests/test_ga_normalize_guard.py）】** 无盐不折叠（生死线）→块版 [剑块,5钥块]≠[5钥块,剑块]（天然成立·更强保证）；含盾折叠→命运绑禁区（禁区做完重写为"走不碰剑块的路/判无效"）；一致性→随 decode 块化更新。

**【下个 session 起步（顺序铁律）】** ①坐实岔路 A 的 (a)（读 simulator 层加载）→定初始态来源 ②实现块涌现层 + dump 坐实 144/4 块 ③改 build_min_pool/pool/meta + decode 块化 ④补分裂验证 ⑤护栏单测块版 ⑥全程 beam 4 守卫字节零回归 + pytest 全绿。**玩家终审每步、每步先确认再下一步。**

**【红线】** 不取代禁区（§S17）；跨块顺路吸/navigate 选路 bias 不解决（留禁区下一棒）；detect 不改保 beam 零影响；用 CC 不用 BCC；偏好涌现（不写死剑盾顺序）。

---

### §S19 岔路A坐实通过：(a)纯静态JSON构造可行【定用(a)·实跑铁证·待玩家拍后实现】（2026-06-14）

> 接 §S18 起步铁律①。本 session 只坐实 (a) 可行性 + 定 (a)/(b) + 出 (a) 版块涌现层方案、**未实现产品块涌现层、未碰 navigate_to/detect/beam**。新建一次性只读实验 `analysis/ga_block_static_view_diag.py`（同口径对照、归档 analysis/）。**玩家看坐实结果拍板后再实现。**

**【坐实方法】** 同一脚本对每个一区层【同时】算 (a) 纯静态构造 与 (b) 重放首踏快照的块划分，**逐格断言 free_all 与块集完全一致**（不只块数、是 cell-for-cell 同分区）。底层 `_partition`/`_is_free_tile`/`_zone_blocked`/`_DELTAS` 全是 quotient 产品函数本体（import 自 §S17 诊断、零口径偏差）。

**【①_is_free_tile 字段依赖（坐实，带 file:line）】** 读 11 类字段：terrain 维度/tile 值、`change_floor`、`zone_blocked`(领域15/阻击18伤格)、`_no_pass_tiles`(tiles.json noPass)、`_in_alive_monster_footprint`(大怪占格)、`entities` 实体 tile、`_tile_to_item`(道具格)、`_live_arrive_event`→`events[loc].enable`+`_suppressed_events`、模块常量(WALL/DOOR/AUTO_OPEN)。`_is_free_tile`/`_zone_blocked` **均不读 hero**（块身份跨属性恒定·印证 §S17 断言1）。

**【②静态 JSON 齐不齐（坐实）】** **齐**。11 类里 10 类直接来自静态 JSON（floors/*.json + tiles.json + monsters.json，`load_floor` 一次性建好）；唯一运行时字段 `floor._suppressed_events` 在初始态**恒为空集**（`simulator.py:350` 硬初始化 `set()`，只有 step 触发事件才填）。一区 **MT1–MT9 零 footprint 大怪、零领域/夹击/阻击怪** → `_zone_blocked`/`_in_alive_monster_footprint` 恒空/恒 False。一区 events 稀疏干净（MT5-9 无 events；MT1 king `enable:false`；MT2/3/4 list-event 全挂 NPC 实体格或空地、判非自由全靠静态 events+entities）。⇒ **构造静态 floor 视图 = 调 `load_floor` 但不进任何 step**，无需新写派生逻辑（`make_static_state(fid)`：`load_floor(MTx.json)` 套最小 GameState、`current_floor=fid`、hero 仅占位）。

**【③(a) vs (b) 块集是否一致（实跑铁证）】** **重叠面逐格完全一致**：10 个 (b) 能捕获的层（MT1-MT10）free_all+块集 **cell-for-cell 全等**，共有层总块数 **144=144**；**MT4 五钥+宝石归属 (a)=(b)：都落 4 初始块、`bids=[0,4,6,10]` 完全相同**（坐实"MT4→4 块"）。**唯一差异=(a) 额外覆盖 MT0**（1 块·标尺 route 首踏从没进过 MT0→(b) 整层盲区）；145=144+MT0。**非不一致、是 (a) 无盲区的优势。**

**【④定 (a)（理由）】** ✅ 用 **(a)**：① 与已验证 (b) 在重叠面严格等价（实跑铁证）；② 摆脱 route 文件依赖（无产品码挂 route 这一地基级债）；③ 初始态天然未操作、不靠 (b)"route 首踏前未改层"隐含假设；④ 无盲区（覆盖 MT0、不受 route 走没走过影响）；⑤ 实现极简（= `load_floor` 不进 step、`make_static_state` 已在诊断脚本验证）。

**【坐实附带·待确认（仅泛化出一区才需复核，本棒一区干净不受影响）】** (a) 二区起若 list-event 挂空地且带方向/条件 `if`（如 MT41 假墙隐藏怪），`_live_arrive_event` 只看 `enable` 不解析 `if`→初始态一律判非自由（保守偏紧、不会误判为自由、安全）；(b) `_suppressed_events` 还承载 `autoEvent` once-key，一区 autoEvent 全空（已验证）、高层需另查。MT0 块结果无 (b) 真值可比（route 盲区）、但 1 块开阔层是最低风险情形。

**【(a) 版块涌现层方案（§S18 步②起·不变·初始态来源已定=(a)）】** `partition_floor_blocks(state)` 包装已验证 `_partition`（CC floodfill），每层用 `make_static_state(fid)` 喂初始态（不再重放 route）；其余 §S18 六条（基因块 id 化、护栏 detect 仍吐 cell 保 beam 零影响、空块跳过、分裂验证、规整保留最小适配）全不变。**实现待玩家拍。**

---

### §S20 块为目标实现完成：4 dump 验收通过【已 commit 25221af·下一步=禁区】（2026-06-14）

> 接 §S18/§S19 起步铁律：本 session **端到端实现 §S18 六条 + (a) 初始态来源（§S19 定）**，4 dump 验收过、玩家拍板、已 commit `25221af`（块为目标地基）。**块为目标只解决块内假序——序列有效性的另一半（跨块顺路吸/navigate 选路 bias）未做、下一步=禁区（§S15）。禁区做完才跑训练。**

**【实现落地（commit 25221af·256+7 绿·beam 47 字节零回归）】**
- 块涌现层：`solver/quotient.py` 新增 `partition_floor_blocks(state)->list[frozenset]`（CC floodfill·与 `_is_free_tile`/`_DELTAS` 同口径）、`count_floor_blocks` 改调它（复用口径·保 (nblk,nfree) 契约）。`extract/block_targets.py`（新·驱动层）：`make_static_state(fid)`（§S19 (a)·`load_floor` 不进 step）+ `build_block_index` 建 cell→块 id 映射。块 id=`(fid, min(cell))`（含 fid·跨层 min 会撞）。
- 基因块 id 化：`build_min_pool` 把 detect 吐的 cell 折成块 id + `block_markers`（进包判据）；`run_ga` 变异/交叉逻辑零改（元素 cell→块 id）。decode 改一处：块 id→代表 cell→`navigate_to`（进块即 `_absorb` 吸光整块）。
- ★护栏守 beam 零影响：`detect_big_items`/`detect_key_targets` 签名/返回值不变（**仍吐 cell**）、块层叠其上 → beam 4 守卫字节零回归全绿（47 绿）。
- 空块跳过两型：①自欺（块已被顺路吸空→navigate 零新增）②不可达（navigate reached=False→原子跳过·state 不变·任何基因永远可解码）。
- 规整保留最小适配（§S18 岔路 B）：`_decode_with_order` 进包追踪改块 id 序、护栏单测块版；**禁区棒根治跨块顺路吸后连同护栏一起删·本棒不删**。
- 单测：`tests/test_quotient_blocks.py`（块划分契约 3）+ `tests/test_block_targets.py`（块 id/代表 cell/cell→块自洽/与 partition 一致/确定性 5）+ `tests/test_ga_normalize_guard.py` 升级块版（含 +16826 哨兵）。

**【4 dump 验收结果（玩家拍板通过·2026-06-14）】**
- ① pool 折叠：detect 10 物品 cell → pool **7 块**；五钥 5 cell → **3 钥块**归并（MT4 两对相邻钥各同块·块内假序消失·直证）。
- ② +16826 块版生死线：无盾 [剑块,5钥块] vs [5钥块,剑块]——剑块 MT5 ≠ 钥块 MT4 **异层必不同块 → 天然不可折叠**（从"规整不能误折叠"升级为"结构上不可能折叠"）；Δfitness(剑早−剑晚)=**+16826.0** 守住、pin 成单测哨兵。
- ③ 一区分裂验证：MT1–MT9 重放标尺 route 查 1475 同层步、"1 旧块裂≥2 新块"反例。**四个目标层 MT1/MT4/MT5/MT9 零分裂**；唯一反例 = MT2 step#71（见下例外）。
- ④ 块为目标 GA 解 decode：块 id→代表 cell→吸光块·空块跳过两型都对、玩家眼看走得对。

**【★已知例外·MT2 小偷 NPC 分裂（doc-only·不加特判码）】**
- 现象：dump③ 报 MT2 step#71 一起块分裂（旧块锚 (1,2) → 裂成 [(1,2),(1,10)]）。
- 成因（实跑钉死·非猜测）：MT2 开局小偷 NPC 越狱剧情走位——`analysis/ga_block_mt2_split_diag.py` 重放定位实体 123（MT2.json events (3,7)=小偷 thief）`move left:2 down:2` 走到 (1,9) 暂时截断走廊、其后 `move`+`hide` 离开。是**脚本剧情 NPC 走位**、非机关/领域结构事件。
- 为何无害：**MT2 无目标块**（采集目标全在 MT1/MT4/MT5/MT9）；块是初始态静态标签（只算一次·从不中途重划分）；navigate_to 实时算连通对瞬时阻断免疫。单向吸纳"只合并不分裂"对**所有目标块**成立。
- 为何不加特判码：它不碰任何目标块、加特判是空操作。**留认知给将来**：万一目标块挪到 MT2、或别层有类似剧情 NPC 走位致目标块分裂，须复核此例外。

**【★下一步=禁区（§S15·不是跑训练）】** 块为目标只治**块内假序**（五钥互碰·规整退役地基铺好）；**序列有效性的另一半——跨块顺路吸 / navigate 选路 bias（剑被顺路吸根本·§S17）依然存在**。下一棒 = §S15 禁区（navigate_to 带禁区寻路·判无效给可区分差分·B' 不碰 quotient）治跨块顺路吸。**禁区做完才上规模跑训练**（规整连同护栏在禁区棒一起删）。

---

### §S21 禁区实现完：序列有效性两半都治了【已 commit 53dd071·5 dump 验收过·下一步玩家定（删规整 / 跑训练）】（2026-06-15）

> 接 §S20：块为目标只治**块内假序**；本 session 实现 §S15 禁区、治**序列有效性的另一半（跨块顺路吸 / navigate 选路 bias）**。B' 寻路 + 序列级判无效 + 进度分 + 禁初始 block_cells，5 dump 验收过、玩家拍板、已 commit `53dd071`。**序列有效性两半都治齐了。下一步玩家定：先单独棒删规整 / 先跑训练。**

**【实现落地（commit 53dd071·263 绿·beam 4 守卫字节零回归·规整暂留）】**
- B'（`extract/ga_navigate.py`·**不碰 quotient**）：`navigate_to`/`_navigate_to_uncached` 加 `forbidden` 参；新 `_zb_with_forbidden(state,forbidden)` 把跨层禁区 `(fid,x,y)` 按 `state.current_floor` 投影成本层 `(x,y)`、并入 `_zone_blocked` 喂 `_free_cells` 的 `zone_blocked` 参（quotient 零改）；新 `_absorb_avoiding` 复刻 `_absorb` 唯一改 free 算法带禁区。**空 forbidden 走 None 快路 = 字节零回归。** 缓存键纳 `frozenset(forbidden)`；编辑 ga_navigate.py 自动冲掉旧磁盘桶（version_tag→`afb82ae29619b565`、disk_hit=0 符合预期）。
- 禁区集（`extract/ga_decode.py`·decode 本体封板不动·加纯函数 `forbidden_after`）：每腿 forbidden = 排 X 后**未进包块的初始 block_cells 并集**（禁初始 cell·待拍1）。
- 序列级判无效 + 进度分（`extract/ga_loop.py` + `solver/fitness.py`）：`_decode_with_order` 返 4 元组（加 verdict）；带禁区 fail → 不带禁区重跑一次区分情况 1（够不到·跳过不淘汰）vs 情况 2（被禁区逼死·判无效）；任一腿情况 2 → 整条 `INVALID_BASE(-2e9)` + 进度分 `1000×导航块 + 10×最深层`（防退化）。`make_decode_fitness_eval` invalid 直接返进度分、**绝不进 fitness（κ=1 不破）**。INVALID_BASE 严居死亡带（-1e9）之下、带腿内梯度拽 GA 往有效序爬。
- 护栏：`test_ga_normalize_guard.py` 升 4 元组（仍禁区关·守封板规整不变量）；+16826 哨兵守住。

**【5 dump 验收结果（玩家拍板通过·2026-06-15·总 413s 全冷算）】**
- ① +16826 生死线：禁区**开/关都** Δ(剑早−剑晚)=**+16826.0**（X1/Y1 都 valid）——禁区没碰坏无盾剑早核心信号、块边界哨兵没漂。
- ② 判无效真实率：n=16 随机块基因 **5/16 = 31% ∈ [0%, 33%]**（小实验三档预测内）。按长：len2–4=0%、len5=25%、**len7=100%（4/4）**——7 块全排随机序几乎总有腿被迫穿后续块；但 ④ 证有效 7 序存在、进度分把 GA 往那拽（**'先短后拼长'认知**）。
- ③【核心场景·这一整轮的赌答】[盾块,剑块] B' 真搜：去盾·禁剑块 **reached=False（无路）**、去盾·不禁 reached=True 步数 242 **剑被顺路吸=True** → 这张图去盾唯一通路必踏剑块、**"先盾不碰剑"根本不存在 → 判无效不换序**。意义：禁区把"先盾不碰剑存不存在"从**拍脑袋假设**变成**算法实测定夺**；这次定夺是"不存在→无效"，但**机制对**——换塔/换位若那条更优路真存在禁区会探出来（有效）、而调换会永久掩盖它。**否决调换坚持禁区的全部价值实测兑现；被修 bug（假装去盾·242 步顺手吸剑·谎报先盾）= 最早 catch 的"序列顺序毫无意义"的根、现在治了。**
- ④ GA 块解禁区下 decode：全 7 块（池序剑→盾→钥）`invalid=False` navigated=7、终态 MT4(7,9) HP72 ATK23 DEF21 keys{黄1蓝1}、665 步可回放、normalized=执行序无误吸。
- ⑤ beam 4 守卫(big_item_pull/door_value/region_potential/pull)+nav_cache=62 绿；全套 **263 passed**、1 无关既存 warning。

**【★规整待单独棒删（待拍3 已定）】** 序列有效性两半治齐（块内假序=块为目标 §S20·跨块顺路吸=禁区 §S21），规整（`_decode_with_order` 进包追踪 + `test_ga_normalize_guard.py` 护栏）**已无实际作用**、是 §S18 岔路 B 留的最小适配。**禁区已跑通验证有效 → 规整可单独棒删**（连同其护栏单测；删时 +16826 信号已由禁区 + 块异层不可折叠双重保住）。

**【★下一步=玩家定（删规整 / 跑训练）】**
- (甲) 单独棒删规整：已无用、§S18 待拍3 已定"禁区跑通后单独删"。低风险、清地基。
- (乙) 上规模跑训练：序列有效性完整、干净地基。**须带认知**：len7 随机序 100% 无效（禁区严是设计内）、GA 靠 INVALID_BASE 进度分梯度从短有效序往长有效序爬（'先短后拼长'）；有效长序确实存在（④ 池序即是）。

玩家定先删规整还是先跑训练。

---

### §S22 规整删完（A 外科式）：序列有效性彻底治齐 + 地基清爽【已 commit cd98476·261 绿·下一步=跑训练】（2026-06-15）

> 接 §S21（待拍3 已定"禁区跑通后单独删规整"）：序列有效性两半治齐（块内假序=块为目标 §S20·跨块顺路吸=禁区 §S21）后规整去重作用归零。本 session 单独棒删规整（**A 外科式**·先出读码报告、玩家拍范围再删）、已 commit `cd98476`。**序列有效性彻底治齐、地基清爽 → 下一步=上规模跑训练。**

**【删除范围 = A 外科式（读码报告后玩家拍）】** 规整 =「算 normalized_order 真实进包序」+「拿它当 fitness 缓存键去重」两件事。读码定两条边界：
- ★`_decode_with_order` 是**混合体**：进包追踪的 `taken` 被禁区 `forbidden_after(taken=)` 共用（ga_decode.py:53-62）→ **非纯规整、不能删**。
- ★normalized_order 有**第二用途**：3 个 dump 脚本（含跑训练 `run_ga_zone1_scaleup.py`）拿它显示「真实进包序」给玩家用游戏眼睛判解（剑第几进包/盾排哪/顺不顺）→ **留作诊断**。
- **删（纯去重消费·全在 `make_decode_fitness_eval`）**：`norm_cache` + 查存命中 + 写入 + `normalize`/`stats` 参（无调用方传）。去重**交回 `run_ga` 基因元组缓存 `fit_cache`**（同基因不重评·ga_loop.py:138-144）。
- **留**：`_decode_with_order` 一字未动（normalized 照算照返回）；`taken`/`newly`/`_is_taken`/`_taken`/`_goal_markers`/`forbidden_after`（禁区核心）。
- **测试**：`test_ga_normalize_guard.py` **整删**（含盾折叠测=已被禁区判无效覆盖 §S21③；执行一致性测=可删）；**+16826 生死线哨兵迁 `test_block_targets.py`**（与块 id 哨兵同源·都守"块划分别漂"）、改挂封板 decode+fitness（不经 normalized_order·与规整解耦）。
- **零损失论证**：块为目标灭块内假序、禁区把含盾 [盾块,剑块] 直接判**无效**（不再"折叠"）→ norm_cache 折叠的等价基因不再产生（§S13 实测规整仅省 14% 评估且不改爬坡轨迹·那 14% 正是含盾折叠）。

**【验收 4 项（玩家确认通过·先给 dump 再 commit）】**
- ① +16826 哨兵 `test_block_targets.py::test_sword_early_16826_sentinel` 绿；dump 实测**禁区开/关都 Δ(剑早−剑晚)=+16826.0**（剑早-4792/剑晚-21618·X1/Y1 都 valid）。块边界漂移哨兵副作用保留。
- ② GA 块解禁区下 decode 正常 + **进包序显示在**：整池 7 块禁区开 → invalid=False navigated=7、终态 MT4(7,9) HP72/23/21、tokens=665、normalized(真实进包序·7块) 完整打印（判读眼睛还在）。
- ③ **beam 零影响**：4 守卫(big_item_pull/door_value/region_potential/pull)点名 **47 绿**。
- ④ 全套 **261 passed**（= §S21 的 263 − 删的规整 3 slow 测 + 新增哨兵 1 slow 测）、1 无关既存 warning；grep `norm_cache|test_ga_normalize_guard|normalize=` 全仓**零残留**。

**【★下一步=上规模跑训练（乙·序列有效性完整·干净地基）】** 跑前先 `python extract/ga_loop.py --persistent` 暖桶。**须带认知**：len7 随机序 100% 无效（禁区严=设计内）、GA 靠 INVALID_BASE 进度分梯度从短有效序往长有效序爬（**'先短后拼长'**）；有效长序确实存在（§S21④ 池序即是）。目标=路线图②上规模跑一区好解（追平/超 689·拉 tok788 三方对照）。

---

### §S23 路线图②首次上规模跑训练：干净地基上健康爬坡 + GA 解玩家游戏眼睛验收过【已 commit·下一步=扩 pool 跑长基因】（2026-06-15）

> 接 §S22（序列有效性彻底治齐、地基清爽）：本 session 首次上规模跑训练（pop15/gen10/seed20260614/交叉0.3/注入[盾]·禁区开·暖桶 5.5s）。**GA 在治齐的地基上健康爬坡、含盾涌现、基因==normalized 序列严格有效、route 经玩家游戏眼睛验收（剑被 navigate_to 顺路吸、先拿下面、逼近 689）→ 已 commit。下一步=扩 pool 跑长基因。**

**【首次上规模结论（健康）】**
- **健康爬坡**：gen_best −4645→−1002（末代−初代 +3643）、基因 len 2→4→5（**'先短后拼长' work**：短有效序起步、进度分往长有效序爬）。
- **有效率升**：初代有效 11/15 → 末代 15/15（INVALID_BASE 进度分把 GA 往有效序拽·§S21 爬坡健康度成立）。
- **含盾涌现** ✅（gen2 起稳定带住·§S11 延迟满足这次没翻车）。
- **序列严格有效**：GA 最优基因（执行序）== normalized（真实进包序），无块内假序、无跨块谎报（两半治齐的实战验证）。
- **route 拿剑确认（玩家游戏眼睛）**：剑 MT5(10,10) **不在基因、但实际被 navigate_to 顺路吸**（去其他目标的路上踏过剑格）→ 剑维度不进基因也能拿到。导出脚本 `analysis/ga_export_best_route.py`（复刻同种子 GA·禁区开 decode·拼 83 前缀·封板 sim replay==decode 终态才写盘）、产物 `route/ga_zone1_s22_best_hp171.h5route`（gitignore·脚本即复现锚）。
- **逼近 689**：fitness GA=−1002 vs 689=−725（差 277）。**GA 不含红钥**、差距主因 689 多跑 MT10 兑现潜力（GA 终态止于 MT9·钥匙家底 318 最高=攥 3 黄钥没花）。
- **没踩血瓶软点**：主干 −5070 vs 689 −3327（差 −1743 < 2000 阈值）、靠兑现属性非满地血瓶撑分。
- **耗时**：冷跑 GA 净 ~30min（首次填 navigate_to 持久桶）、暖跑 **5.5s**（miss=0·disk_hit=415）。pool 当前 **7 块**。

**【★机制澄清（记牢·不预设权重）】** GA 靠 **fitness 评分**让好块组合**涌现胜出**、**绝不给块预设权重**（预设权重=写死塔特定策略·违反"偏好涌现"红线，见 CLAUDE.md「不要做的事」）。分工铁律：**"哪些块有价值"由玩家游戏知识圈定加进 pool**（人挑候选、缩小搜索空间）、但 **GA 对 pool 内块一视同仁随机选**、靠 fitness 搜出好组合（机器做取舍）。人圈候选 ≠ 给权重——前者缩搜索空间、后者替算法做决策（违红线）。

**【★三个坑（扩 pool 前须知）】**
1. **扩 pool 后禁区无效率更高**：当前 7 块 len7 已 100% 无效（靠进度分爬）；扩到 len10-15 随机序无效率只会更高、**初代可能大批废** → 扩 pool 后**先测长基因无效率**。
2. **高到爬不动 → 限长 / 注长种子**：若无效率高到 GA 起不来，**限基因长度上限** 或 **注入长有效种子**帮起步（§S21④ 池序即长有效序、可做种子）。
3. **别让一夜全烧冷算**：冷 pop15/gen10=30min vs 暖 5.5s；扩 pool 产生**大量新块组合要冷算 navigate_to**、6 小时算力大半可能烧在冷算上 → **别一夜跑完没几代**。先暖桶、配置时把冷算预算算进去。

**【★下个 session 流程（玩家今晚要跑上训练再睡）】**
1. **dump 一区全部候选块**（不只现在 7 块·是所有值得当目标的钥匙块/宝石块/属性块）给玩家**圈定加哪些进 pool**。
2. **测扩 pool 后长基因（len10-15）禁区无效率**（坑①）。
3. **据无效率定配置**：限不限长度 / 注不注长种子 / pop+gen 多大（≈玩家想要的时长且别全烧冷算·坑②③）。
4. **配置玩家确认后启动跑一夜训练**。

---

### §S24 过夜两机跑完：A长基因/B短基因结果(校正版) + 早熟三旋钮+minlen 实战有效【已 commit·下一步=§S25 升级标尺评估】（2026-06-15）

> 接 §S23（首次上规模跑·三个坑·四步流程）：本 session 扩 pool 到 34 块、两机并行跑一夜（A 长基因抗早熟 / B 短基因），早熟修法实战有效、没再现昨晚 gen2 塌缩。**最深层不是判断基准（A/B 都到过 MT9、A 到了又回来）；基准是 fitness + 实际拿到的属性/块。** 玩家从两机结果看出当前 fitness 标尺（红钥前比潜力）不够硬 → 下一步升级标尺（§S25）。

【过夜两机结果(校正版)】
- A长基因(pop40/limit10-18/immig6治早熟)：gen1就-1566、15块、ATK26/DEF25、13腿、拿一堆纯黄钥(终态5黄钥)fitness被钥匙拖累。到过MT9后又回来。
- B短基因(pop20/maxlen8)：最终-968、192/23/22、1黄、5腿、到过MT9。
- ★最深层不是判断基准(A/B都到过MT9、A到了又回来)；基准是fitness+实际拿到的属性/块。
- 早熟修法(三旋钮)有效、没再现昨晚gen2塌缩。

**【本 session 代码（已随本次 commit）】** `extract/ga_loop.py` 加 `min_len` 基因长度下限旋钮（补齐已有 `max_len`），贯穿 `_random_individual`/`_mutate`(下限处禁 delete)/`_crossover`(不足则补)/`run_ga` 4 处；缺省 `min_len=None`＝字节零回归（注入种子是显式给点·不受下限约束·只 GA 自生成基因守 [min,max]）。launcher `analysis/ga_overnight_34.py` 加 `--minlen`。机器 A 用 `--minlen 10 --maxlen 18`。pytest 非 slow **265 绿**、零回归。

---

### §S25 ★下一步方向(玩家定·下个 session 先评估别实现)：判断3红钥硬目标 + 判断4全舍纯钥块（2026-06-15）

【★下一步方向(§S25、玩家定、下个session评估)】玩家从两机结果看出现在的fitness标尺(红钥前比潜力)不够硬，要做两件升级标尺：
- **判断3红钥硬目标**：把"拿到红钥"当每个基因的硬目标/硬筛。红钥极难拿(基本要25+攻击)、比boss稍易但仍难门槛、只有攒够攻防的优秀基因走到→极好衡量基因好坏、比"比潜力"硬得多准得多。同路线图③(红钥长臂)提前激活当标尺、往北极星(§S14过boss比属性)走。
- **判断4全舍纯钥块**(玩家机制事实)：navigate_to不是"去一个目标点"、是【用最小代价去某点、路上自己决定去哪拿钥匙/血瓶再去目标】→开门必需的钥navigate_to自己绕去拿、不需GA放钥块。钥匙后效性极小(只守门怪+开门、跟宝石/属性后效没法比)、寻路本就能分清哪些钥提前拿优。所以纯钥匙块可全舍、GA只搜有大后效的宝石/属性/红钥块顺序。同"剑不该在基因池"理。红钥块留、纯黄钥块舍。
- **判断1/2(间接)**：短基因属性不够→红钥硬目标推GA往长走；navigate_to无后效→不改它(早否决)、靠GA+禁区搜全局权衡。

【下个session评估(先评估+坐实命门、别实现)】
- ★坐实命门：navigate_to会不会自己绕去拿开门钥(找"需开门到、开门钥不在直接路径"的深目标实测)——这是全舍纯钥块的地基、实测别靠记忆。
- 红钥硬目标怎么落地：红钥位置/到达门槛(坐实25+攻击)/fitness怎么改成硬目标(改fitness封板件、评估beam零影响+16826哨兵影响)/红钥深目标长链路GA搜得到吗(注入含红钥种子§S13)。
- 全舍纯钥块后pool剩几块(34块里几个纯钥块)。
- 两件怎么配(红钥块留/纯钥块舍/禁区块为目标判无效适配)。

---

### §S26 ★红钥末腿头部精英 + 判断4 砍钥块【实现完成 + 短跑活体验证·已 commit】（2026-06-15）

> 接 §S25（玩家评估完拍板）：判断3（红钥硬目标·方案c 强制末腿）+ 判断4（全舍纯钥块）已**落地产品码 + launcher 接线 + 单测 + 短跑活体验证 + commit**。★标定反预期（薄态够不到红钥·失败烧满 max_pops）→ 方案升级为【头部精英末腿】（每代只 base-fitness 前 elitek 条跑红钥末腿·与 pop 无关→过夜放大 pop 末腿不爆炸）。**"可行性存疑"已澄清：是【扎实度门槛】非【不可达】——末腿契约测证 689 式扎实态够得到（见下）。**

#### ★方案升级：头部精英末腿（标定反预期 → 玩家方案甲）
- **标定反预期（重大）**：§S24 最强基因终态(ATK26 DEF25 HP184@MT5(1,8)·**薄**)navigate_to 红钥 `('MT8',9,1)`，**max_pops 2000~16000 全 reached=False·失败烧满 pop 预算**（30s@2k / 118s@8k / 228s@16k·≈14ms/pop）。若【全员】每代跑末腿 → 过夜被烧满预算拖死。
- **★可行性已澄清（解除"存疑"）**：标定的"够不到"是【薄态(HP184·位置 MT5(1,8))】；末腿契约测 `test_redleg_reach_from_solid_689` 证【扎实态】s689(HP689/ATK26/DEF25·过 boss 路线终态)navigate_to 红钥【**够得到**】(reached_final=True·navigated=1·红钥进包)。→ **同属性(26/25)不同扎实度(HP184 vs 689)够不到/够得到**，正是 §S26 命门（玩家游戏知识定：够不够看【整个状态扎实度】非属性数字）。**红钥可达·门槛是状态扎实度(HP+位置)·非属性数字。** 判断3 作牵引有效：GA 朝扎实态爬、够到即触发 B。
- **方案甲 = 头部精英**：每代 base fitness 排序后，**只前 elitek=3 条**跑红钥末腿（薄基因失败烧满预算·限只 3 条→过夜可控·与 pop 无关）。`elite_cache` 跨代不重烧（top-k 收敛后基本全缓存命中）。`redcap=8000` = 薄态烧满约 118s 的护栏（暂定·首个 reach 后据实测调）。

#### 实现（产品码 + run_ga 钩子 + launcher + 测·全 commit）
**【extract/ga_loop.py】**
1. `_decode_with_order`：加 `final_goal/final_markers/final_max_pops` 三参 + 红钥末腿段（主循环后追加一腿 navigate_to 红钥块·禁区空；失败=原子空操作 state 原样不判 invalid；末腿后走 `track`=基因∪{红钥块} 重算 `newly/side/normalized` 保序列严格；`reached_final`=终态红钥 marker 是否被吸·直查最稳）。`final_goal=None` 字节零回归（仅 verdict 多 `reached_final=False` 键·无害）。
2. `make_decode_fitness_eval`：加 `final_goal/final_markers/final_max_pops/bonus_b` 四参；`reached_final` 真→`base+bonus_b`（**wrapper 加·非 fitness 本体·守 κ=1：B 是"红钥已在手"的已兑现价值非潜力**）；invalid→`_invalid_score`。默认 `bonus_b=0+final_goal=None` 字节零回归。
3. `build_min_pool`：判断4 砍纯钥块·`pool=[剑块,盾块]+宝石块`（7→4）；`meta["keys"]` 仍留（+16826 哨兵用 meta 拼基因走封板 decode·不靠 pool→哨兵零改自守）。
4. `build_harness`：涌现 `red_block/red_markers`（从 `detect_key_targets` colors 全集取 redKey 色折块·非手写坐标）。实测 `red_block=('MT8',(9,1))  red_markers={('MT8',10,2)}`。
5. `run_ga`：加 `elite_eval_fn/elite_k`（**塔无关**两阶段精英钩子）——base 排序 → 前 elitek 条跑 `elite_eval_fn`（末腿+B）、其余取 base → eff 分数流入 history/selection/log。`elite_cache` 跨代不重烧。默认 `elite_eval_fn=None/elite_k=0` 字节零回归。

**【analysis/ga_overnight_34.py launcher】**：判断4 滤 34→**13**（assert 13·`red_block` 不在 pool）；`base_eval` + `elite_eval`（共享 `decode_cache`·elite_eval 带 final_goal/redcap/bonusb）接 run_ga（`elite_eval_fn=elite_logged/elite_k=elitek`）；`--elitek 3 --redcap 8000 --bonusb 500` 旗标；`dump_best` 末腿重算显红钥 miss/reach + base/+B/eff；on_gen 日志 红钥✓/✗ + redleg 计数。

**【测·270 非slow + 4 slow】**：`test_ga_loop.py` +5 塔无关（字节零回归 / 点火只前 k / miss noop / reach 流 B 单调 / 只评估 top-k）；`test_ga_redleg_contract.py` +4 @slow（**薄态 start→末腿原子空操作终态不变·非 invalid / 689 扎实态→reach+reached_final+navigated≥1+红钥进包 / reach 恰好 +B / miss 不加 B**）。

#### 短跑活体验证（pop8/gen3·15.6min·exit 0·机制全对）
- **①头部精英点火对**：redleg 只在 base 前 3 名点火（gen0 redleg=3·累计 4·与 pop 无关→过夜放大 pop 末腿不爆炸）。
- **②原子空操作活体验证**：每条末腿 miss→`eff_fit==base`（redleg#1 eff=-4732=base·#2 eff=-5180=base）·终态不变·B 没加。**契约测的 miss 半在真实 start 态坐实。**
- **③成本主体是 base 非末腿**：gen0 8.6min 主体是 base 冷算长基因（len13=87s·len4=143s·len5=202s·decode 固有成本）；末腿自身只 redleg#1 烧 54s（薄·慢失败）、#2-4 全 0s（`elite_cache` 跨代摊薄）·**redcap=8000 没打满**（54s<118s）。
- **红钥=✗ 每代（符合预期·非失望）**：pop8/gen3 基因全弱/短（最优=剑种子 len1·HP754/20/10），没达 689 扎实态→末腿正确够不到→原子空操作。短跑只为验耗时不为出解。

#### 北极星两层口径（§S14·B 隔开两层）
- **reached 层**（终态红钥到手）：层内比 `base fitness`（≈HP/属性·RP 剩血潜力轴）、整体 +B 抬到 failed 层之上。
- **failed 层**（够不到红钥）：层内比 `base fitness` 属性梯度（攻防越高越接近能拿红钥）、GA 朝攒攻防爬。
- B=500 中等量级：大到隔开两层、小到 reached 层仍能跟 fitness(689)=-725 比（守过渡基线对照尺）。

#### ★过夜目标摆正（防睡醒误判·重要）
- **红钥很可能仍 ✗（够不到）**：§S24 pop40 跑出的最强才 184/26/25（薄·HP184）·够不到红钥（要 689 式扎实态）。这次 GA 可能又只攒到 184 那种、红钥仍 ✗、reached 层恒空、B 不触发。
- **★这不该叫"失望"**：这次目标不是"立刻够到红钥"，是【看 GA 往红钥(扎实态)爬得怎么样】。红钥末腿是给 GA 往更扎实(更高 HP+攻防+好位置)爬的牵引、base 属性梯度本就指向攒更多、红钥是终极目标。
- **★成功标准 = GA 最优解的 HP/属性比 §S24 的 184/26/25【更扎实了吗】**（HP 涨?属性涨?往 689 那种状态靠了吗?）·非"够到红钥"。**睡醒看"比 §S24 更扎实没(往红钥爬没)"·不是看"够到红钥没"·别误判失望。**

#### 过夜配置（玩家认可·gen 设大·玩家自起手动停）
- `python -u analysis/ga_overnight_34.py --tag A --pop 40 --immig 6 --mut 2 --minlen 10 --maxlen 18 --elitek 3 --redcap 8000 --bonusb 500 --gen 50 --persistent`
---

### §S27 ★★重大方向转向：GA 触顶 → 转课程学习框架 + 双线并行【方向决定·本 session 只 commit+落盘·下个 session 双线启动】（2026-06-15）

> 接 §S26（红钥末腿过夜大跑·本机机器1）：过夜实测**每代约 70min**（gen0 79min / gen1 又 66min·pop40+末腿+长目标导航），50 代要 ~58h、本机跑不完。但本 session 重点不是 GA 结果——**而是玩家与顾问经深度分析锁定 GA 的【结构性天花板】，决定把主攻方向从「调优 GA」转向「课程学习框架」，并双线并行（server 续跑 GA 探极限当基准 / 本机实现新框架）。这是本项目迄今最大的方向转向。**

#### 【一、背景：GA 的根本天花板（不是再调一个旋钮的事）】
经一长串工作（块为目标 §S20 / 禁区 §S21 / 规整删 §S22 / 上规模 §S23 / 抗早熟 §S24 / 红钥末腿 §S26），锁定 GA 的**结构性**天花板（参数修不了）：
1. **价值无法被中间态代理函数表达**——只有走到结局（过 boss 剩余 HP）才知道。`κ=1` 有害、`α` 软化全崩、所有"聪明"旋钮都输给中性——这一长串负向是**同一句话**：价值是不可约的非线性（阈值门控），线性/势能代理喂不出来（见 [[project_shared_alpha_result]] [[project_alpha_big_sweep]] [[feedback_realized_value]]）。
2. **有大后效的【路径决策】被锁在 navigate_to 内部、GA 基因控制不了**——例 MT7 骷髅：硬打扣 210-260 血 vs 绕路（2 钥匙 + 3 史莱姆扣 30+）。GA 基因**只选块、不带路线**，路线全外包 navigate_to → 够不着这层决策。
3. **【深谷/稀疏奖励】最致命**——打一区 boss 的链：打 2 卫兵 → 拿红钥 → 开门 → 打 8 小怪 → 打 boss，**每步损耗、只有打赢 boss 才有奖励**。MCTS 那类靠 rollout 的方法在深谷走不通（随机走必死、几乎到不了谷底）→ 会和之前所有算法一样**避开这条路**。
4. **★GA 做不了课程学习的根因**：基因**不带路线**（只"选哪些块"）、路线仍 navigate 外包 → **无法"从中间状态出发、连路线一起学一小段"**。

#### 【二、★新方向：课程学习框架（玩家认可·本机实现）】
- 用**真实通关存档（剩余 HP=14382）**把"打过一区 boss"切成几个短段（如 段5 打 boss / 段4 开门打小怪 / 段3 拿红钥 / 段2 攒攻防 / 段1 开局），**由存档关键节点定切分**。
- **从最后一段（谷底=打 boss）开始学**、每段从某状态出发搜"怎么走剩最多血"、**价值从谷底往回逐段传播**（段 N 回报 = 本段剩血 + 段 N+1 已学价值）。
- **绕开深谷死穴**：不靠"从谷口 rollout 到谷底"（走不通）、靠"从谷底往回传价值"（每段都够得着）；**段内搜索以状态为中心、路线也在搜里**（管得了 MT7 骷髅那种路径决策）。
- **真实存档是脚手架、非死先验**：用真实可行路径切段 + 定每段起点状态，把无解深谷变成一串够得着的短段（切段不写权重——区别于被反复否定的 α/κ 势能先验）。
- **核心不是"完整 MCTS"**：段切短后每段小规模搜索可能就够、不需 MCTS 大量 rollout。**MCTS 只是每段搜索的候选之一。**

#### 【三、★双线并行（下个 session 启动）】
- **线1（server）：GA 续跑 50-100 epoch 探极限**——验证 GA 天花板在哪、是否误判、当"旧方法极限"基准。GA 投入（块涌现 / navigate_to / fitness / 禁区）可能部分复用到新框架。
- **线2（本机）：实现课程学习新框架**。**第一步＝设计框架 + 实现验证【最后一段（打 boss）】**（最短、谷底、价值传播起点、定生死的小验证）——**不是一上来实现整个框架**。

#### 【四、能复用 / 用不上的】
- **能复用**：`navigate_to`（段内搜索）、块涌现、模拟器、真实存档。
- **用不上**：GA 的种群 / 变异 / 交叉 / 早熟旋钮 / 红钥末腿——另一范式。

#### 【五、本 session 交接】
- 本机 §S26 过夜跑（tag A·seed20260615）跑到 **gen1**、最优 **fitness=-1244**（HP155/ATK25/DEF25·含盾·红钥末腿 miss✗·每代 ~70min）。GA 角色已转 server，**本机此跑去留见 session 末**。
- 本 session **只方向决定 + commit + 落盘 §S27，未实现任何新框架代码**（红线）。
- §S25/S26 支撑脚本已入库（commit `85e74e7`）：命门坐实 / 纯钥块审计 / 末腿标定。

**【★下个 session 第一步】** 线2 本机：设计课程学习框架 + 实现验证**最后一段（打 boss）**。线1 server：续跑 GA 50-100 epoch 当极限基准。
---

### §S28 修红门 Pareto bug + boss 段闭环验证 + ★扫出 V_boss 价值表（课程学习框架可行性坐实）（2026-06-16）

> 接 §S27（方向转向课程学习）。本 session 执行玩家 3 步计划并全部完成：①修红门 Pareto 支配 bug ②验 A/B 真起点端到端搜通 boss 段 ③★网格化扫 V_boss 价值表（框架核心产出 + 可行性判据）。零回归、已落盘。**结论：框架【可行】当稠密奖励。**

#### 【一、修红门 Pareto 支配 bug（solver/quotient.py·守 beam 红线）】
- **病根**：开红门 (6,9) 落到惰性事件格——自由块不增、无 flag → "开门态(少1钥)"与"未开门态(多1钥)"商图指纹**全同** → 开门态被 Pareto 支配剪掉 → boss 真起点搜不通。
- **修法＝修指纹（非修自由格）**：`_qfp` 加 `distinguish_doors` 开关，True 时把「当前层仍关着的门格集合 `_closed_door_cells`」追加进身份维 → 开门(门格 terrain→0 移出集合)后两态分指纹、开门态不再被剪。门类 tile 取自通用 `DOOR_KEY_MAP|SPECIAL_DOOR|AUTO_OPEN_TILES`（塔无关）、**只读 terrain 不解释任何事件**（区别"修自由格"须判事件条件分支=解释自定义事件=踩红线）。
- **★beam 零回归（红线守住）**：`distinguish_doors` 默认 **False=字节零回归**，beam/历史全路不变；仅课程学习穷尽搜显式 True。关时 `_qfp` `return base` 逐字段同旧；门维**追加在末尾** → `fp_by_floor=Counter(fp[0])` 仍取 fp[0]=当前层不受影响。pytest 非 slow **270 passed 零回归**。

#### 【二、boss 段闭环验证（真起点 tok1168 ATK27 DEF27 HP735·穷尽 beam_k=None·distinguish_doors=True）】
| 验证 | 目标 | final_hp(max-HP出口) | 耗时 | 出口Pareto |
|---|---|---|---|---|
| A 核心 boss 战 | MT10(6,1)队长格·单层 | 701 | 3.8s | 7 |
| B 整段到下区 | MT11(6,10)·跨层{MT10,MT11} | **961** | 61s | 107 |
| C 红门已开态起搜(对照) | 同 B | **961** | 60s | 77 |
- **B==C==961 → 红门 bug 已死**（修前 B 的开门态被剪搜不出整段；修后门闭真起点 B 与门已开对照 C 同值）。
- **基准下界＝人手存档出 boss 段 HP701**（tok1250 ATK30 DEF30）。
- **搜索完胜人手**：max-HP 出口 HP961（+260）；**fitness 最优出口 HP841 ATK30 DEF30**（打了 boss 吃战利品·ATK/kills 涨·**独立重放校验 ✓到达 MT11(6,10)**·+140 vs 人手）。
- **出口 Pareto 权衡坐实**：max-HP(961·跳战利品保血) vs fitness-best(841·吃战利品换 +属性)——fitness 正确选战利品路（属性复利>裸血）→ 佐证"段奖励用出口 fitness 而非裸 HP"。

#### 【三、★★网格化扫 V_boss 价值表（框架核心产出 + 可行性判据·analysis/curriculum_scan_vboss.py）】
真起点深拷只覆写 (ATK,DEF,HP)、每点穷尽搜 boss 段取 final_hp。
**遍① delta(ATK,DEF)=final_hp−HP_in @ HP=735（真实起点·死亡剪枝有界）**：
```
  ATK\DEF    24    27    30    33
    24       死    死    94   196
    27      142   226   310   394
    30      224   302   380   458
    33      429   480   531   582
```
**遍② delta 随 HP @ (27,27)**：HP 200/300/400/500/600 → delta=**0**；HP 700/735/900 → delta=**226**（拐点 600→700·拐点上方完全恒定）。

**可行性判据（三轴）**：
1. **delta 随 HP：阶跃 + 拐点上方恒定 ✓**。拐点上 delta=226 恒定(700/735/900)、真实进塔 735 稳在拐点上方 → **可分离模型 V_boss(a,d,h)=h+delta(a,d) 在实际工作区成立**。拐点本身是信号（告诉攒攻防"血/属性不够兑现 +226 战利品、继续攒"）。
2. **delta 随 (ATK,DEF)：结构化非乱跳 ✓**。DEF 每行**完美线性**（+3DEF：ATK27 行 +84 / ATK30 行 +78 / ATK33 行 +51·斜率随攻升而降=回合少则每点防省血少）；ATK 有**台阶**（ATK33 行整体抬升=跨少打一回合阈值）。→ DEF 可稀疏采样+插值、ATK 须密采(每 1-2 点)防漏台阶。即 CLAUDE.md 的"非线性/阶梯"但**有结构可插值**。
3. **成本：唯一摩擦 ⚠（离线可接受）**。单点 3s(弱·死)~182s(33,33)、24 点全网格 ~26min。一张够用表(ATK 密×DEF 疏·单 HP 层)≈1-2h **离线一次性**；在线查表+插值 **O(1)**。

**★结论：课程学习框架【可行】当稠密奖励。** V_boss = h+delta(ATK,DEF)（拐点上方）·delta 对 DEF 线性/对 ATK 弱台阶/HP 拐点上方恒定 → 可粗网格离线预计算+插值 O(1) 服务给攒攻防当稠密奖励，**正面解掉 §S27"价值无法被中间态代理表达"的天花板**——V_boss 就是那个能表达的代理（穷尽搜出来的真值表、不是被反复否定的线性势能猜测）。

**诚实保留（未坐实·按 CLAUDE.md 不猜）**：
- delta=0 平台机制没 trace（"零战斗路线" vs "净零路线"·精确 0 连 5 个 HP 值）→ 标待确认、不推演。
- 弱生还格(24,30 等)在 735 是否已饱和未复核（其拐点可能 >735）→ 严谨表需对弱格补高 HP 档复核。

#### 【四、本 session 交接 / 下一步】
- 入库：`solver/quotient.py`(修红门·门控默认关) + 3 只读脚本(`curriculum_dump_zone1` / `curriculum_validate_boss` / `curriculum_scan_vboss`)。封板件 fitness/decode/navigate_to/detect 零改、beam 零回归(270 绿)。
- **下一步（课程学习框架第 2 步）**：把 V_boss 落成可查的稠密奖励（粗网格预计算+插值），往回照亮**攒攻防段**——每个攒攻防状态查 V_boss(它的属性) 得真实价值梯度，正面解稀疏奖励/深谷死；再考虑段切分往更上游传播。补严谨 V_boss 表前先 trace delta=0 平台机制 + 弱格饱和复核。

---

### §S29 修 events 污染 bug（封板 simulator 授权外科）+ V_boss/fitness 关系厘清 + 小验证配线（★小验证未跑·钉死前提）（2026-06-16）

> 接 §S28（V_boss 表坐实、框架可行）。本 session 做三件**前置**、第四件（小验证）**故意未跑**（玩家叫停先钉死状态落盘后 clear）：①修 §S28 留的 delta=0 平台机制 = **events 污染 bug**（确认+外科修复·beam 47 守卫字节级零回归）；②厘清 V_boss 该评什么、和 fitness 什么关系（收回一个过度简化判断）；③把"gene→seam→查 V_boss"的配线**设计清楚**并**钉死一个关键前提**。**小验证（关键岔路·甲/乙）下个 session 才跑。**

#### 【一、★events 污染 bug：确认 + 外科修复（封板 simulator.py + quotient.py·玩家已授权）】
- **病根坐实**（trace 脚本 `analysis/curriculum_trace_delta0.py` 控制实验证）：`_copy_state` 把 `events` dict **按引用共享**（simulator.py 注释声明 events "read-only after load"），但 `show` 指令就地写 `ev["enable"]=True`、`keep-move` 就地写 `dest_ev["enable"]=(tile_id!=0)` → **一支搜索的事件改动污染兄弟支及 base**。线性重放（存档）无分支故抓不到；**任何分支搜索（beam/GA/quotient）普遍受影响**。
- **§S28 delta=0 平台 = 此 bug 的伪信号**（§S28 诚实保留①解了）：杀队长 afterBattle 的 `show([6,11])` 把 MT10 下楼梯**永久启用**并污染到弱分支 → 弱分支没打 boss 就"零损血绕路下楼梯"出段 → `final_hp=HP_in`（delta=0）。**delta=0 不是真机制（不是"零战斗路线"）、是污染 artifact。** 修后弱分支（HP<生死线）应**老实 found=False（死在半路）**，不再有假 delta=0 行。§S28 的 **2D 表（HP=735·生还格真打 boss）仍可靠**——只有 1D-HP 扫的低 HP"delta=0"行是污染。
- **修法＝最小外科 per-state 覆盖（玩家定的 `_enabled_events` 模式）**：FloorState 加 `_event_enable: dict[loc_key->bool]`（`_copy_state` 浅拷一份·便宜）；show/keep-move **写覆盖**不动共享 events；新 helper `_eff_enable(floor, loc_key, ev, default)` 在**所有读点**把覆盖叠加在静态 `ev["enable"]` 上。用 **dict 非 set**（keep-move 要写 False）。改动点：simulator.py 9 处（字段/拷贝/helper/3 读点 443/1045/1442/3 写点 1568/1578/1804）+ quotient.py 2 处（import + `_live_arrive_event:84` 读点改走 `_eff_enable`，因它读 runtime `floor.events`、复刻引擎门控，不改则 show 启用的到达事件漏判）。
- **★beam 47 守卫字节级全绿（零回归确认·玩家最关心）**：4 守卫（big_item_pull/door_value/region_potential/pull）点名 **47 passed**、pytest 非 slow **270 passed**。**修复没改 beam 产品结果**——这是修掉一个**潜伏** bug（beam 现用法没被它咬到、或没被守卫覆盖到的方式咬），属"去隐患零回归"非"修正 beam 输出"。
- **beam.py:205 `_is_region_boundary` 不受影响**：它读**静态 JSON** `floor_data["events"]` 判 boss 门 zone 边界、不读 runtime → 本修复反而**消除**了它一处潜在污染风险，无须改。

#### 【二、V_boss/fitness 关系厘清（收回一个过度简化判断·见 memory `feedback-vboss-fitness-relationship`）】
- **收回"HP155 是 fitness 判不准实锤"**：fitness 评的是"地上剩余血瓶 + HP + 钥匙的**等价 HP**"（不只瞬时 HP）。HP155 低但地上有血瓶（钥匙能拿）→ 等价 HP 不低 → fitness 给那个分**合理**、是 GA 当前 navigate 局限下的**合理最优**，非废解。
- **★V_boss 该评 SEAM（boss 入口到达态）、不是中间态瞬时**：到达态已整合早期所有决策 + 路上回血 → 在 seam 查**自动满足"前期烧血没关系"**（按净到达态判·阈值下 V_boss=0 不是负）。在中间态查瞬时属性会**前期误罚烧血**（漏掉中间→seam 的回补）。§S27"逐状态查 V_boss 当稠密奖励"的提法因此有缺陷——稠密是在 (a,d,h)-到达空间里、用在 seam 不是每个 timestep。
- **★配合非替换**：fitness 管中间态潜力（含可达血瓶/钥匙的线性等价 HP·处处稠密梯度）；V_boss 管 seam 终值（boss 段 nonlinear 拐点 + 连杀 sequencing + key-gating·线性等价 HP 漏掉的）。**score = fitness + λ·V_boss**（V_boss 作非负 terminal bonus 叠在 fitness 上）→ 既解死区无梯度、又不前期误罚烧血。**别简单"换掉 fitness"**（换掉抹掉 fitness 信号、前期误罚烧血）。

#### 【三、小验证配线设计（已设计·★未跑）+ 钉死的关键前提】
- **gene→seam→查 V_boss 配线**（不碰封板件、新写一只读 smoke 脚本，下个 session 落地）：
  1. `build_harness()` 出 start/zone/step/13 块 pool/block_markers/meta["block_cells"]/roster_fit/big/zone_fids/decode_cache。
  2. `_decode_with_order(gene, ...)`（**不传 final_goal**）跑主基因 → 得 `term_state`（gene 攒攻防自然终态）。`base = fitness(term_state)`（= 现有 GA 信号·原样）。
  3. **seam 腿**：`navigate_to(term_state, ("MT10",1,11), zone, step, max_pops≈8000, cache)` → `(seam_state, moves, reached)`。**★不能复用红钥末腿的 `reached_final`**（它靠 `_taken`=判该 cell 物品被吸、但 seam 是**楼梯格无物品**→ `_taken` 恒 False）；必须**直接用 navigate_to 返回的 `reached` 布尔**判到没到 seam。
  4. `reached` 真 → **V_boss(seam_state)** = `search_quotient(seam_state, GOAL=("MT11",6,10), seg_step{MT10,MT11}, max_states≈400k, cross_floor=True, beam_k=None, distinguish_doors=True).final_hp if found else 0`（复刻 `curriculum_scan_vboss.py` 口径）。`reached` 假 → V_boss=0。
  5. `score = base + λ·V_boss`（λ 候选 10·让 bonus 真咬·但 fork 判据与 λ 无关）。**纯 fitness 组 = λ=0**（同 seed 对照）。
  6. **每个 gene 记 (base, reached_seam, seam HP/ATK/DEF, V_boss)** → fork 判据 = **有没有任一 gene 的 V_boss>0**。
- **seam_state 带 gene 自己的钥匙/金/红钥**（非真存档的）→ 没收红钥的 gene 查 V_boss 时**开不了红门到不了 boss → V_boss=0 是真信号**（玩家说的"留够钥匙"要求·非配置 bug）。
- **★★钉死的关键前提（否则小验证 V_boss=0 是假信号·会误判成上乙）**：**seam 必须在 zone（navigate_to 到达范围）内**，否则 final_goal 永远失败 → V_boss 永远 0、但这跟"navigate 瓶颈"无关、是配置问题。**查证结果：`ZONE1 = [f"MT{i}" for i in range(1,11)]` = MT1..MT10、★包含 MT10。** `build_zone` 建了 MT9↔MT10 楼梯免费边（`down_stair["MT9"]=(1,11) ↔ up_stair["MT10"]=(1,11)`、link 实测存在），seam = **MT10(1,11)** 在 navigate 到达范围内。**→ 小验证若出 V_boss=0 是真信号**（navigate 送不出可存活 seam / gene 没攒够钥匙血量）、**不是配置假信号**。前提已排除。

#### 【四、本 session 交接 / 下一步】
- **入库**：`sim/simulator.py` + `solver/quotient.py`（events 污染 bug 外科修复·beam 47 守卫绿·零回归）+ 2 只读诊断脚本（`curriculum_trace_delta0`=bug 控制实验证据 / `curriculum_weakcell_saturation`=§S28 弱格饱和复核·暂搁）。**小验证脚本未写、未跑。**
- **★下个 session 第一步＝跑小验证（关键岔路）**：按【三】配线写 smoke（pop8/gen3·λ=10 + 对照 λ=0）→ 报 **①有没有 gene 到 seam（reached）②有没有 V_boss>0 ③最优解 seam 态属性/HP/V_boss ④对比纯 fitness 组**。判据：**有 V_boss>0 → 甲（GA+V_boss）可行、eval 是瓶颈一部分、继续甲**；**仍全 0（且已确认 seam 在 zone）→ navigate/搜索力瓶颈（GA"选块不带路线"架构锅）→ 上乙（state-centric 搜索）**。
- **§S28 诚实保留②（弱格 735 饱和）**：本 session 因重心转向**降级搁置**（脚本 `curriculum_weakcell_saturation` 在库）；甲/乙定了再回头。

### §S30 ★定生死探针坐实 navigate 天花板（架构瓶颈）→ 决定上"搜索管路径"新算法 + 调研算法选型（2026-06-16）

> 接 §S29（小验证未跑·钉死前提）。本 session：①跑小验证 + 标定排除"max_pops 假阴性" ②玩家手排强基因探针**定生死**、PART A/B **双向印证** navigate 天花板 ③**决定上新算法**（让搜索管路径决策、非 navigate 贪心外包）④**调研算法选型**（查资料·★本 session 不实现）。**甲/乙之争结束 → 上乙的完整形态。**

#### 【一、小验证 + 标定（排除假阴性、但暴露混淆）】
- **小验证**（pop8/gen3·λ=10 + 对照 λ=0·同 seed）：两组**全 0/18 到 seam、全 V_boss=0**、best=-906。表面倾向乙，但**被测全是低层弱态**（best 卡 MT3 HP203 ATK23）。
- **标定** `calibrate_seam_maxpops.py`（递增 max_pops 8000→20000→50000→100000 测最扎实 gene 的 term 态能否到 seam）：top1/top2 卡 **MT3**、烧满 100k(1213s) 仍到不了；全 13 块"上界"decode **退化**成 MT1(7,3) ATK11 base=-17632、2s 烧穿（=被弱态/无钥真封死·非预算）。**→ 排除"8000 假阴性"**（加预算到 100k 也到不了），但暴露**混淆**：测的是弱态、**分不开"搜索力 vs 架构"** → 须强基因探针。

#### 【二、★定生死探针（双向印证·本 session 核心）】`probe_handgene_seam.py`
- 玩家手排**强基因**当探针：剑→MT4→盾→一路宝石→红钥→navigate 到 seam。**映射坐实**（`cell_to_block` 折·不猜）：13 个坐标**正好完整覆盖整个 pool_13** + 红钥末腿（= 全 13 块 pool 的强基因·最大拾取上界）；坐标与块 id 差是**块跨多格、块 id 取块内 min 格当锚**（剑(11,11)→块(10,10)、盾(9,7)→(9,6)、红钥(10,2)→(9,1)）；1 个 MT9 宝石(6,5)→块(5,4) **不在 pool_13**（判断4 没选中）已标出排除。
- **PART A 忠实 GA 管线**（真 `_decode_with_order`·带 §S15 禁区·@8000·final_goal=红钥块）：进包 **5 块** `[剑→MT4→盾→MT9宝石→MT7宝石]`、reached_final(红钥)=**False**、终态 **MT7(3,1) HP75 ATK23 DEF21 钥{}**、base=-5406、seam **到不了**、V_boss=0。
- **PART B 架构天花板**（手动逐块·★**无 forbidden**·升档 8000→30000→100000 破预算假阴性）：HP 轨迹 **400→剑754→MT4 716→盾108→MT9宝石42→MT7宝石75**；★**拿到盾**(DEF 10→20·爬到 MT9)、但贪心 navigate 把 **HP(400→75)+钥匙烧光在爬路上**；此后**每一腿 0s 烧穿**（MT3/MT1/MT6/MT8/MT10宝石/红钥/seam 全到不了·从 MT7 HP75 无钥**可达空间极小=真封死非预算**）、V_boss=0。
- **★★双向印证**：PART A（带禁区@8000）与 PART B（无禁区·升满 100k）**拾取同样 5 块、卡在同一个 MT7(3,1) HP75、seam 全 0s 烧穿** → 带不带禁区、预算 8000 还是 100k 结论一致 = **强信号非偶然**。

#### 【三、结论 + ★决定（甲/乙定了）】
- **结论**：**不是搜索力**（强基因手喂了、盾也拿到了 DEF20）、**是【贪心 navigate 路径执行把资源葬送 = 架构/路径瓶颈】**。`verify_before_prune` 满足（搜索力 vs 架构已分开）。
- **navigate 天花板（数据坐实玩家判断）**：navigate_to **贪心、血够就永不绕路、不省钥匙、撞门就开** → 路径决策（绕路省血 / 省钥 / 开不开某门）**锁死在贪心寻路器内部、上层选块的 GA 控制不了**（从 MT7 骷髅一路硬撞过来的）。
- **缺口在"送达/路由"非 seam 不可达**：seam=MT10(1,11) 在 zone 内、§S28 V_boss 表已证**存在 V_boss>0 的 seam 到达态**（原理可达）；到不了纯因贪心执行把资源烧干、人晾在 MT7。
- **★决定：上新算法 = 乙的完整形态**——让**搜索把细粒度路径决策（绕路/省钥/开不开门）本身纳入**，而非外包给贪心 navigate。不只块层、**连路径决策一起搜**。

#### 【四、入库 + 下一步（算法选型调研）】
- **入库**：3 只读诊断脚本（`curriculum_smoke_vboss.py`=§S29 小验证配线 / `calibrate_seam_maxpops.py`=假阴性标定 / `probe_handgene_seam.py`=定生死探针），**均只读·不碰封板件**。
- **本问题 5 决定性特征**（驱动算法选型）：①**确定性+完全可观测+单人**（无随机/隐藏/对手）②**稀疏奖励+超长视野**（过 boss 才有终局奖励·中间几百步资源损耗）③**状态可精确模拟+可回退**（前向搜索/规划可行）④**已有精确 V_boss**（boss 段穷尽搜出的终局价值表 = 从目标反向一段真值已知）⑤**有人类示范**（14382 通关存档·可行非最优）。
- **★下一步＝算法选型调研（查资料·不实现）**：候选 = ① rollout-free MCTS（价值函数替 rollout）② AlphaZero/MuZero 单人改造 ③ 经典启发搜索 A*族/HTN 分层规划 ④ RL(DQN/PPO)+课程+示范 ⑤ 反向归纳/retrograde/反向 DP（V_boss 是种子）。每个评：本特征组合下适配 + 先例 + 工程量(是否要 NN/GPU) + 能否用 V_boss + 能否解路径决策 → 出**对比表 + 推荐方向**、玩家选。**本 session 只调研选方向、不实现任何算法**（玩家红线·项目最大算法选型决策·查清再选）。
