# 跨层模拟器开发交接文档

**写于**：2026-05-31（机制侦察阶段封板）  
**目的**：新会话快速上手，不重复上一阶段已确认的结论。

---

## 📌 2026-06-13 收尾·S1 pull_big 独立旋钮 α_big + S2 α_big 扫描（剑盾线；**不**取代下方红钥 Stage-2 主线）

> 本 session 两小步、产品码+守卫已 commit（只读扫描、未改任何逻辑、未碰 door_pull）。

- **S1**：`pull_big` 装独立衰减旋钮 `--alpha-big`，距离衰减 `/(1+dist) → /(1+dist)^α_big`，与 door_pull 的 α **解耦**（根因：一个 α 背"愿意去远"与"目标价值排序"两冲突角色，见 [[project-alpha-dual-role]]）。**默认 α_big=1 字节零回归**、**147 pytest 绿**（守卫：①α_big=1 零回归 函数级+@slow 搜索级 ②满额 G=β·ΔRP₀ 对任意 α_big∈(0,1] 仍是守着引导上界 ③拒 α_big≤0）。
- **S2 扫 α_big∈{1,0.7,0.5,0.3} 关键发现**（`analyze_priorities.py` 重放真引擎读拾取时序；玩家网站回放终审）：
  1. **旧"剑盾"诊断里"盾前扫光 6 个 MT1-5 宝石"前提已失效**——door_pull 落地后基线演化，现基线 α_big=1 盾在 **step422**、盾前只 **3 个**（MT3/MT3/MT4）。（下方 §A line40-41 的 step432/6宝石/576-25-25 是**旧快照·已被本条取代**。）
  2. **α_big=0.7 唯一无异常**：盾 step417、盾前 2 个、HP**583**（略优基线 571/26/24）。
  3. **α_big≤0.5 崩**：硬闯远盾 HP→67（step288）；α_big=0.3 截断（670 步只吃 5 件、终 21/22）。
  4. **四条同深 MT10 入口、无红钥**：MT10 卡死＝α 无关的红钥/门老问题、本次不涉及；**无崩爬**。
- **下个方向**：(a) ~~α_big∈[0.5,1.0] 细扫 × 预算 overnight~~ **✅已做→见下 §S3**；(b) 随机化/遗传搜索（目标层编码 + sim 当 decoder + 真 HP 当 fitness，待独立设计；**fitness 须带"潜力"项、不能只用终 HP，见 §S3.7**）；(c) future：核 scoring 是否处处用 ΔRP（阶梯感知）而非线性近似。
- **S2 构建产物**（ab0.7/0.5/0.3 floorbest jsonl + 4 条 .h5route）按惯例不入仓。

### §S3（本 session·2026-06-13）α_big 细扫 [0.5–1.0] × 双预算【即上方"下个方向(a)"】
> 只扫参、**未改任何产品码**（git status 除 route/ 产物外全 clean）。所有数 `analyze_priorities.py` **重放真引擎**核实（落盘 `route/analyze_recheck_*.txt`，非记忆）。范围 α_big∈{0.5,0.6,0.7,0.8,0.9,1.0} × 预算 {K200/cap120k, K500/cap480k}＝12 格。剑（MT5(11,11)+atk10）**全 12 格 step81**。

终态（最深层·HP 最高态；重放核实）：

| α_big | 120k 终 HP·ATK·DEF | 480k 终 HP·ATK·DEF |
|---|---|---|
| 0.5 | 269·26·25 | 636·25·24 |
| 0.6 | 150·21·22 | 612·26·26 |
| 0.7 | 583·26·25 | 689·26·25 |
| 0.8 | 595·26·24 | 517·26·27（MT10(11,11)） |
| 0.9 | 546·26·25 | 598·26·27 |
| **1.0** | 571·26·24 | **718·26·26** |

1. **零回归锚**（重放核实）：α_big=1/120k ＝ MT10(1,10) **571/26/24**、1169 步、剑 step81、盾 step422——与上方 S2 基线特征逐项一致。
2. **全 12 格止步 MT10**（重放核实）：0.8/480k 落 MT10(11,11)、余 MT10(1,10)；红钥/门老问题，4× 预算未推过（α 无关）。
3. **盾前掉血崩＝结构性**（重放核实·对照盾步）：α_big≤0.6 两预算全部盾前 HP→67（0.5/120k step288<盾309；0.6/120k step300<321；0.5/480k step300<321；0.6/480k step300<321）——4× 预算只把终态拉回、**治不了崩本身**，印证"撑不住≠预算不够"。
4. **α_big∈{0.7,0.9,1.0} 两预算盾前干净无崩**（重放核实；0.7 盾后取 MT8 掉到 53 属正常代价、非盾前崩）。
5. **0.8 非单调**（重放核实）：120k=595 干净；480k=517 且盾前 step312 HP→54 轻坑、终点换 MT10(11,11)——**加预算反而 HP↓**（beam 拓宽选到别的深点），别默认"更大预算=更好"。
6. **终 HP 最高＝α_big=1.0/480k＝718**（重放核实，全局最高）：**预算越大、基线 α_big=1 优势越明显**；与共享 α 扫描同向（软化净负）、别再重扫（见 [[project-alpha-big-sweep]]，一次 cap480k 6 格≈3.4h）。
7. **玩家判定：α_big=0.7 路线骨架最优**——终 HP 虽非最高，但**余血瓶/余钥匙/攻防底子潜力最高、最接近人手、已能不靠商人过一区**。→ **下一步 GA fitness 必须带"潜力"项、不能只用终 HP**（否则会选 718 那条把资源耗尽的）。
- 产物：12 条 floorbest/h5route 按 gitignore（`*.jsonl`/`*.h5route`/`*.txt`）不入仓；**扫描 runner `route/_sweep_runner.ps1` 入仓**（参数化 beam/cap/ctag/alphas，可复用）。

---

## 🎯🎯🎯🎯🎯 下个会话主攻【2026-06-12·钥匙价值【已落地+阶段1实测完】→ 下个 session 做 Stage-2（红钥过 boss·γ+β_big 组合）·取代下方所有旧🎯】

> **本 session 成果**：把上节"读码地基"的方案【落地+阶段1 rigorous 实测】完。`extract/door_value.py`（门锚定全臂梯度 door_pull）+ `tests/test_door_value_guard.py`（**13 条 κ=1 守卫·6 命门全过**）+ 接线 `probe_crossfloor_beam.py --gamma-door/--door-win` + 分析器 `extract/analyze_door_open.py`（**重放真引擎逐态核对门开/pocket 吸，不靠观察推断**）。**全 pytest 142 过、零回归**（γ=0 字节零回归守卫钉死）。**⚠ 全部未 commit（见末尾状态）。**

### door_value.py 实现（驱动层·塔无关）
- `door_pull(zone,state,door_reward,γ) = Σ_{门后有未吸价值·够得到} γ·R_未吸(门)/(1+dist_arc)`。
- `R(门)`＝门后**紧邻 pocket**（远侧无门分量 ∩ gated＝仅经此门可达格）内**小宝石 ΔRP₀ + 血瓶 HP (+win 若 boss 在 pocket)**；**排怪 toll**（已在 D，防二重计上）、**排大件**（pull_大件已引导）。ΔRP₀ 取自与满额兑现 G **同一份 ranked**（detect_big_items 参照态）。
- pocket 用**紧邻∩gated** 切分 → **零双计**（守卫钉死：同宝石/血/格绝不落两门）。穿行门只领到下一扇门那段、死袋落唯一入口门、可绕过门 pocket 空。
- **只进 `beam_score_extra` 排序键、绝不进 D/value_vector**；γ=0 → 空表 → door_pull≡0 字节零回归。κ=1 免疫：锚在【pocket 未吸】非【门闭】（无开门即掉分崖）+【持有钥匙本身零加分】（钥匙只经 penalty 归 0 帮够得到的有价值门）。

### 阶段1 实测结论（纯隔离 γ，β_big/β_small=0，K=50，rigorous=`analyze_door_open.py` 重放真引擎）
1. **机制对、方向对** ✅：门后兑现总值随 γ **单调↑**（γ=0→0.1→10 ＝ **14,720 → 19,766 → 26,651**）。**idea3 旗舰门 MT7(3,5)（R=5046=门后 +1atk 宝石+50血）：γ=0/0.1 都没开、γ=10 开了**——door_pull 治好了 idea3 §B 的病。最高 R 门 **MT1(9,5)(R7085) 基线漏、door_pull 拿到**。
2. **⚠ γ 须 ~10、不是 0.1**：door_pull 被 `(1+dist)` 重折扣（R~5000/dist~20–50 → γ=0.1 仅贡献 ~10–30，对比区基分 ~95,659 ＝可忽略）。γ=10 才咬得动，但此时**轻微畸变**（为大门丢小门 MT7(7,7)R200、过度刷 MT6/MT7 血袋无属性/爬升收益）→ **γ 尺度要校准**。
3. **⚠ 纠正 idea3 误判：所谓"6 扇门"实测 4 扇是空袋（几何坐实，非 bug）**：MT8(3,1)/(4,1)、MT9(9,4) `gated=1`（仅门格本身）＝**完全可绕过**，开它纯亏钥匙——算法本就不该引导；MT8(1,9) gates 4 格但 **0 宝石 0 血**（走廊·价值在更深门）＝routing-only。**真正有门后价值的只 MT7(3,5)、MT8(10,7) 两扇**。idea3 病机（门后价值不进 V）说对了，但**"该开的门"集合乐观了**。**且无病态囤钥匙**（可达好门都开了、余钥匙是下层钥匙过剩）——κ=1 没复发。
4. **⚠ 红钥过 boss 没测到**：纯 γ（β_big=0）下搜索**在 MT7 止步＝组合战力门**（爬到 MT8+ 要大件攻防＝β_big 的活）。上层门（MT8/MT9）和红钥 acquire→spend 长臂在纯-γ 隔离里**根本测不到**。**如实报告：短臂（可达门）已证 door_pull 有效（MT7 治好）、长臂红钥过 boss 绝非已解决。**

### Stage-2 方向（已和玩家定·下个 session 做）
**红钥过 boss 本质需 β_big 和 γ 同时开**（够到红钥门要先靠 β_big 爬到 MT8、再靠 γ 的 door_pull 引导花红钥开 boss 门——长臂两个必要环节、缺一到不了红钥）→ **"一次只动一个变量"在这条长臂不成立**。顺序：
- **(a) 先扫 γ∈{1,2,5,10,20} 找畸变拐点**、定一个干净安全的有效 γ（解决 γ=0.1 可忽略 / γ=10 畸变；可与 β_small 尺度对齐）。
- **(b) 再上 γ+β_big 组合**：让英雄先靠大件爬到 MT8+、再看 door_pull 引不引导花红钥开 boss 门＝**红钥过 boss 头号验证**。**诚实预期**：组合可能到 MT8+，但红钥超长弧**展开层仍受门控**（事实4：keys==0 时门是死边界、展开层逐步解钥的态够不到门后）、**可能还要联通块视野配合**（打分层已跨层看到、展开层生成态仍卡 fact4）→ 如真这样**如实报告"短臂治好、长臂还需联通块视野"**（[[crossfloor-vision-direction]]、idea3 方向一）。

### ⚡ 白天追加（凌晨写完上面🎯段后又干的两批·断电前完成、本次补记）
> 上面🎯段是凌晨 2:44 写的（只到 Stage-1）。之后白天又推进两步、断电前完成，但没来得及记进 handoff，现补。

**① 上午 9:01–9:23：Stage-2 红钥过 boss 组合扫描【已做·结论 6/6 未过 boss】**
- 跑法：K200 × β_big{4,10,25} × γ{1,2}（均 door_win=on 计 boss 巨值）；配套验证器 `extract/verify_redkey_boss.py`（重放真引擎判 BOSS_FLAG「10f战胜骷髅队长」+HP>0·铁律封板）+ 导出器 `extract/gen_bosspass_h5route.py`。产物 `extract/stage2_g{1,2}_bb{4,10,25}.txt` + `crossbeam_floorbest_K200_*_gd{1,2}w_*` jsonl。
- **结论：6/6 组合无一真过 boss**。最好态 **576/25/25 卡 MT9**——到了 boss 区但属性不够杀队长。
- **根因：强度↔深度在 120k 步预算内不可兼得**（够属性则走不到深处、到深处则属性不足）；door_pull+β_big 把英雄带到 MT9 已是组合上限。**短臂（可达门 MT7）Stage-1 已证 door_pull 有效；长臂红钥过 boss 仍未解决**，符合 Stage-1 fact4 预期（展开层受门控、可能需联通块视野配合）。

**② 下午 17:05：两问诊断【已做·`extract/analyze_priorities.py` 重放真引擎·只读不改产品码】**

**【A·剑盾优先级】拿剑后为何先回 MT1–5 扫光小宝石、最后才拿盾？= 小宝石群体兑现压过单个远盾在场引导 151.6×**
- 路线事实（bb25_gd1w 最深态 576/25/25）：step81 拿剑 MT5(11,11)→DEF10；step145–411 回 MT1–9 顺扫 6 个小宝石；step432 才拿盾 MT9(9,7)→DEF22。
- 打分对账（刚拿完剑、HP754/20/10、盾在 dist=304 远处那一刻）：盾【在场引导】β_big·ΔRP/(1+dist)=25·5808/305=**476**；盾【拿到才兑现】G=β_big·ΔRP₀=**506,600**（引导 1064×，但没走到拿不到）；中途 6 小宝石【拿到兑现】累计 G=**72,186**。→ 远盾在场引导 476 vs 小宝石群体兑现 72,186 = **群体压过 151.6×**。
- 根因：盾巨值锁在"拿到才兑现"G 里，奔盾 304 步长途上只有被 (1+dist) 压扁的 pull_big(476) 远处微弱吊着；近处小宝石顺路一个个拿、每个立即兑现 ~15k 持续进账 → beam 局部最优 = 先扫光近处小宝石。**确认玩家猜想：β_small·G 群体压过单个远盾引导。**
- 能否/怎么拉开：**能，但须结构性、守红线**（剑盾优先是搜索该自己算的、不写死规则）。两条同源方向：(i) 给"未拿大件"一个**不随 dist 衰减、随未兑现大件势能递增**的陡引导（别直接削 (1+dist)——会复发 hover 就近病）；(ii) 小宝石 G 在"还有大件未拿"时按未兑现大件势能**打折压低**。**注意**：晚拿盾致中段 HP 危险（step411 掉到 45、拿盾才缓），但终态仍 576/25/25 → 剑盾顺序**不是卡 MT9 根因**（根因=强度深度不可兼得），拉开主要改善 HP 曲线安全性。

**【B·MT8 左下 2 攻防（MT8(4,10)+atk1 / MT8(5,11)+def1）从不拿】= (b) 门后谷为主，不是 (a) 蓝钥误判**
- 事实：俩攻防在 **MT8(3,11) 蓝门**后（pocket 含这俩，R=7,035）。蓝钥 3 把/6 门稀缺，但**奖励表里唯一有价值的蓝门就是 MT8(3,11)**（其余 5 扇蓝门门后空）。door_pull 把它排 MT8 全门最高分：首到 MT8(HP45/23/12) door_pull=**72.53**（vs yellow 门 1.70/0.14）。
- **不是 (a) 蓝钥误判**：door_pull 不按色加权，但唯一该开的蓝门恰是它给最高分的 MT8(3,11)——无"因蓝钥稀缺错误省掉该开的门"；"省蓝"在其余 5 扇空蓝门上有真实依据，即便按色加权该开的也还是这一扇。
- **是 (b) 门后谷**：R=7035（俩攻防）**没进 V_zone/价值向量**，只在 door_pull 软引导里，γ=1 仅 72.53，被区基分(~95,659)+近处小宝石群体兑现(72k)淹没；首到 MT8 时 HP=45 自身难保更不会为 72.53 分神。不是怪墙挡（dist=96 可达、door_pull 算得出），是**引导量级太小+门后价值未入 V** 的价值谷 → 同 Stage-1"γ=0.1 可忽略、要 γ~10 才咬得动"。

### 当前状态 / 待办（⚠ 全部待 commit）
- **未提交**（本次一起 commit）：`extract/door_value.py`、`tests/test_door_value_guard.py`、`extract/analyze_door_open.py`、`extract/verify_redkey_boss.py`、`extract/gen_bosspass_h5route.py`、`extract/analyze_priorities.py`（以上 untracked）+ `extract/probe_crossfloor_beam.py`（已改 +30/-3）+ `docs/handoff.md`（已改）。
- **已提交**：满额兑现 G（`extract/big_item_pull.py` build_pickup_bonus/pickup_bonus）在 **07bd423**。
- **待办**：① ~~Stage-2 红钥过 boss~~ **已做（6/6 未过·根因强度深度不可兼得，见上）→ 升级为「长臂红钥过 boss 未解」**；② **联通块视野**（红钥长臂展开层最后拼图、优先级↑）；③ **剑盾/MT8 破谷**（A 的 (i)/(ii) 结构性拉开 + B 的门后价值入 V/γ 标定，都须守"不写死规则"红线）；④ 红钥到 MT10 缺口、MT45 regression(token4141) 仍挂。
- 实测产物：Stage-1 `stage1_*.txt`、Stage-2 `stage2_g{1,2}_bb{4,10,25}.txt` + `crossbeam_floorbest_K200_*` jsonl、`crossbeam_floorbest_K50*`（供 analyze_door_open/analyze_priorities 复跑）。

---

## 🎯🎯🎯🎯 【已落地·见上节】钥匙价值【地基已读码坐实】→ 方案已出并实现（2026-06-12 晚读码）

> **方案已基于本节落地（door_value.py），阶段1 已实测，见上一节。本节保留作"两几何分裂"事实地基的存档。**

> **本 session 末尾成果（纯只读读码、零产品码改动、未出完整方案）**：玩家定了【先做钥匙价值、破谷往后放】，并要求先把"钥匙价值方案的成败押注点"用读码坐实再落盘。押注点 = **门后价值在【打分层】是已乐观计入、还是被门挡住**。读了四处关键码，结论 = **两几何分裂**（打分层门乐观、展开+Pareto 层门门控）。下个 session 直接基于本节事实出方案，**别再重读这四处**。

### 四处读码已坐实的事实（逐条，带 file:line）
1. **vzone 门可穿（打分层门乐观）**：`_passable`（`extract/vzone.py:101`）`return k is not None and k[0] != "wall"` —— **只挡 wall**；`_enter_cost`（`vzone.py:110`）非怪节点返回 `0`。→ 门（非 wall、非怪）**零代价可穿**。确认之前"门=`('pay','door')`、`_passable` 只挡 wall、门零代价"**仍成立**。**打分层的距离/区势能/pull 已经"看得到门后"**（路由直接穿闭门、不门控）。
2. **区势能 D 已乐观计入门后【怪 toll】**：`_future_potential`（`solver/beam.py:296`）= `λ·Σ 区内非当前层存活怪 toll`，用静态 zone 图（门乐观可穿＝事实1）。→ 门后的怪与本层怪**一视同仁计入**（无论该门开没开）。**⚠ 但 D 计的是【怪 toll】**：门后的**宝石/钥匙/血瓶不是怪、不进 D**。
3. **value_vector=持有资源、门后道具未吸不进 Pareto**：`value_vector = _value_map`（`solver/search.py:60-71`）= `hp/atk/def/mdef/gold/kill + key:* + item:*`，**全是英雄【持有】量**。→ 钥匙在保护维（`key:*`）；门后道具**在地上时不进 value_vector**，只有**吸收后**才以 `item:*`/`atk`/`def`/`hp` 进。配合 `_protect_dims`（`beam.py:344`）`key:*` 按**本层门数封顶** → 钥匙稀缺（keys ≤ 本层门数）时花钥匙才真降保护维。**⇒ "开门=纯损一把钥匙"机制坐实**：开门 `key:X −1`、门后道具未吸＝不在 V → 开门态被"不开门兄弟"**严格 Pareto 支配**（少一把钥匙、其余全等）直到吸到门后（idea3 的 6 例纯 key 轴被支配＝此）。
4. **展开层被钥匙硬卡（门控/悲观）**：`_free_cells`（`solver/quotient.py:114`）floodfill 走 `_is_free_tile`，**闭门不是自由格** → 挡 flood、门后不在自由块；`_boundary_ops`（`quotient.py:246`）闭门**只有 `h.keys.get(DOOR_KEY_MAP[t],0) > 0` 才发 `("door",…)` 算子**，**keys==0 → 不发算子 → 门是死边界、门后展开层够不到**。开门算子 `_expand_op` 走到门旁发 mv → 引擎开门花钥匙。

### 综合＝两几何分裂（钥匙价值方案的事实地基）
- **打分层**（vzone / 区势能 D / pull）：**门乐观**。距离、区势能已穿闭门看到门后【怪 toll】；pull 距离门无关。
- **展开 + Pareto 层**（`_free_cells`/`_boundary_ops`/`value_vector`）：**门门控/悲观**。门＝付费算子需 `key>0`（无＝死边界，事实4）；开门＝`value_vector key −1`，门后道具未吸不进 V → 开门态 Pareto 被支配（事实3）。
- **病机（精确）**：因打分层乐观，**开门【不抬】排序键**（门后怪 toll 已在 D 里；门后宝石/钥匙根本不是怪、不在 D；pull 门无关；HP 不变），**却【花掉】一把钥匙**（value_vector −1）→ 被不开门兄弟 Pareto 支配 → 在吸到门后价值【之前】就被剪。门后【可兑现价值】（宝石→G、钥匙、血）对排序键**不可见**，直到吸收（G 拿到才兑现）；钥匙代价的谷先把开门态剪了。

### ⚠ 给下个 session 出方案的两条硬约束（本次读码暴露、必须遵守）
- **信用别盲目加"门后价值"＝会二重计上**：区势能 D 已乐观计入门后【怪 toll】，再把它当"门后价值"加一遍＝重复。**信用只补 D/pull 没看到的缺口** ＝ 门后**宝石/钥匙/血**的可兑现价值（只有 G-拿到才兑现 / 吸收 才捕获的那部分）。
- **信用绑【真开门动作】、拿到才兑现非 κ=1**：钥匙的"期权价值"极易变 κ=1（守着没开的门算门后潜力＝κ=1，**最易复发处**）。信用必须只在**不可逆开门（花了钥匙）+ 真穿门吸到门后**那刻兑现；放 **beam 排序键、不进 value_vector/D**；γ=0 零回归。

### 钥匙价值方向（已和玩家定，落盘）
**优先做、收益最大**：管整局钥匙经济 + **红钥到 MT10 拿不到、过不了 boss 这个终极拦路** + 是 **MT1 破谷的前置**（破谷净 −1 黄钥）；病根已坐实（idea3 的 6 例纯 key 轴 Pareto 淘汰、门后价值不进 V）；是**项目最早就该做、一直被绕开的线**（D_K 当年判负的根因正是"门后价值不进 V"，idea3 现钉死）。解法＝给"花钥匙开门"一个**门后价值信用进 beam 排序键**（不进 value_vector）、**拿到才兑现非 κ=1**、**重点验红钥过 boss**（红钥的门后价值＝解锁 boss 房＝过 boss 巨值）。**破谷三+一往后放**。完整方案下个 session 出（本 session 只落盘地基、未出方案、未改产品码）。

---

## 🎯🎯🎯 下个会话主攻【2026-06-12·三想法分析完成→"延迟满足"钉死为唯一真病·四思路待分析·取代下方所有旧🎯】

> **本 session 成果（纯只读分析、零产品码改动）**：按玩家"三个想法都做、想法3为重"的要求做完，把"算法哪里不行"从主观判断变成**逐决策级硬数据**，并把所有病归约到**一个共性根因＝延迟满足**。三份报告：`extract/idea3_player_param_reverse.md`（想法3·反推参数）、`extract/idea2_mt1_region.md`（想法2·谷-崖实证=靶心）、`extract/idea1_g_route_compare.md`（想法1·G档路线对比）。**下个 session 从"四条破延迟满足思路"切入（先分析别实现），方向一·联通块视野很可能是前提。**

### 想法3 里程碑：用玩家通关路线当【标尺】反推参数空间（`idea3_player_param_reverse.md`）
- 第一次用玩家真实打法把"算法哪里不行"量到**逐决策级**。一区 **168 决策**：
  - **主干健康**：135 可表达决策里 **66%（89个）调参可复现**、其中 71 个**光区势能基线（不用大件引导）就和玩家一致** → **不用推翻重做**。
  - **剑盾误判彻底解决（逐决策级证据）**：never-argmax 的 45 个里**玩家独占 atk 轴=0、def 轴=0** → 所有抢剑抢盾决策**全部可复现** → 满额兑现 G 稳了、**结构盲区与大件无关**。
  - **真正要换架构就两件、且都是"延迟满足"形态**：①**联通块视野**（30 飞行楼传 + 20 远处目标 + 门后视野：模型困在"当前层+逐级楼梯算子链"、看不到跨层一步到位和门后那片）②**钥匙价值**（开门在 value_vector 上纯损一把钥匙、6 例纯 key 轴被 Pareto 淘汰已证；门后回报不进 V）。

### 延迟满足的本质＝下个 session 靶心（想法2 谷-崖实证钉死，`idea2_mt1_region.md`）
- MT1 道具区（2 黄钥 + 2 蝙蝠 1 法师 → 1 攻 1 防 1 黄钥 1 红血）**玩家拿、算法不拿**。引擎真走进去算：排序键从入口 **0→−14→−38→−52** 穿三怪只掉血、**踏上宝石才跳崖 +21278**。**【净值是正的】**。
- **根因不是奖励太小、是顺序 + beam 近视**：算法看得到崖顶宝藏（净值正）、但**走不过中间那道谷**（beam 贪心、绕路状态分跌时就剪了、走不到崖顶）。
- **和之前所有病同根**：climber 被 grinder 挤死 / 远征链腰斩 / 剑盾误判——全是**"先付出（降分）才有回报，贪心 beam 在付出阶段就剪了"**。beam 结构性奖励"每步不亏"、惩罚"先亏后赚"，而玩家会忍着穿三怪拿宝石因知道终点有回报＝延迟满足。

### 下个 session 要分析的四条破"延迟满足"思路（**先分析别实现**，本 session 刻意未展开）
1. **思路一·目标导向 approach 梯度**：给高净值目标（门后/跨块的）一个 approach 梯度托住谷底（像 pull 但扩展到所有高净值目标）。难点=梯度要穿门/跨块指向门后目标（现 pull 距离困在吸收视野看不到门后）→ **和联通块视野是不是同一件事**。
2. **思路二·前瞻 lookahead**：让排序键含"这条路通向的最高净值"、绕路状态因通向 +21278 不被剪。难点=**极易滑回 κ=1**（"通向的潜力"正是 κ=1 的病）、要特别小心。
3. **思路三·分坑保护谷里探索者**：像 stairs 分坑、加维保护"正在穿谷朝高净值目标走"的状态不被高 HP 兄弟挤掉。**不改价值、最不犯 κ=1**。难点=怎么界定"正在朝高净值目标走"（又回 approach/视野问题）。
4. **思路四·联通块视野是前提**：三条思路都卡在"要先看到崖顶在哪（门后/跨块目标）"，而联通块视野正是解决"看到门后/跨块"的 → **可能先做联通块视野、approach/前瞻/分坑才有指向对象**。

### 当前状态 / 待办
- **满额兑现 G 代码**（`extract/big_item_pull.py` 的 `build_pickup_bonus`/`pickup_bonus` + `probe_crossfloor_beam.py --beta-small` 接线 + `tests/test_big_item_pull_guard.py` 守护）本 session 落地、**甜区（β_big=25, β_small=3）到 MT10 HP=649** 同时治剑盾误判 + 就近病 4→1，**已提交（07bd423）**。h5route 编码器已通（路线都 h5route 交付）。
- **待办**：① **联通块视野 + 钥匙价值**（两个换架构方向、都治延迟满足、下个 session 主攻）；② 红钥匙缺口（到 MT10 无红钥匙、过不了 boss）；③ MT45 regression（token4141）。

---

## 🎯🎯🎯 下个会话主攻【2026-06-11 晚·四决策点解剖根因坐实 → 两方向待落地·取代下方所有旧🎯】

> **本 session 成果**：把"结合两套打分(region 区势能基分 + 大件 pull 引导)"在四个迷惑决策点上【解剖】到机制根因。报告 `extract/four_point_dissection.md`、探针 `extract/probe_dissect_score.py`（只读、调真打分函数 `region_reference`/`_future_potential`/`pull_big`，β×λ 只重新取值、不调产品）。**下个会话别再走探针/选路径(已落地)，直接按下面两方向改 pull 形态。**

### 根因（精确不等式，已坐实）
- 打分键 `V = HP − Σcost − λ·区势能 + β·pull`；产品 **β/λ = 25/0.2 = 125**。
- pull 给大件【周围一整圈格】灌高分(不只"拿它"那一步)；拿到大件→离场→pull 清零。
- ⇒ "拿 > 守"需 **(1+dist) > β/λ = 125**：盾在隔壁(dist≈0)→守着反超 ~125 倍 → 搜索绕着盾转就是不拿(开门不进 / 盾旁打史莱姆 / 为接近付 160 血)。
- **"治剑盾误判"与"就近病"是一体两面**(同一个 pull)：pull 拽到盾旁=治误判；同 pull 在盾旁造 hover 平台=就近病。
- 盾最终仍拿到：靠 `value_vector`(含 atk/def，`solver/search.py:63`) Pareto 保留；但 V 地形被 pull 污染成"绕着剑盾转"。这解释了上轮"任何 β 都治剑盾误判却都抬病合计"。

### 四点归两类（详见报告表）
- 点1 MT7 骷髅士兵 = **pull 驱动闯关**(−160 血；β=0 不杀、β≥4 才杀，局部 V 第一)。
- 点2/3 MT9 守盾不拿 = **hover 平台**(守盾旁 V≈84,681 ≫ 拿盾 autoopen V=−7,190)。
- 点4 MT5 不深入拿钥匙 = 大件拿光后 **pull=0、纯 region**(钥匙不进 V、势能只罚不奖 → 无吸引力)。

### 玩家定的两个解法方向（本 session 不评估/不改码，留下个 session）
**方向一·联通块视野(治点1)**：点1 选 MT7 骷髅士兵因"它是【当前层】离盾最近的格"，但算法困在当前层算距离、看不到 MT8 黄门其实离盾更近。根因=联通块视野没真正跨层(上下楼梯被当成要打分的动作、而非联通块内随便走)。要：**别给上下楼梯打分；楼梯连通的层并成一个大联通块、块内随便走、视野里有整块格子(MT6 时看得到 MT8 黄门)**；先按联通块展开、再对每个目标找可行路径(联通块性质保证两点必有路)。落地=**pull 距离 d 从"当前层+楼梯算子链"改成"整个楼梯连通块内的真实跨层最短路"**。与 fly/活体距离一脉相承但更彻底。

**方向二·拿取奖励机制(治点2/3/4)**：根因=价值体系只有"势能罚分"+邻域接近 pull(/(1+dist)) 只给"靠近奖励"，"拿到那刻"反因 pull 清零而扣分 → 守>拿。**关键设计(玩家明确)**：**邻域接近 pull 必须保留**——它提供"朝大件走"的方向梯度，去掉就丧失方向、退化成近视(只有踩到才知道、看不到远处大件往哪走)。正确做法：**保留接近 pull 引路 + 在拿到那一刻额外给奖励、且这奖励必须 > 守着的接近 pull**(驱使"直接去拿"而非"守旁边享受接近奖励")。接近 pull 引路、拿取奖励临门一脚(够大、压过守>拿落差)。**通用到小宝石/钥匙**(点4 钥匙也要拿取奖励、否则不积极拿)。**天然不犯 κ=1**：奖励只在"真拿到"那刻给(κ=1 是奖励还没拿的潜力 → 悬而不拿；这是奖励已拿到 → 诱发去拿)，见 [[feedback-realized-value]]。

**分工**：方向一治"看得到"(远处目标距离被正确看见)；方向二治"拿得动"(看见了就真去拿、不守着)。

### 当前状态 / 待办
- 结合两套打分代码**已实现+提交 commit `3cb9f7f`**(region 基分 + 大件 pull、`detect_big_items` 按 ΔRP 数据涌现不硬编码、四层管线、23 守护测试钉死 value_vector 零回归 + 拿到即兑现 pull 归零)；pull 形态有上述根因病，下个 session 按两方向改。
- h5route 编码器已通(以后路线都 h5route 交付)。
- **待办(老问题)**：① 红钥匙缺口(到 MT10 无红钥匙、过不了 boss)；② MT45 regression(token4141)。

---

## 🎯🎯 下个会话主攻【2026-06-11 认知突破·取代下方旧🎯】：玩家要的"宝石对剩余所有怪累计减伤"=代码早实现好的【区势能 `_future_potential`】，激活它(路径B)即总解药

> ## ✅ 已实现并提交（2026-06-11 更新 · 下方"只读探针、未改产品码"的原计划已落地）
>
> **状态变更（务必先读）**：下方整段是【实现前】写的方案记录。该方案【已执行完毕并提交 `3cb9f7f`】（9 文件 +1106/−22，`pytest` 123 全绿 = 100 基线 + 23 守护）。落地形态 = **结合两套打分**：
> - **兑现侧（路径B 落地）**：region 区势能基分 `_future_potential`（`solver/beam.py`，lam=0.2）直接进 beam 排序基分，叠在 stairs 分坑上。小宝石/血走这条兑现侧。
> - **引导侧（路径A 收窄到只对大件，避开红线B）**：`β_big·pull_big`（`extract/big_item_pull.py`）。大件由 `detect_big_items` 按 ΔRP（整区减伤）降序找【最大乘性缝】**数据涌现**——本塔自然落到 {铁剑@MT5, 铁盾@MT9}（与小宝石差 ~4×、缝≥min_gap 2.0），**不硬编码剑盾/物品 id**。pull 只进排序键、**绝不进 value_vector/D**；拿到即离场→pull 归 0、兑现跃升远大于守着引导→**结构性不复发 κ=1**。
> - **四层管线**（全加性、塔无关、`extra=None`/β=0/λ=0 字节零回归）：`solver/beam.py`(score_points/equiv_hp_over_roster 加 `extra`) + `solver/quotient.py`(search_quotient 加 `beam_score_extra` 透传) + `extract/big_item_pull.py`(驱动闭包) + `extract/probe_crossfloor_beam.py`(`--score region --beta-big` 接线、`--kappa` 已删) + `extract/probe_big_item_gap.py`(ΔRP 口径参照)。
> - **23 守护测试**（命门）：`tests/test_pull_guard.py`(7) + `test_big_item_pull_guard.py`(10) + `test_region_potential_guard.py`(6)，钉死 β=0/extra=None 字节零回归 + pull 不进 value_vector + κ=1 结构性免疫。
> - **vzone.py 已清理**：删 κ/D_rel/D_K/Φ_key 三套废弃探索残留（只留结合所需 + 塔无关几何原语 `_zone_key_geometry`）；废弃残留留存 `git stash@{0}` 可恢复，**未污染提交**。
>
> **K=200 β_big∈{4,10,25,60} 扫描已落定（2026-06-11）**——审计驱动 `extract/beta_big_route_disease_audit.py`，报告 `extract/beta_big_route_disease_audit.md`，同一套病判据 apples-to-apples 对基线 bb=0（纯 region λ0.2，无 pull）。三条诚实结论：
> 1. **剑盾误判全治**：bb>0 每档剑前 MT3 累计损血从基线 **560 压到 74**（pull 把"先去 MT5 拿铁剑+10atk"拉成主路），玩家点名的"MT3 裸打近 500 血"病征消除。
> 2. **但加 pull 一律抬高病合计**：基线病合计 **1 → bb>0 全部 6~7**（主要是就近病 0→3~6）。**没有任一 β 在病合计上做到不差于纯 region 基线。**
> 3. **代价非单调**（HP：基线301 → bb4=183 → bb10=165 → bb25=282 → bb60=263）：低档 pull 反把 HP 打崩、就近病更多，高档回血——非"β 越小越好"。可能含 K=200 beam 留存随机性。
>
> **最不亏的 pull 档 = β_big=25**（治剑盾误判、早拿剑@步82、终末 282HP/24/23、就近病4；病合计6 仍>基线1），已导出 `beta_big25_lam0.2_mt10_route.h5route`（前缀83+region 1016步纯RULD，封板对账 MT10(1,10) HP282 ✅；到 MT10 仍无红钥匙=老问题，回放走到入口即止）。**取舍归玩家**：是否用"升高的就近病"换"治好的剑盾误判"，求解器不预设。
>
> ---
> *（以下为原始计划记录，已落地，保留供溯源。）*

> **下个会话从这切入。** 上一轮把"就近打/早拿血/透支潜力"诊断成"跨层视野不够"(下方旧🎯)。本轮更深一层、点到结构性解药：玩家要的核心估值（宝石价值=对本区**所有剩余未打怪**累计减伤，不只 boss 单怪）**代码里早完整实现了**，只是当前调的 β 路线没用它。~~**先只读探针量化、再定接法，未改任何产品码。**~~（**已过时**：已实现+提交 `3cb9f7f`，见上方 ✅ banner）

### 【核心突破】解药=已实现的区势能 `_future_potential`，不用新发明
- 玩家要的"宝石对本区所有剩余存活怪累计减伤"（治就近打 / 透支潜力）= **region 打分键的区势能 `solver/beam.py:287 _future_potential`**（读活体 `_alive_mids_on` / 有时序性 / 塔无关 by 门禁结构 / 拿到才兑现）。它早把玩家说的"方向二"实现完了。
- **但一直调的 β 路线用的是另一套键**：`vzone.py:608 beam_rank_score = HP−D_free + β·pull`，其中 `pull`（`vzone.py:575`，value 在 :601）只算 **boss 单怪**减伤，且 **lam=0.0 把区势能关了**（cut 文件名 `crossbeam_cut_K50_vzone_b{β}_lam0.0_stairs.jsonl` 为铁证）。
- 所以玩家说的"结合两套打分" = **把已实现好的区势能重新激活用回来**，不是发明新东西。

### 【为什么是总解药】区势能天然免疫所有病
- **读活体** → 免疫静态高估（= 方向A 要修的「机制①」、`probe_pull_flyaware` 实测的距离 ×3.2 高估）。
- **拿到才兑现** → 不是 κ=1 预付惰性。
- **有时序性**（杀一只少一只）→ 早拿盾自然值钱。
- **视野到 boss 且塔无关**。
- → 走 **路径B（区势能直接进基分、叠在已就位的 stairs 分坑上）** 可能**同时**解决：就近打 / 早拿血 / 静态高估 / 透支潜力 **四个问题**，**连方向A 都不用单独修**。

### 【历史包袱已澄清】这条组合从没试过、不是重蹈覆辙
- 之前"区势能 λ → 中层饿死 → stairs 分坑才是杠杆"（`probe_crossfloor_beam.py:401`、本 handoff 下方"时序V"段）否定的是「**区势能治前沿塌缩**」，**不是**「区势能治坑内就近打」。
- **「stairs 分坑(已就位) + 区势能坑内价值」这个组合从没跑过** → 不是重蹈覆辙，值得验。

### 【路径A vs 路径B】（探针定，别提前拍）
- **路径A（引导侧）**：把区势能数值当 pull 的 value 进**排序键**（引导）。风险=红线B：守着宝石的引导分 > 拿到的兑现分 → κ=1 惰性复发。
- **路径B（基分侧·玩家初步倾向）**：区势能直接进**坑内基分 V**、叠在 stairs 分坑上 → 免疫机制① + 不预付。风险=历史"中层饿死"（但见上，该组合没试过）。

### 【下个会话第一步·只读探针】（~~玩家已认可，别碰产品码~~ → 已完成、产品码已改并提交 `3cb9f7f`）
在几个真实决策态（**MT5 刚拿剑 / 就近打点 step506 / step366 / 首入 MT9**），量一颗 **+1atk 宝石**三个数：
- **(a) boss 单怪减伤** = `boss_toll(atk)−boss_toll(atk+1)`（现 `pull` 的 value 口径，`vzone.py:601`）。
- **(b) 对本区剩余存活怪累计减伤**（区势能口径，**复用 `beam._future_potential`**：构 `FutureCfg(roster=build_future_roster(state), lam=1)`，取 `_future_potential(state@atk) − _future_potential(state@atk+1)`）。
- **(c) 拿到后 base=HP−D_free 跃升** = `v_zone_score(κ=0)[0]`（`vzone.py:512`）在 atk→atk+1 的增量。

**探针一次回答四件**：①区势能 value(b) 比 boss 单怪(a) 大几倍？②兑现侧 base 跃升(c) 能否撑住**路径A 的红线B**（守着引导分 > 拿到兑现分 → κ=1 复发？核心张力，必须量化）？③据此选路径A 还是 B。④**专验路径B 关键**：**stairs 分坑已就位下，开区势能(lam>0)还会不会中层饿死**（跑 `probe_crossfloor_beam.py --score region --lam>0 --diversity stairs`，看 reach 深度）。

### 【接探针的技术锚点】（本轮已核实的复用点，省得下轮重推）
- `_future_potential(state, future)`（`solver/beam.py:287`），`future=FutureCfg(roster, lam)`（:179），`roster=build_future_roster(state)`（:201，读 `_floors_dir`/`floor_ids`/`floor._tile_to_enemy` 构静态怪位 + 门禁结构判区界）。off 当 `future=None/lam=0`。
- ⚠ **待确认（探针第0步必查、勿静默假设）**：重放出的 sim 态（`build_start()+sim.step`）是否带 `_floors_dir/floor_ids/floor._tile_to_enemy`——若不带，探针自己按 `_floors_dir` 构 roster。
- 决策态来源：重放 `k200_mt10_route.json`（`probe_pull_flyaware.replay_snapshots` 模板）或 β cut 路线（`export_bscan_routes.pick_best_mt10`）到目标步；MT5 剑后约 step239(HP710)、就近打点 step506@MT7 / step366。
- 现成只读模板：`extract/probe_pull_flyaware.py`（重放取态 + pull 分项拆解）。

### 【红线】（实现期守死）
- pull / 引导项 只进**排序键**、**绝不进 D / value_vector**；价值**拿到才兑现**（别 κ=1 预付）。
- sim 封板预检 / 塔无关（不写死层号·怪·阈值）/ 引擎只当裁判 / **玩家真实游戏终审** / 不硬编码策略偏好。
- **fly 边 和 方向A（读活体距离）是备选**——区势能路径B 可能让它们都不必要，**先验区势能路径B**。

---

## 🎯 下个会话主攻：跨层连通块「全局价值视野」（β 路线玩家实测诊断，2026-06-11）

> **下个会话从这里切入，别再从细节打转。** 六条 β best-MT10 路线已导出给玩家网站实测（`beta{0,0.5,1,2,4,8}_MT10entry_hp*.h5route`），玩家看完路线行为后给出核心诊断如下。

### β 路线实测结论（玩家）
1. **β=0.5 是真进步**——算法开始懂重要目标、尤其剑盾。但玩家诚实修正：MT4 那步**不是"理解了卡血"，是单纯发现红宝石离得近**（进步是真的，但没那么聪明，别高估）。
2. **剑盾之外优先级就乱**：β 高时会打没意义的怪（不挡路、非零伤、只给金币）、开了门不进去；**β 越高越频繁**。
3. **pull 治住了「早拿剑」但没治住「早拿血」**：MT5 拿完剑后仍直接去 MT1 拿血药——700+ 血、钥匙也不紧张时去拿血**完全没道理**。是玩家早提的老问题（血够时不该提前换血、纯换血除攒 boss 血外都是亏）在 β=0.5 下的残留。
4. **【根因·核心】跨层视野不够**：MT9 打完，最优一定是回 MT1–5（大量属性在那），但算法优先把临近的 MT8/MT7 先打了——这才是"突然一堆无意义操作"的根源。**算法看世界是"以当前楼层为中心、就近打"，而不是"整个能到达的区域是一个大连通块、按价值全局规划"**，所以被临近低价值目标勾走。

### 下个会话主攻方向（玩家定）
1. **跨层视野要更广阔**——【能楼梯到达的就是一个大连通块、隔层之间也能到达，算法就是不断扩大连通块】。让它在 MT9 时就有底层 MT1–5 的视野、能"回头拿更值的属性"，而不是就近打 MT8/MT7。**与早先缩点思想（楼梯=免费边、坐标不重要、扩大连通块）一脉相承，但"跨层广度"还没真正做到、算法仍有楼层中心倾向。**
2. **性能预期**：后期连通块边数会很大但**不应超过 1000**，玩家判断计算机能在合理时间跑出来（随规模增大再评估性能）。
3. **几条算法需明白的规则（待之后让其从价值涌现、严禁硬编码——CLAUDE.md 铁律 + [[feedback-realized-value]]）**：
   - 不打算花钱时，**拿道具/血量换金币是绝对亏的**（光前期这操作就没意义，更别说后面的幸运金币）。
   - **血量充足（如 700+）且钥匙不紧张时，不该提前去拿血药**（纯换血除攒 boss 血外都是亏的）。

### 当前状态快照（2026-06-11 clear 前）
- **pull 三层架构**（剪枝界 / 排序基分 / pull 只进排序）已落地，治住开局视野病（β=0.5 开局拿剑）。
- **MT2 小偷 sim 已修**（三段修法、网站实测坐实、检查点全绿）→ 已提交 **commit `9248e26`**。
- **h5route 编码器打通**（能导出路线给玩家网站看回放）；六条 β 路线已实测：0.5 真进步、高 β 乱、早拿血残留。
- **待查 regression**：全程 token4141 老问题（MT45 dead，与小偷修复无关，见下方🟢段）。
- **方向衔接**：本诊断是 [[project-goal-oriented-direction]]（目标导向规划）+ [[project-per-region-valuation]]（按区估值）向"**跨层广度**"的推进；红线仍是 [[feedback-realized-value]]（价值拿到才兑现、规划只引导）。

---

## 🟢 MT2 越狱小偷 (3,7)→(1,9) desync 收尾修复：hide 抑制修法 + 玩家网站实测裁定 1b + ⚠OPENING_PREFIX 82→83 + 撞出预存全程 regression（2026-06-11）

> 承接下面"sim 小偷 move no-op 实质差异"。上会话已落 move/keep-move 两段修法（无 loc move 以触发格为起点 + keep-move 落点激活事件，口径1b，**工作区未提交**）。本会话补最后一环、经两个防误伤前置探针圈死范围、玩家网站实测裁定后实现并验收。

### 修法（已落 `sim/simulator.py`，107 单测 + 17 检查点全绿、真值未动）
- **无 loc hide 无条件登记抑制**：`_execute_instruction` 无 loc hide 分支原"仅 `remove` 时登记 `_suppressed_events`"→ 改为**无条件登记**，匹配网站"hide 移除触发块、再踏不触发"。
- **根因**：MT2 (3,7) 越狱小偷事件末尾 `hide` 无 remove 无 loc → 旧逻辑不登记抑制 → u#75 二次踩 (3,7) re-fire 整段 → `move keep [left:2,down:2]` 把已搬走的 (3,7) 空实体又搬到 (1,9)、错误清掉小偷。修法后再踏被抑制、不 re-fire。
- **范围圈死**（两前置探针 `probe_hide_suppress_blastradius.py`）：全塔无 loc hide 中本修法新增抑制仅 7 处，全是一次性 hide（越狱小偷链/specialTrader 购买/MT25·49·50 boss 入场），无一依赖 hide 后重踏；唯一落入范围又被跨层 show 复活的 MT2(10,11) 经核查是 MT29(6,2) `show` 后自身 `hide remove:true` **永久自毁**的一次性链 → 不需配套 show 解抑制。

### 玩家网站实测裁定（决定性）
- **1b 坐实**：网站 u#70 撞 (3,7) 越狱对话、小偷搬到 (1,9)；英雄下行 u#79 撞 (1,9) 铁剑铁盾对话**停一步**（1b"keep-move 落点激活事件挡路"对）。
- **新发现并已修**：网站二次踩 (3,7) **不清** (1,9) 小偷（旧 sim 错误清掉）→ 即本次 hide 抑制修法。
- **AB 单变量对照**（`extract/verify_thief19_fix_ab.py`，临时只还原 hide 那一处）：修法前 u#75 (1,9)=0 被清 / 修法后 =123 仍在；u#82 都重收敛进 MT3(2,11)；**全程 6354 token 终态字节一致 → hide 修法对全程零回归**。

### ⚠ OPENING_PREFIX 82→83（连锁，下个会话必记）
- 修法后 (1,9) 小偷正确多挡一步，进 MT3 的开局前缀从 `tokens[:82]` 变 `tokens[:83]`（实测 `tokens[:83]`→MT3(2,11) hp400/atk10/def10，与原入口态一致）。
- 已同步 7 处"开局→MT3 前缀"语义常量：`probe_crossfloor.OPENING_PREFIX`(主·被多处 import)、`verify_mt3_quotient.PREFIX`、`analyze_mt3_quotient.MT3_ENTRY_TOKEN`、`diag_mt3_redgem`、`probe_beta_choices`、`probe_mt2_thief_desync`、`gen_beta_h5route`(注释)。**下面 line"N=82→MT3"全部已过时、按 83 读。**

### ⚠ 预存全程 regression（本次撞见，非本次引入，下个会话起点候选）
- test_checkpoints 金标准口径跑全程 6354 token，终态 = **MT45 HP=-25 won=False dead=True**，而非早期基准 won=True HP=14382。`git stash` 还原 HEAD（无任何工作区修法）跑全程**终态完全相同** → **早于本次修法、与之无关**。
- 与下方 "唯一 FAIL — token4141（sim 停 MT40 没进 MT41）" 吻合，疑同一断点致下游全错。17 检查点只到 token2000、覆盖不到 → 长期潜伏未被发现。

### 工作区状态（2026-06-11 已提交 commit `9248e26`）
- `sim/simulator.py` 三段修法（move/keep-move 无 loc + hide 抑制 + 注释）+ OPENING_PREFIX 82→83 连锁 + 7 个小偷线探针 + 文档，已随 commit `9248e26` 入仓（14 文件 +1010/-15）。
- 未入仓（别的工作线，原样留工作区）：`vzone.py`/`probe_crossfloor_beam.py`（V_zone/beam）、`verify_hide_suppress_fix_emulation.py`（无效对照，被 `verify_thief19_fix_ab` 取代）、六条 β `.h5route` 导出件（.gitignore 挡）。

---

## 🟡 h5route 网站回放排查：撞出【FMT 实为 fly(飞行道具)，非换层标记】决定性发现（待验证假设）+ 系统排除清单 + sim 小偷 move no-op 实质差异（2026-06-10·clear 前落盘，调查暂停、未改码）

> 本轮排查 `beta05_mt10_route.h5route` 网站回放失败（玩家报"挂在 MT2 小偷处、少走一步后面全错"）。系统排除开局保真/β段choices·小偷/FMT换层必需性/RLE跨换层后，**撞出一个可能是真凶的决定性发现：FMT 被网站引擎当 fly(飞行道具)瞬移执行**。⚠ 该发现【未用实数据验证，标待验证假设，别当定论】。**玩家叫停调查，先落盘再 clear，sim/solver/data 一行未改、产物全是只读探针。**

### 【决定性发现·待验证假设】FMT 在网站引擎 = fly(飞行道具)，不是换层标记

- **源码坐实**（`extract/route_encode_one_src.txt` 的引擎 `decodeOne`）：**没有 FMT 专用分支**。逐字符解码时以 `F` 开头的 token 命中 `case "F": decodeObj.ans.push("fly:"+nxt)`（nxt=getString 读到分隔符的串）→ **`FMT3:` 引擎解成 `fly:MT3`**（用飞行道具瞬移到 MT3）。
- **回放 handler**（`extract/replay_action_handlers_src.txt`）：`fly` 项 = `core.flyTo(floorId)`，先查 `core.canUseItem("fly")`。即路线里的 FMT = 触发飞行道具瞬移。
- **我的 `decode_route.py` 误解**：docstring/parser 把 `FMT<n>:` 当 "FLOOR:MTn = 被动换层标记"——**错**。`memory/project_h5route_tooling.md` 旧第 2 步"跨层处插 FLOOR:MTn 更稳"是错指引，已在 memory 改正。**本 handoff 下面"生成新路线标准流程"第 3 步、"换层标记(FMT)"段也按此误解写的，同样要废。**
- **含义**：生成的 .h5route 若带 FMT，网站当"飞行道具瞬移"执行；若此刻没飞行道具/不该瞬移，回放从该点乱套。**这可能正是 h5route 回放出错的真因（与 MT2 小偷对话无关）。**
- ⚠ **未验证 + 重要纠偏**：`beta05` 路线本身**全程无 FMT**（开局前缀 `tokens[:82]` 实测只有 `CHOICE:1`×2 + 纯移动，β 段纯 RULD）→ **FMT=fly 不直接解释 beta05 的失败**。它威胁的是【标准生成流程 `insert_floor_markers` 主动插 FMT】那条线。**beta05 的具体失败根因本轮未定，调查暂停。**

### 本轮系统排除（有效，下次别重复查）

1. **开局保真** ✅ 排除：`mt10_bosspass_77steps`(前缀 `tokens[:1169]`，含整个开局+小偷)玩家网站验过正常 → 开局（含小偷段）在网站忠实回放；`tokens[:82]` 是其逐 token 干净前缀（`probe_thief_events.py` 实测：开局只有 u#0/1 两个 `CHOICE:1`，其余纯移动、无 FMT）。
2. **β 段不 fire choices / 小偷** ✅ 排除：`probe_beta_choices.py`/`probe_mt2_thief_desync.py` 实测 β 段(token≥82) **fire 的 choices=0、(3,7) 触碰=0**，只 fire 一个无害 `setValue`。
3. **FMT 对换层非必需** ✅ 坐实：回放 handler `move`=`core.moveHero(action)`（与实时游戏同函数）→ **踏楼梯自动换层、不需任何 token**（`replay_action_handlers_src.txt`）。纯 RULD 段在网站踏楼梯会自动换层。
4. **RLE 跨换层多步** ✅ 排除：`probe_rle_crossfloor.py` 实测玩家路线有 53 个"跨换层多步 RLE 组"且回放正常 → 非病因。

### 待验证假设的【验证逻辑】（玩家给的对照思路，记下）

- 两个已验证文件（`51_roundtrip_full` + `mt10_bosspass_77steps`）玩家网站验过成功。
- 若它们成功纯靠"踏楼梯自动换层"（不靠中途 FMT 驱动换层）→ 证"不插中途换层 FMT"是对的；而带中途 FMT 的路线被当 fly 瞬移就会错 → **这个对照能验证 FMT=fly 假设**。
- ⚠ **核对点（别照搬玩家口述）**：按本 handoff line17，`bosspass` 实际**含玩家原本的 FMT10（前缀内）+ 末尾工具插的 FMT11**，且仍回放成功 → 说明那些 FMT 位置要么玩家真有飞行道具、`flyTo` 落点恰好对，要么 FMT11 在已踏楼梯后冗余无害。**验证时必须把"玩家真实 fly 用法"与"工具插的伪换层 FMT"分开核**，否则结论会乱。

### 顺带发现的 sim/网站实质差异（真 sim bug，记下，未必与本问题相关）

- **MT2 小偷 (3,7) 的 `move` 指令无 `loc`** → `sim/simulator.py:1748` `if loc is None: return` 直接 no-op → **sim 里小偷没从 (3,7) 搬到 (1,9)，只靠后续 `hide` 清 (3,7)**；但**网站上小偷真的走到 (1,9) 再 hide**。这是 sim 与网站对小偷终局位置的实质差异。
- 旁证（`probe_thief_events.py`）：sim 里开局走完后 (1,9) 是空地、可通行（玩家 u#79 D 从 (1,8)→(1,9) 正常移动），小偷事件在 u#70(撞)与 u#75(踏入)各 fire 一次。

### 本轮产物（全是只读探针，未改产品代码）

- `extract/probe_thief_events.py` / `probe_beta_choices.py` / `probe_mt2_thief_desync.py` / `probe_rle_crossfloor.py` —— instrument `_execute_event_list` 的只读诊断脚本。
- **sim/solver/data 一行未改。**

---

## 🟢 .h5route 编码器打通（路线一键生成、网站看回放，玩家不再手动按）+ pull 三层架构落地 + β 扫坐实"过boss"双重幻觉（2026-06-10·clear 前落盘）

> 本会话两件事：①写出并验通 .h5route 编码器——以后任意解算路线都能生成 .h5route、玩家在 h5mota 点开看【游戏自己引擎】回放，不必手动按键。②落盘 pull 三层架构 + β 扫结论（红钥匙/埋伏漏算导致"过 boss"是双重幻觉）。

### A. .h5route 编码器 —— 任意路线一键生成、网站看回放

- **`extract/encode_route.py`** = `decode_route.parse_rle_route` + lzstring 两层包装的**精确逆**。整盘往返自检通过（`python -m extract.encode_route`）：用玩家自己存档 decode→encode，**RLE 串逐字符一致(5188 字符)、meta 一致、6354 动作逐项一致**。decode↔encode 互逆已证。
- **两个验证文件玩家网站验收 ✅ 通过**（编码器全链路打通，含 Python lzstring↔网站 JS lz-string 兼容）：
  - `51_roundtrip_full.h5route`：原存档整盘往返，回放与原存档完全一致。
  - `mt10_bosspass_77steps.h5route`：前缀 `token[:1169]`（游戏起点→MT10 入口，含原本 FMT10）+ 77 步过 boss 段（末插 FMT11），回放到 **MT11 HP=701 ATK=30 DEF=30**。
- **🔑 最关键格式（下个会话必记）**：`.h5route` **没有"中途初始态"字段，回放永远从 seed 决定的游戏起点跑**。所以任何【从中途态出发】的解算路线（β 路线、77 步 boss 段），**必须在前面拼上"游戏起点→该段入口"的前缀 token**，否则网站会从游戏开头放、属性/位置全不对。
  - β 路线前缀 = **开场 82 token → MT3** 那段（`build_start()` 的 `OPENING_PREFIX=82`）。
  - 77 步 boss 段前缀 = `token[:1169]`（玩家存档重放到 MT10 boss 访入口，`export_mt10_boss_route.capture_boss_entry()` 定位）。
- **外层 meta（取自 `51_*.h5route`）**：`name='51' version='Ver 3.0' hard='' seed=1722097160`（= `encode_route.DEFAULT_META`）。
- **RLE 规则**：连续同向 `U/D/L/R`→字母+次数(=1 省略)；`CHOICE:n→Cn`；`FLOOR:MTn→FMTn:`；`ITEM:n→In:`；`KEY:n→Kn`；`MOVE:x:y→Mx:y`；其它(如 help)→`(token)`。
- **换层标记（FMT）**：sim 实测 `FLOOR:MTn` 是换层驱动（处理该 token 时楼层翻 + 英雄重定位到入口格）；纯 RULD 解算段则在踏楼梯那步 sim 自动换层。**生成时两者都给**（踏楼梯步 + 末尾插 FMT 标记）→ 对网站"自动换层/标记驱动"两种可能行为都鲁棒。FMT 由 `gen_h5routes.insert_floor_markers(段, 入口态)` 靠 sim 重放检测跨层点插入。
- **生成新路线 .h5route 标准流程**（下个会话照做，模板见 `extract/gen_h5routes.py`）：
  1. 拿解算动作串（纯 RULD，从某入口态出发）。
  2. 拼前缀 `tokens[:N]`（玩家存档重放到该入口态；β 用 N=82→MT3、其它段用 `capture_*` 定位）。
  3. `insert_floor_markers(段, 入口态)` 在跨层处插 `FLOOR:MTn`。
  4. **封板 sim 预检**：`replay_all(前缀+段)` 断言终态符合预期（排除拼接/编码错，不替代网站终审）。
  5. `write_h5route(路径, 前缀+段, meta)`。
- **工具**：`extract/encode_route.py`（编码器+自检）、`extract/gen_h5routes.py`（生成脚本+预检模板）。

### B. solver 当前状态：pull 三层架构 + β 扫坐实"过boss"双重幻觉

- **pull 三层架构已落地**（弃 κ 势能形后的目标导向新方向，落实红线 B"已兑现价值"）。`--beta β` 旋钮（`extract/probe_crossfloor_beam.py` + `extract/vzone.py`）：
  - **剪枝界（admissible D）= HP−D_free，不含 pull** → 上界仍 ≥ 真 final HP、剪枝不错杀。
  - **value_vector（保态身份维）不含 pull**。
  - **pull 只进 beam 排序键**（`beam_score_fn`/`_beam_rank_score`：排序键 = HP−D_free(κ=0) + β·pull）。pull = 朝可达高区势能、能下降攻防的道具的引导；**拿走即离场、门控够不到记 0** → 只在拿到时兑现、规划层只引导方向，**绝不"没拿就因有潜力加分"**（这正是 κ=1 反向病，命门单测钉死）。β>0 时基分恒用 κ=0（pull 取代 κ，一次只动 β 一个变量）。
  - **守卫单测 `tests/test_pull_guard.py`**：β=0 与纯 HP−D(κ=0) **字节零回归** + **value_vector 不含 pull**。
- **β 扫结果**（`extract/bscan_summary.py` 汇总 `crossbeam_cut_K50_vzone_b{β}_*.jsonl`，β∈{0,0.5,1,2,4,8}，固定 `--score vzone --beam 50 --diversity stairs`）：
  - **β=0.5 是甜区**：开局即拿铁剑（**第 82 步 vs 原 352 步**）——pull **治视野病是实打实进步**（玩家确认 MT4 刷临界点/MT6 省钥匙都是好做法、正常这么玩有机会过 boss）。
  - **但"过 boss"是双重幻觉**（`extract/export_bscan_routes.py` + `extract/bscan_routes_redkey_audit.md` 坐实）：
    1. **红门免费**：D 把上锁红门当免费穿（admissible 松弛）。**2843 个到 MT10 的 cut 态，0 个持红钥匙**；红门 (6,9) 是 MT10 入口区→boss 竖井的唯一网关（BFS 双落点 (1,10)/(6,10) 证实），无红钥到不了队长。
    2. **埋伏 8 怪漏算**：D 是最短路、只算路径上的怪；到达态(踩埋伏前)竖井路径上 0 埋伏怪（6 骷髅+2 骷髅兵在侧/顶格 kill-box），8 怪必杀损血全没算。
    3. **真实余量全负**：把红门+埋伏全 8 怪重算后每个 β 余量翻负（β=0.5 最佳 HP−D 从 +172 → **−227**）。
  - 结论：**pull 治视野病（开局拿剑）真进步；但"+余量过 boss"不成立**，是 D 两处乐观上界叠出来的假象。
- **待修决策问题**（玩家观察，拿完高价值物品后"迷茫"；下个会话方向）：
  - **MT7 绕路打零价值史莱姆浪费钥匙**。
  - **零伤怪发疯**（搜索对零损血怪行为异常、乱打）。
  - **提前拿血**（过早拿血瓶，应留到需要时）。
  - **D 漏算红钥匙门 + 埋伏全怪**（已在 `vzone.py` 文档化为 admissible 上界、非 bug；但正是它造成"过 boss"幻觉——要让搜索不被假余量骗，需让 D 认"红门要红钥"+算上埋伏必杀 8 怪的损血）。

---

## 🔴 κ=1(D_rel) 实测基本惰性/有害 → 玩家定架构新方向【目标导向规划 + 已兑现价值】，"潜力导向"形态弃用（2026-06-10，clear 前落盘·未改码·落地评估留新会话）

> 本轮导出 κ=0+stairs / κ=1+stairs 两条路线做**关键决策对照**（非总分）。玩家看完确认了 κ=1 的核心病,提出架构级新方向。**只导出+评估,本会话 sim/solver/vzone 一行未改,落地可行性评估留给新会话。**

### 这次实测坐实的（硬证据）

- **2×2 矩阵（κ × 多样性）**：κ=0+none→MT8；κ=1+none→MT8；κ=0+stairs→**MT10**(maxDEF25/ATK26)；κ=1+stairs→**MT9**。结论：**多样性(stairs 分坑)才是真正的杠杆；κ 惰性,且与 stairs 叠加时有害——把 stairs 那列从 MT10 打回 MT9。**
- **grab-incentive 探针（κ"悬而不拿"病的实证）**：同一态两种打分。拿剑前 ATK11/DEF10/HP340：V(κ=0)=−6549，V(κ=1)=−1408(savings=5141)；拿剑后 ATK21：V(κ=0)=−924，V(κ=1)=−348(savings=576)。**拿剑的 ΔV：κ=0=+5625，κ=1=+1060(弱约 5 倍)。** 恒等式 V(κ=1)=V(κ=0)+savings 精确成立——拿宝石 D_free 的 boss 项降多少,κ·savings 就同步降多少(宝石离开可达池),净拿取奖励=(1−κ)·Δboss,κ=1 时拿≈不拿。
- **纠正两处旧误判**：①κ=0 和 κ=1 最优路共享前 80 个动作(MT3 384 血骷髅裸打在 step30、属于共享脊柱)→ **MT3 裸打不是 κ 差异、不是"剑视野病"**;② κ=1 并非局部全劣:它**更早拿剑**(step182@MT5/HP426)、**拿了盾**(step511@MT9/HP56)、528 步 vs κ=0 的 1653 步、终态 HP38 vs 6——玩家"差不代表没价值"的直觉成立。但也非全优:MT7 atk21 损 558 vs κ=0 atk23 损 400,且 beam 始终没产出 MT10 态,卡死 MT9。

### 【κ=1 的真实病——玩家诊断】

- **像无头苍蝇**：突然去打对路线没帮助的怪(想拿属性但血不够=白扣血);血跑到一半不够,导致属性没拿到、血也没了。
- **MT9 盾优先级高了点,但视野仍不够**：盾在 MT9 很容易拿,它却偏先去 MT8 打怪(想拿攻击宝石但血不够=白打)。
- **没上 MT10 很可能不是楼层优先级判断错,是血耗光了根本没机会上。**
- **MT3 骷髅 / MT1 蝙蝠裸打的老问题 κ=1 也没解决。**
- **根因**：【高价值物品(剑盾)视野缺失】+【无目标的贪心导致资源耗散】。

### 【玩家提的新方向：目标导向规划】（待 clear 后评估落地）

1. **每一步先确定一个目标**(剑盾/高价值物品/攻防宝石/血瓶),评估身上物品够不够,不够则看血量/钥匙能否用廉价转化补足——而不是现在这样无目标贪心、血跑一半不够。
2. **全局价值视野**：玩家拿到塔第一时间会规划"如何用最小代价拿最高价值物品"。给物品价值排序,视野优先进高价值物品(哪怕在其他层),别被眼前小宝石勾走。
3. **相邻增益**：拿了一个高价值物品、旁边还有高价值物品,则这两处价值同时增加(引导把相邻高价值一起规划)。

### 【两条必须守的红线】（clear 后实现时关键）

- **红线 A**：目标必须【从价值排序涌现】(哪个价值最高朝哪规划),**不能硬编码"先拿盾"这类规则**(铁律)。本质即现有 CLAUDE.md 铁律"不把'剑盾优先/加到某阈值'写成死规则"。
- **红线 B**：相邻增益(第 3 条)要【和 κ=1 的反向病严格区分】:价值在【拿到时兑现、规划时引导方向】,**绝不能"还没拿就因旁边有高价值而加分"**——后者正是 κ=1"守着潜力刷分"的反向病,极易做反,必须明确"拿到才兑现、没拿不加分"。
- **评估笔记(供新会话)**：现 D_rel 把潜力折进了 admissible 剪枝界 D(→ 预先兑现 → 悬而不拿);玩家的"吸引/牵引"应放 beam 排序/保态层(→ 只在拿到时兑现 → 提交)。当前 v_zone_score 一个返回值身兼"排序分"与"上界 D"两职,要拆。pull 是排序启发,**不可进 admissible D**,否则复发 κ=1 的病。"高价值"必须从 boss_toll-Δ 数据涌现(剑盾 Δ 大→价值高),严禁硬编码。

### 当前状态总账

- κ=1(D_rel) 实测基本惰性甚至有害(见 2×2);诊断为"乐观潜力折价近似常数 + 奖励悬而不拿"。
- κ=0 那条到 MT10 但 HP=6 过不了 boss(骷髅队长 boss_toll(26,25)=360,**差 354 血**)。
- **D_rel/钥匙价值的工作不作废**(钥匙信号坐实那部分有效),但"潜力导向"这个形态要改成"目标导向规划 + 已兑现价值"。
- 本会话产物：仅新增 extract/ 导出脚本(export_k1_vs_k0_compare.py + 两份对照 md);sim/solver/vzone **一行未改**。对照详见 `extract/k1_vs_k0_compare.md`、`extract/mt10_best_route_k0stairs.md`。

---

## ⚠️ D_K（丙/钥匙预算最短路）验证：上轮结论作废 + 钥匙可达性盲区纠正（2026-06-10）

> 本轮在做"丙/D_K 原语验证 step 1"（不接搜索，仅验算 + 引擎重放 + 钥匙建模）。途中玩家纠正了一个**根本错误**：我用"地图总钥匙数"算稀缺度，犯了钥匙可达性盲区。**上轮的解读结论作废，但原始实测数据仍有效。** 玩家叫停，先落盘再 clear，不改代码、不选方向。

### 【作废】的结论（上轮误判）

- ❌ "D_K = D_free，钥匙零约束" —— 这是在**全局钥匙池**下测出来的（全图黄钥 56 把 vs 黄门 65 扇），所以 Dijkstra 觉得钥匙不紧张、D_K 退化成 D_free=1394、③/D_K **无信号**。
- ❌ "黄钥匙充裕（56/65）→ Φ_key/D_K 白做、解的是不存在的问题" —— **错**。

### 根因：钥匙可达性盲区

我只数了"地图上印着几把黄钥匙"，没区分**这一区、这个阶段，真正拿得到几把**。玩家给的铁例子（我完全没考虑过）：

- **MT2 有三把黄钥匙，但守着它们的是【三区的怪】** —— 正确打法是打到三区才回来拿，所以打一区时这三把**不可能拿到**。
- **MT8 右下的钥匙，守门怪很强，一区一般不拿** —— 会**打完 boss 上二区加了属性再回来拿**。

所以那 56 把里，相当一部分是"现在拿不到 / 不该现在拿"的。**真正一区此刻可用的黄钥匙远少于 56。** 而且即便 56/65 也已经**不是一钥匙一门**了 —— 钥匙缺口是必然的，大部分门都有开的价值，但钥匙不足，所以必须**抉择开哪些门**。

### 【纠正后】的结论

- ✅ 钥匙价值评估**是真问题**，不是上轮判定的"不存在的问题"。
- ✅ 我误判成"白做 / 无信号"，正是因为这个钥匙可达性盲区 —— 用"地图总钥匙数"算稀缺度，把"拿不到的钥匙"也算成了富余，得出"黄钥匙不缺"的假象。
- ✅ 真实情况：一区钥匙紧张、要抉择，③Φ_key / D_K 解的是真问题。
- ⏳ **待定（clear 后玩家拍板方向）**：如何用"当前阶段真正可达且值得拿的钥匙"来建模钥匙稀缺度，而非地图总数。

### 仍然有效的实测 / 诊断（与盲区无关）

- ✅ **起点钥匙 = 0**：`build_start()`（`probe_crossfloor.py`，82 个开场噩梦 token 后的真实 MT3 态）实测英雄 `keys={yellowKey:0}`（无蓝钥）、hp400 / atk10 / def10 / mdef0、items={fly:1, I333:1}、flags 含 {'03':1}。**上一份 summary 写的"黄3蓝1"是错的，已纠正为 0 钥匙。**
- ✅ **D_K 朴素"路径捡钥匙"会不停机**：捡钥匙后没把钥匙从图上移除 → 零成本环路反复重捡散落钥匙 → 预算无界增长（实测黄预算撑到约 150 万个唯一值，python 吃到 6.5GB 被 kill）。要"每把钥匙只捡一次"得做 2^56 子集追踪，**不可行**。简单封顶又会洗回 D_free，无意义。
- ✅ **几何计数（全图静态）**：门 黄65 / 蓝6 / 钢6 / 红1；钥匙道具 黄56 / 蓝3 / 红1。（注意：这是"地图总数"，正是上面盲区的来源，**不等于可用数**。）

### 待查 bug

- ⏳ **`vzone.py:299` "重复数已开的门"灌水**：静态分支门数用各层【初始】terrain 算，英雄在 MT1-9 已开过的门、静态图仍按门计 → 门数虚高。一并记着待查（与钥匙可达性是两个独立问题，但都让"门/钥匙账"算不准）。

---

## 🟢 工作区两条线分提交 + 钥匙荒/早换五方案落盘（P 已实现验证、③ 为下一步）（2026-06-10）

> 上一会话把"P 购买能力"实现并验完零回归就停了（符合"一次只动一个变量"），但改动一直未提交、五方案也只活在对话里没落盘。本次把工作区**拆成两条独立线分别提交**，并把方案状态写进文档，免得下次对不上。

### 两个 commit（线分清、零回归全绿）

- **commit `5e319a8`（线A：V_zone 前置1）** — `_killable` 解耦"可杀性"与"节点身份"：带 afterBattle/arrive 事件的怪**仍可杀**（先死后触发语义），节点身份由 `_is_free_tile` 单独保住。结构性修复 MT10 骷髅队长"打不动"误判。含配套单测 `tests/test_quotient_killable_hooks.py`（7 例）+ V_zone 全量接入前置1（`extract/vzone.py` 前置2 修 + `probe_crossfloor_beam.py` 注入 `beam_score_fn`）。
- **commit `fb4b5e3`（线B：P 购买能力）** — `allow_purchase` 开关 + `_resolve_choices`：搜索可买商人钥匙/祭坛属性，"祭坛买几次"靠 Pareto+beam 自收敛（非硬编码）。**默认关 = 字节级零回归**（`allow_purchase=False` 与 P 前 HEAD 一致，已用 `verify_p_purchase.py` 实测）；开启后 reach MT8→MT9。
- **零回归**：`pytest tests/` **100 passed**（含 46 检查点、MT33/MT38 单向阀、killable_hooks）。两条线分提交后 `git status` 仅余未跟踪临时产物，无已跟踪文件残留改动。

### 钥匙荒 / 早换 五方案状态（落盘，免得只活在对话里）

| 方案 | 内容 | 状态 |
|---|---|---|
| **P** | 补购买能力（商人钥匙 / 祭坛属性，付代价算子） | ✅ **已实现验证**，commit `fb4b5e3` |
| **K** | 钥匙保护口径改 boss 路径（保护维度对齐"打过本区 boss") | ⬜ 待做 |
| **③** | 钥匙期权项 Φ_key（把"手里钥匙"折算成势能） | ⬜ 待做，**玩家下一步**；Φ_key 估值口径**待玩家拍板**，未定不预设（铁律：优化偏好不写死规则） |
| **H1** | D 预见涨属性（启发里前瞻"换了装备后损血会降"） | ⬜ 待做 |
| **H2** | 没拿资源的期权值（未取道具/钥匙的潜在价值） | ⬜ 待做 |

### 前置状态订正（line-8 旧标"前置2待修"已部分过时）

- **前置1** ✅ 已修，commit `5e319a8`（见上）。
- **前置2**（boss 层 V_zone 退化修：D 指向 boss 格而非"该层任意格"）：**代码已落地**（`extract/vzone.py` 里 `BOSS_FLOOR/BOSS_CELL/BOSS_FLAG` + `boss_cell_live/live_shortest_toll/v_zone`），且**已随 commit `5e319a8` 提交**。但**端到端全量接入验证未在本会话复核**——下次接入时需重跑确认，勿当已验完。

---

## 🟢 V_zone(区势能启发) 验证 A/B/C/D 全过 + 纯V_zone独立缓解Q2砍谷底(分坑零贡献) + 全量接入【前置1已修✅/前置2待修】（2026-06-09）

> `V_zone = HP − D`，D=当前格→MT10 boss 的最短损血路（admissible 下界）。在 `extract/` 隔离脚本里全程验完（**零碰 solver 核心**：复用展开原语 `_absorb/_boundary_ops/_expand_op/_qfp` + `beam.beam_select`，唯一变量是把 score 换成 V_zone）。结论：**纯 V_zone 自己诱导出延迟交战**（MT3 裸攻态 wave15 自主搜到去 MT5 拿铁剑），且**面对 MT10 埋伏硬必杀段不怯战**（连杀 8/8）。怯战 caveat 排除、动态 kill-neutral cache **不为此做**。但 D 测顺带挖出全量接入的**两个硬前置**。

### A/B/C/D 各证什么（脚本 `extract/vzone*.py`）
- **A（`vzone_verify.py`，裁判自洽+admissible）**：shortest_toll 路径逐格累加==Dijkstra dist；atk↑→D 单调降（6839@atk10→55@atk80）。单怪 toll=引擎 compute_combat、Step2 边对拍 31 条（三重保证）。
- **B（`vzone_verify.py`，认拿剑价值）**：atk+10（MT5 铁剑）让 boss 路 D 从 6839→1461（省 5378 血），去拿剑单程才 246 血 → 净赚 ~4886，V_zone 认价值。
- **C（`vzone_verify_c.py`，延迟交战自主涌现）**：MT3 裸攻态真跑 beam（纯 V_zone 评分），**wave15 自己搜到去 MT5 装铁剑**，谷底（朝 MT5 去、掉血、剑没拿到）被砍峰值 37-38 但**没砍死**。
- **D（`vzone_verify_d_ambush.py`，硬必杀不怯战）**：真实 step 重放到踩 MT10(6,5) 触发埋伏后的态（8 怪已放、隊長 move 到(6,1)），覆盖属性让杀真付血，**K=50 与 K=1（最大竞合）都连杀 8/8、不卡死**。机理：踩埋伏后英雄被 8 怪封在自由块里，`_boundary_ops` 只产块边界 kill 算子（**无非杀逃逸**）→ 怯战在量子(块)抽象上不可表达 → 连杀是唯一推进。

### 纯 V_zone 隔离结论（决定全量接入）
- **V_zone 本身缓解 Q2 砍谷底，`_stairs_key` 分坑零贡献**：关分坑 vs 带分坑，装剑 wave(15)/装剑态/谷底峰值(37 vs 38) 全部不变 → 分坑非必需（留作通用安全网无害）。
- **怯战副作用排除**：固定 mon_cache 口径惩罚杀怪（杀→HP↓ 但 D 不降→V_zone↓），但在拿剑程和 MT10 硬必杀段都没造成瘫痪。

### ⚠ 全量接入【两个硬前置】（玩家 2026-06-09 定的方向）
- **前置 1（队长结构性不可杀，真 blocker）✅ 已修（2026-06-09）**：MT10 队长(6,1) 挂 afterBattle → 旧 `_killable`(`solver/quotient.py`) 一刀切排除挂 afterBattle/beforeBattle/到达事件的怪 → 展开原语**只生 trigger 算子、生不出 kill** → beam 过不了 boss。
  - **修法**：把 `_killable` 里的事件排除**整段删掉**——「是否独立节点」与「是否可杀」**解耦**：怪是 noPass、英雄活着踩不上怪格 → 怪格上的一切事件（afterBattle 战斗钩子 / 到达事件）**都是「怪死后」语义**，故都可杀；杀经 `_expand_op→step` 在引擎 `_fight_monster` 内**自然触发 afterBattle**。独立节点身份由 `_is_free_tile`（怪一律非自由格、永不并入块）**独立保住**，与可杀正交。全塔核对无「活怪格可踩触发剧情」之例（MT33 单向阀走 flower+outEvents、不经 `_killable`；MT40/MT42 两个「怪+到达事件」格皆死后通行守卫；beforeBattle 全塔=0=死代码）。
  - **关键发现——队长是【两步】过、非直接 kill**：队长被**决斗喊话到达事件 `events["6,2"]`**（`"来吧,我要与你决斗!"`+`hide remove` 自删）门控。缩点序列：①连杀 8 埋伏怪（kill 算子）→ autoEvent 开 (6,3)；②(6,2) 是独立 **trigger 节点**挡在队长与自由块之间，队长此刻**不**直接出 kill；触发 (6,2)（自删入 `_suppressed_events`、英雄踏入）；③队长暴露在边界 → **kill 算子出现** → 杀之 → afterBattle 开 boss 三门 (4,4)/(6,7)/(8,4)+清红门(6,9)+置 `flag:10f战胜骷髅队长` → 自由块涨到 39 格 → **走 (6,11) 楼梯进 MT11**（实测 atk20 态 HP=2475）。
  - **⚠ `extract/vzone_verify_d_ambush.py` 的「结构性停在队长」结论已过期**：其硬编码叙事写于旧 `_killable`（脚本自己已打印矛盾 `_killable 判定=True … 预期 False`），且其 loop 在「8 怪清完」即 `break`、只看那一瞬的边界算子（=(6,2) trigger）、**没多走一步触发 (6,2) 暴露队长**。非真死路。
  - **测试**：`tests/test_quotient_killable_hooks.py`（7 项）——队长可杀 / 两步门控→杀→afterBattle 开三门 / 队长杀前仍独立节点 / MT2(6,2)(8,2)+MT8(9,5)(11,5) 四钩子怪「可杀但仍独立节点」。MT33 单向阀 4 单测仍 PASS=反证隔离。**全套 100 green、`_killable` 不在 sim → 重放/检查点自动零回归**。
- **前置 2（boss 层 V_zone 退化，次要）**：`shortest_toll` 目标是「到本层任意格」，src 已在 boss 层 → reach=0 → D=boss_toll 常量 → **零推进梯度**（D 测实证：MT10 各格 reach 全 0、D 全 1045、V_zone=HP−常量）。**修法**：boss 层目标**指向 boss 格 / 出口**，而非「任意层格」。

### 已确认 + 下一步
- **已确认**：全量**带分坑**（通用安全网，玩家拍板1）；**动态 cache 不为怯战做**（D 已证硬必杀段不瘫痪）。
- **下一步**：修这两个前置 → V_zone 全量接入 beam 核心（**λ 旋钮、λ=0 零回归保底**，与时序V 同口径）→ 跑一区验证。
- 残留（D 未覆盖）：D 是隔离测、无全塔全局竞合；全局怯战（爬一半失血态被别处高-V 挤掉弃坑）靠分坑兜，且埋伏室入坑即封死、怯战只可能在「要不要踏入(6,5)」入口决策处，D 未测入口。

---

## 🟢 时序V(区势能·按存活残留)落地+校准正确+零回归；但【析因证明】捅破MT7天花板抢铁盾的杠杆=beam宽度(K50→200)+floor分坑，**不是**时序V(λ)——λ处置待玩家定（2026-06-08）

> 玩家 2026-06-08 钉死「估值最本质缺陷=减伤有时序性，区势能必须按存活残留算」。本段把它实现进 `solver/beam.py`（`build_future_roster`/`_region_bounds`/`_alive_mids_on`/`_future_potential`，塔无关，boss 层由门禁结构 `_is_region_boundary` 读出非写死），并用**一次一变量**析因隔离出真正的因果杠杆。结论：**时序V实现/校准/零回归全对，但它不是这一关的因果杠杆**——需玩家终审 λ 去留。

### 时序V 是什么（修两层缺陷）
- 公式：`V = HP − Σ_{m∈R近}cost_exact − λ·Σ_{m∈区·非当前层·当前存活}toll`。
- 修 ①**R 近视**（旧 V 只看当前层批次前沿怪）→ 补「当前区到 boss」整段；修 ②**时序性**（减伤只惠及拿到后才打的怪）→ 怪集按 `_alive_mids_on` **存活残留**算，杀一只少一只，早拿盾比晚拿值钱。
- 视野=当前区到 boss（每区须打 boss 才进下一区，boss 层=上行楼梯被初始门禁 `enable:false` 封的层，一区唯一=MT10）。

### 校准（数据真算，**盾/剑价值的事实来源**，非硬塞权重）
一区裸态 Σ_区 toll=**95,659**；假想加装对「本区到 boss 剩余存活怪 grind 损血」的落差：
| 永久增益 | Σ_区 | 落差(减伤) |
|---|---|---|
| +10DEF（铁盾@MT9楼梯） | 75,395 | **−20,264** |
| +30DEF | 41,083 | −54,576 |
| +100DEF（神圣盾@MT44飞行，楼梯够不到） | 13,860 | −81,799 |
| +10ATK（铁剑@MT5楼梯） | 46,449 | **−49,210** |
| +100ATK（神圣剑@MT44飞行） | 33,715 | −61,944 |

**弱英雄期 ATK 减伤(49,210) > DEF 减伤(20,264)**——时序V 会因此强烈奖励攒攻，是它的固有倾向（见下 λ 副作用）。

### 析因（一次一变量，reach / 是否抢到 MT9 盾态 / hit_cap）
| K | λ | 分坑 | 到达 | DEF态 | 备注 |
|---|---|---|---|---|---|
| 50 | 0 | none | MT7 | 12 | 基线 |
| 50 | 0.05 | none | MT7 | — | 加 λ 无用 |
| 50 | 0.05 | floor | MT7 | — | 光分坑(窄K)无用 |
| 200 | 0.05 | none | MT8 | — | 光宽 K(无分坑)只到 MT8 |
| **200** | **0.05** | **floor** | **MT10** | **24** | **突破**：reach MT10、★每层超 route 首过、realized 区 toll 95,659→35,331(↓60,328)、3 重放✅ |
| **200** | **0** | **floor** | **MT10** | **25** | **λ=0 也突破**！MT8 530/25/25、MT9 361/25/25，HP 还更高 |
| 500 | 0.05 | floor | MT7* | — | *wave67 撞 cap、cap 饿死，无效格 |

- **因果落点**：捅破天花板的是 **beam 宽度(K 50→200) + floor 分坑**（窄 K 时低层刷怪便宜货占满槽位挤死 climber；分坑按 current_floor 各保骨架强制留爬楼党）。
- **λ 不是杠杆**：`λ=0/floor/K200` 已 reach MT10、DEF=25；加 λ=0.05 到达时 HP **反而略低**（与「ATK 减伤>DEF→λ 诱导付 HP 抢攻」一致）。
- **教训**：`hit_cap=True 且 wave 数少`（K500 wave67 撞 cap）的跑分**对 reach 不可结论**——climber 在后段 wave 才出现，cap 饿死会假阴。

### 零回归（守红线）
- `λ=0` 时 `_future_potential` 返回 `0(int)`，V 路径与旧版**字节级等同**；**93 单测全过**。时序V 没坏，只是不是这关的因果杠杆。
- 病根与修法已钉进 `docs/solver-design.md`「估值最本质缺陷=减伤有时序性」段。

### ⚠ 待玩家终审 + 下个会话起点
1. **λ 去留**（玩家拍板）：(a) 留低 λ(如 0.02)当温和先验、靠它在更深区/更大塔指方向？(b) λ 归 0、时序V 只作诊断/校准工具？(c) 别的判断？我不预设（铁律：打法策略是搜索该自己算的结论，不是死规则）。
2. **【拿盾真伪待逐 token 证实】**：breakthrough 日志里 **maxDEF=24 在每一层(连 MT1)都出现**——这是「爬上去拿了属性再回访低层」的累计签名，**光看 Pareto 表无法判断 DEF=24 到底是 MT9 铁盾(+10)还是纯防御宝石堆的**。本会话写了 `extract/export_shield_route.py` 重跑 K200/λ0.05/floor、取拿盾候选**逐 token 引擎重放**，定位 MT9 铁盾那一步(第几步/此前累计杀怪/当刻属性) + 量化「晚拿盾」损失(盾对剩余未杀区怪减伤 vs 开局即拿满额 20,264)，落盘 **`extract/shield_route_decision.txt`/`.json`** 供玩家下会话①照走真实游戏终审 ②判定是否「晚拿盾多打怪」亏路线（玩家怀疑：先打怪攒属性才拿盾、已死怪没享受减伤=亏，只因 route 首过基准故意留得低才显「支配」）。
3. 红线照旧：goal=MT0 未喂盾坐标=真自主；每候选封板 `solver.verify.replay` 独立核对；route 仅下界对照、不喂走法；任何「支配」一律玩家真实游戏终审。
4. 后续（玩家锁的顺序）：飞行边（第 2 步，接 MT44 神圣盾+100/神圣剑+100 与 MT0）；退役 phase1 强制骨架（第 3 步）。

---

## 🟢 A段Δ形式V落地=平局(754) + 根因定位(层序骨架锁死可达层) + 向上远征验证实验在建 + roster单pass优化封板（2026-06-08）

> 接「V+beam 落地」之后：把段内价值函数升级为 **Δ形式 V（杀怪中性）**，并诊断为何 754 卡住不动。结论：**Δ形式 V 行为完全正确，但 754 是平局非进步**——根因是 route 层序骨架结构性锁死了「能去哪些层」，大额永久回报落在骨架够不到的上层。下一步「向上远征最小验证实验」已建好待跑。

### A段 Δ形式 V：行为对，但 HP=754 是平局非进步
- **Δ形式 V**：`V = HP − Σ_{m∈R} cost(m)`，R 是**固定参照怪集**（不因杀怪缩小）→ 度量的是 atk/def 等级，**对杀怪中性**（杀不杀同一只怪 V 不变）。修掉了旧 V「杀怪即涨分」的偏置。
- **行为验证全对**：不再为涨分清楼、杀怪中性、自然攒到 sword1。
- **但 HP=754 仍是平局非进步**：段末最优点 ATK20/DEF10 与 route 完全一致，754 纯粹来自**少打 4 只怪省的血**，没有任何属性维度超过 route。即 Δ形式 V 在「借 route 层序骨架」的笼子里已做到最优，天花板就是 754。

### 根因（自己悟出 + 玩家证实）：route 层序骨架锁死「去哪层」
- **phase1 借 route 的换层顺序**作骨架，块图搜索只在每层内朝 route 出口格优化。前 9 段层序 MT1→2→3→2→3→4→5→4→5 在 **MT4↔MT5 反复横跳**，**MT6-13 整个会话从没踏足**。
- 因此 MT6-13 上的**大额永久属性回报**（盾类）对局部 V **结构上不可见**——不是 V 估错，是这些点根本不在搜索能到达的状态空间里。
- 推论：要打穿 754，必须让搜索**有能力离开骨架**去上层取永久回报再返回主线——这正是下面的验证实验要测的假设。

### 当前在建：向上远征最小验证实验（B 第 3 点，待跑）
- **目标**：在骨架上允许插入一段**向上远征**——从反弹区（MT4/MT5）经**楼梯**爬若干层、取永久防御回报、再返回，验证「正确路线含跨多层大回报」假设；用 Δ形式 V 当指南针 + 缩点 + beam。
- **红线**：可达性扩展**非硬编码目标**（去哪/取什么/值不值由 Δ形式 V + 引擎损血算，不写死「去MT9拿盾」）；远征只走**楼梯**（fly 魔杖在 sim 里过宽、无真实 canFlyTo 门禁，不可用）；任何「支配 route」的点一律**玩家真实游戏终审**。
- **数据驱动探出的拓扑**（probe_topology.py，非手推）：上行楼梯链 **MT5→MT6→MT7→MT8→MT9**；最近的大额 DEF 回报 = **shield1@MT9(9,7)**；**MT10→MT11 楼梯 @(6,11) enable=False**（事件门禁）→ 自然爬升上限卡在 MT9/MT10。
- **状态**：脚手架 `excursion_experiment.py` 已建（起跑已验证：run_phase1(6) 后 50 个点在 MT5@(2,11)）；刚修掉 `GameState.floor` 只读属性 bug（误给 `meas.floor=` 赋值→改成只设 `meas.current_floor`，property 自动解析）；**长实验未端到端跑完，下个会话从干净状态重跑**。

### 旋钮已锁（双值 V 的两个自由度）
- **Σ 锚（参照怪集 R）= 段末前沿存活并集**：R = beam 批次内「在任一前沿点存活且可杀」的怪 (floor,x,y)→mid 之并集。
- **diff 基线 = 段入口 atk**（仅展示用；排序与基线无关，秩等价）。
- **BIG（杀不动的 cost）**= 全批次最大可杀损血；跨过防御阈值使 cost 掉到可杀 → V 跳变。

### 顺手封板：roster 单 pass 优化（beam.py）——已完成非待办，93 全绿
- 旧码每个 (point,monster) 战斗算**两遍**（先 region_reference 求 BIG，再 equiv_hp_over_roster 求 V）。新 `score_points` **只算一遍**：每点缓存 (hp, Σ可杀, 杀不动数)，求出 BIG 后 `V = hp − sk − uk*big` 纯算术，数学等价（Σcost = sk + uk*BIG）。
- phase1 / export_path 已切到 `score_points`；**93/93 单测全绿（零回归）**。

---

## 🟢 状态相关价值函数 V + beam 控宽落地：前 9 段 K∈{50,100,200,500} 全复现 exact、收敛 K=50、11× 提速——墙（段间宽×段内成本）被打穿（2026-06-08）

> 玩家 2026-06-07 拍板的 [状态相关价值函数 + beam 控宽] 实施完毕并通过前 9 段全验收。新增 `solver/beam.py`（塔无关）、`phase1.py --beam K` 通道、`seg_sensitivity.py` 实验编排。**exact 前 9 段（真值）= 754；K=50→500 四档全部复现 754，收敛 K=50。**

### 实现（塔无关，红线照旧）
- **V 标量**（`solver/beam.equiv_hp`）：`V(state) = HP − Σ_{段边界可杀怪} damage(m, state)`，纯引擎 `compute_combat` 算（含 cross/knife 被动），**无任何硬编码常数**；边界可杀怪复用 `quotient._boundary_ops` 的 kill 算子，与段内搜索同口径。属性高→边界怪损血小→V 大；HP 高→V 大。
- **beam 截断**（`beam_select`）：保护维 = `value_vector` 的 `key:*`+`item:*`（钥匙/消耗道具各类）；**硬保护**=保护维 Pareto 骨架必留（每非支配组合留其 V 最高代表，防「省钥匙换血」假优），剩余配额按 V 降序填到 K。血瓶残留落在**身份维**（`merge_frontier` 的 `residual_fingerprint`，不同则都留），不进 V 标量。
- **落盘审计**：`phase1._apply_beam` 把每段截点写 `extract/phase1_beam_cut_{tag}.jsonl`（含 value 向量 + V 分 + actions），**绝不静默丢**。

### 前 9 段敏感性曲线（每段出口最优 HP；exact=真值，route=下界基准）
| 段 | 层 | route | exact | K50 | K100 | K200 | K500 |
|---|---|---|---|---|---|---|---|
| 4 | MT3 | 926 | 926 | 926 | 926 | 926 | 926 |
| 5 | MT4 | 852 | 852 | 852 | 852 | 852 | 852 |
| 6 | MT5 | 754 | 754 | 754* | 754* | 754* | 754* |
| 7 | MT4 | **716** | **754** | 754* | 754* | 754* | 754* |
| 8 | MT5 | **660** | **754** | 754* | 754* | 754* | 754* |

（`*`=该段发生截断；seg6/7/8 exact 宽 1285/9378/12881 全被截到 K，**最优 HP 仍 == exact**。seg0-3 全 1000/1000/400/400。）

### 验收四条 全过
1. **beam ≥ route**：每档每段成立；**seg7/8 beam 严格超 route**（754 vs 716/660，+38/+94）——优化器找到比人类 route 更省血的路。
2. **复现 exact**：K=50 即逐段 best_hp == exact（含被截到 50 的 seg6/7/8）。
3. **裁判一致**：exact + 四档 beam 全程 `mismatch=0`、末态前沿重放裁判通过。
4. **截点落盘**：✅ `phase1_beam_cut_seg{5..8}_*.jsonl`。

### 收敛 K + 提速（墙被打穿）
- **收敛 K = 50**：K50=K100=K200=K500，10× 宽度区间零增益 → V 抓对了特征。
- **总耗时**：exact **3508s** → K50 **311s**（**11.3×**，无损）。逐段：seg8 exact 1722s（单段！）→ K50 **41s（42×）**、seg7 1529s→137s、seg6 236s→111s。
- **机制**：beam 把「传给下一段的入口点」从 ~1.2 万砍到 K，消掉「**段间宽 × 段内成本**」那个乘数。新瓶颈 = 段内 per-entry 搜索（seg7 K50 仍 18249 段内态/137s），比旧墙小 ~10×。

### 待续（下一步）
- **本验收只覆盖前 9 段**（exact 真值存在的区间），度量 = 逐段 best_hp。seg9-224 exact 不可行，**K=50 在更深段是否仍无损未测**——由三保险（裁判链 / route 基准 / 玩家终审）兜底，下一步**带 K=50 推深**看曲线是否继续饱和。
- 保护维硬保护设计意在保住「非 HP 最优但钥匙/道具更多」的下游有用点；本实验度量的是 best_hp 复现，下游有用性随推深暴露。

---

## 🟢 缩点商图接入主管线 + 10 段膨胀曲线 + 前沿成分诊断：定位真正的墙，玩家拍板下一步【状态相关价值函数 + beam 控宽】（2026-06-07，commit 6a664f6）

> 块图搜索 `solver/quotient.search_quotient` 接入 `phase1._run_block` 替换段内朴素 BFS（任务 #13）；seg3 盗贼 NPC `hide` 触发格修复（`_expand_op` 撞开后再多走一步踩上已清格）；补 `hit_cap`/`intercept_locs` 上报。塔特有信息（route 层序 / 层 / 格）只在 `phase1.py` 读，`solver/` 全程塔无关。

### 现状：线性链推进到 seg9，exact Pareto 推不到全 225 段
- **10 段全曲线**（块图+商图，借 route 层序骨架）：seg4(MT3) 原 23min/cap 墙 → **8s 完整跑完**；段末宽 4→128(seg5)→9378(seg7)→**~1.2 万饱和**；总耗时 2.3h，**裁判不一致=0、hit_cap=0、软截断=0，末态前沿重放裁判 3/3 一致**。
- **墙 = 段间前沿宽 ~1.2 万 × 段内 ~85min/段**（seg9 gen=3.46M / 5081s）。route 共 **225 个 floor-segment**，纯 exact Pareto **推不到底**。
- 首个商人 seg5 MT4(6,1) blueShop → 商图识别为拦截格、记 `intercept_locs=[(6,1)]` **未强解**；道具 fly/I333 全程 distinct=1 统一拾取无分叉。seg4 最佳出口 = route 一致 HP=976、无严格支配。

### 前沿成分诊断（玩家「先诊断成分」裁定的产出；diag_frontier_composition.py，N=6 width=128）
- **分层分解 5 → 69 → 128**：真阈值维 (hp,atk,def,mdef) Pareto 仅 **5** 点；加记账维 gold/key/kill → 69；全前沿 128（+59 value-被支配、靠地图残留指纹存活）。宽 = 真阈值数的 **25.6×**。
- 撑宽**唯一**主成分 = `floors` 残留，且只 **MT3(怪10/地形8)+MT4(怪31/地形12)** 在变，MT1/2/5 全常量。
- **⚠ 证伪「死层折叠」的近端收益**：diag_floor_schedule.py 算出 route **首个死层 = MT3@seg72**，MT4 回切到 seg247、MT5 到 seg245，**seg72 前无任何死层** → 当前深度地图残留是**合法载荷非虚胖**（「留怪到更强再杀」coin×2/后杀更优真实成立），**无可廉价无损剪掉的胖**。

### 玩家拍板的下一步方向（下个会话实施）：状态相关价值函数 + beam 控宽
- **思路**：对每个**非血瓶持有物**按当前 state 估**等效血量**，前沿点按「HP + 持有折算等效血量」打分，**每段只传 top-K**（beam）。
  - **黄钥匙** ≈ 等效血量，且随推进期望涨（越深越值钱）。
  - **攻宝石**价值 = 能否跨过**临近必打怪的减伤阈值**：临近卫兵（差 1 攻就跨阈）时**暴涨**，差 5 攻才见效时**暴跌**。
  - **centerFly / 飞翼** = 使用时机价值。
- **四要点**：
  - **(a) 估值全部可计算**——减伤阈值用**引擎战斗公式**算「ATK/DEF +1 对边界算子怪的损血变化」，**不拍脑袋**（呼应铁律：不在对话心算、所有计算由代码完成）。
  - **(b) 有损，但配三保险**：每段引擎裁判链 + route 基准（结果**不得差于 route**）+ 玩家终审。
  - **(c) 与缩点互补**：缩点**无损**合并等价态、beam **有损**丢弃低估值态——两层正交。
  - **(d) 第一版手写折算公式、不训练**；远期可用跑出的数据**学 V 函数**。

### endgame 优化待办
- **死层残留折叠（无损）**：把剩余 forced 层表传进 `solver.frontier.merge_frontier`，对不再回切的层折叠其残留指纹——seg72 前零收益，endgame（seg240+ 大批层死亡）才显著。记为待办，非近端。

---

## 🟢 solver MVP B段（MT17·装备阈值决策）闭环：三条验收全过 + 出口 Pareto 前沿 + 严格更优查询【cls 数据驱动口径修正】——route 坐实在前沿、装备阈值两子前沿实证（2026-06-05，commit 854759d）

> 入口 `tokens[:1462]` → MT17(5,11) HP655/A44/D32 → 出口格 MT17(2,11)(token≈1499)。塔特有参数由 `mvp_b.py` 注入、通用闭环在 `seg_experiment.run_segment`，`solver/` 全程塔无关。route 真值取自 route 全程重放(见 `diag_mvp_b_truth.py`)。

### B段结论：回路闭环成立 + route 是该段 Pareto 前沿上的非支配点
- **三条验收全过**：① 找到到 MT17(2,11) 的合法 **21 步**路线 `URRDRRRRLLLLULLDLLLLR`；② 搜索宣称终态 == 引擎独立重放终态(逐字段一致)；③ 终态 HP=719 ≥ 基准 459。
- **出口 Pareto 前沿 = 44 个非支配点**(goal_hits=1342)，ATK 仅 ∈{44,64}、DEF≡32(段内无人取 MT17 出口仍留地上的 redGem/blueGem——拿宝石不划算是搜索算出的，非预设)。
- **装备阈值【两子前沿】实证**(搜索涌现，主题正中)：段内 sword2(+20ATK,在(2,2))把前沿劈成两支——
  - **取剑**：ATK64 子前沿，HP 高点 **591**，代价是吃光地图 2 个储备 redPotion；
  - **弃剑**：ATK44 子前沿，HP 最高 **719**，代价是永久少 20 攻。
  两高 HP 点**都不真支配 route**(各自在 redPotion 储备维或 ATK 维落后)——复刻 A段「吃储备血瓶换 HP」的证伪模式，再次印证 `solver-design.md` 的「地图剩余 HP 消耗品」价值维。属性边际价值阶梯状(取剑跨阈值改写后续损血)在此被前沿分叉直观坐实。
- **route 在前沿**：route 完整向量 `HP=459 ATK=64 DEF=32 金=235 kill=89 黄钥=1 地图剩[bluePotion=1 redPotion=2]`，为非支配点；要 HP 更高必须放弃钥匙或提前清掉地图储备，无「白赚」的严格更优点。

### 口径修正（本段核心教训：item:* 维同 A段「map:钥匙」同根——口径不齐造假胜负）
- **假支配现象**：严格更优查询曾误报「找到 1 点严格支配 route」——该点与 route **各属性全等**，仅因前沿向量带 `item:fly=1` 而 `route_vec` 缺该维(当 0)→虚假支配。根因：`_value_map` 把 hero.items 全展成 `item:*` 维，而 route_vec 没有 → 口径不齐。
- **修复（玩家 2026-06-05 裁定，数据驱动细分）**：新增 `seg_experiment._project_for_compare`，按 `items.json` 的 **cls** 裁剪 `item:*` 维后再判支配——**消耗品**(`cls==tools`，会随使用减少→余量是真价值维，如 centerFly/bomb/earthquake 段间传前沿时关键)**保留**；**常驻能力道具**(`cls==constants` 等不消耗类，如 fly/wand/book/I333，段内两边恒等)**投影掉**。两边同口径投影后再比。route_vec 持有维补 `mdef` 对齐 _value_map。**按 cls 从 items.json 读，绝不写死 id，塔无关。**
- 本段实测：入口/出口 hero.items 全为 constants(fly/I333/book/wand)、无任何 tools 消耗品、mdef=0 → 投影后前沿与 route 同口径 → 结论翻转为「**前沿中无任一点严格支配 route**」(route 在前沿)。

### 性能：B段 11.6K 状态 / 13.4 秒，_copy_state 占比升至 ≈63%（copy 优化依据加强）
- expanded=11,637 / generated=46,548(≈step 调用数) / 去重指纹=11,093 / 队列峰值=339 / goal_hits=1,342 / hit_cap=False / 搜索耗时 **≈13.4 s** / **288.5 μs/step**。
- 状态数比 A段少 ≈18×(单层近目标段)，但**每步更贵**：A段 ≈182μs/step → B段 ≈288μs/step，因 B段入口加载 **17 层**(A段更早、层少)，`_copy_state` 每步全量深拷更重。cProfile：`_copy_state` cumtime **≈7.84s/12.47s ≈ 63%**(A段 ≈48%)——**深拷随加载层数线性变贵已坐实**，是 copy 优化(单层段内只深拷当前层)的硬证据。**仍按【选项 2：暂不优化】暂缓**，待 C段(MT33)跑完确认是全局瓶颈后再定，封板引擎(commit fbdfc17 / tag simulator-complete)不冒回归风险。

### 下一步：C段（MT33 极限压测）
- C段：MT33，**HP=6 极限态**(损血精度试金石)。先重放取 C段 route 真值 → 写 `mvp_c.py` → 跑闭环(沿用本段 cls 口径修正后的 `seg_experiment`)。验收沿用三条 + 出口 Pareto 前沿 + 严格更优查询。

---

## 🟢 solver MVP A段闭环：BFS+Pareto支配+引擎裁判全通——route 坐实为有效 HP 基准、253 证伪、Pareto 维度定型（2026-06-05，commit fe7da8a）

> 入口 `tokens[:680]` → 出口格 MT8(3,11)。求解器全程**塔无关**(`solver/search.py`)，塔特有参数(层/token/出口/基准/route 真值)由 `mvp_a.py` 注入、通用闭环在 `seg_experiment.run_segment`。设计与领域知识落盘见 `docs/solver-design.md`。

### A段结论：回路闭环成立 + route 是修正后前沿最高属性层的有效基准
- **搜索回路三条全过**（搜索内 step 推进 → 引擎独立重放裁判，引擎只当裁判不进搜索循环）：① 找到到 MT8(3,11) 的合法 **43 步**路线；② 搜索宣称终态 == 独立重放终态(逐字段一致)；③ 终态 HP=253 ≥ 基准 203。
- **出口 Pareto 前沿 = 14 个非支配点，ATK/DEF 全为 26/24（宝石拿满）**——"疯狂拿宝石"是搜索**自然涌现**的，前沿无任一点放弃宝石(修正前曾爆炸到 143 点、含 HP=22「几乎没拿」的退化点，见下"教训")。
- **route 在 HP 维上不被支配，是合格下界**：route 完整向量 `HP=203 ATK=26 DEF=24 金=119 kill=44 黄钥=3 地图剩[redPotion=3 bluePotion=1]`。严格支配 route 的点**仅 1 个**，且**与 route 全维相同、只多 +1金/+1kill**(`金=120 kill=45`，HP/ATK/DEF/钥匙/地图剩余全持平)——这是一次**零损血顺手多杀一只金币怪**(HP 中性)，对"剩余 HP 最大化"目标**无影响**。即"route 在前沿最高属性层上(全宝石拿满)"，技术上仅被一个 HP-中性的免费多杀点支配。
- **253「更优路线」证伪**：253 HP 虽 +50，但 `yellowKey=2`(比 route 少 1 把硬通货)、地图剩 `redPotion=2`(吃掉了 route 特意留在地上的 1 个储备血瓶)——**补上「钥匙 + 地图剩余血瓶」两维后 253 不再支配 route**。那 +50 是"提前兑现储备 + 少持钥匙"的虚高，非白赚。

### 教训落盘：搜索会钻记分牌盲区，Pareto 维度必须补全
- 旧查询只比 ATK/DEF/HP → 把"吃储备血瓶 + 少持钥匙"的高 HP 出口误判为严格更优。**修正：出口价值 = 持有(HP/ATK/DEF/MDEF/金/各类钥匙) + 地图剩余「给 HP 的消耗品」(血瓶)**；宝石/装备(永久增益)与钥匙只计**持有**、不计地图剩余(拿到才兑现)。分类**数据驱动**(读 `items.json` pickup 效果，`_gives_hp_on_pickup`)，塔无关不写死 id。
- **本塔宗旨(已固化为 `solver-design.md` 铁律)**：血量充足时**绝不用道具/吃血瓶换血**(血瓶留地上 = 银行里的 HP / 夹击储备)；**钥匙是硬通货必须进 Pareto 维度**；同持有属性下**地图留着资源的出口严格优于吃光的**。一句话：血够绝不换血，少血才换多资源。
- **基准对比口径坑(本段踩过)**：`route_vec` 与前沿向量**必须同维度口径**——cfg 的 route 真值含"地图剩钥匙"，但前沿已不发该维，不投影则 route 白赚无人匹配的维 → 虚假非支配。修复：`seg_experiment._route_vec` 把 route 的 map 也过 `_gives_hp_on_pickup` 滤成"只留血瓶"。

### 性能：A段 207K 状态 / 150 秒，_copy_state 是头号热点
- expanded=207,265 / generated=829,060(≈step 调用数) / 去重指纹=187,152 / 队列峰值=4,917 / goal_hits=15,924 / hit_cap=False / 搜索耗时 **≈150.9 s**。
- **`sim/simulator.py:_copy_state` 占大头**(cProfile cumtime ≈74s/150s ≈ **48%**，tottime ≈65s)——每步全量深拷整个 GameState(含所有层)。
- **copy 优化按【选项 2：暂不优化】暂缓**：封板引擎不动(commit fbdfc17 / tag simulator-complete，80 单测 + 46 检查点全绿，不冒回归风险)。**待 B/C 段跑完、确认是全局瓶颈后再定**(届时方向：单层段内只深拷当前层)。

### 下一步：B段（MT17 装备阈值决策）
- B段：入口 `tokens[:1462]` → 出口格 MT17(2,11)(出口 token≈1499)，主题**装备阈值决策**(攻/防跨过怪攻·连击·坚固阈值致损血突变，属性边际价值阶梯状)。
- 验收沿用三条(合法到达 + 引擎裁判一致 + HP≥基准) + 出口 Pareto 前沿 + 严格更优查询。route 真值(基准 HP≈459、属性 / 持有钥匙 / 地图剩余)经 route 全程重放取(见 `diag_mvp_segments.py`)，写入 `mvp_b.py` cfg(口径同 `mvp_a.py`)。

---

## 🟢 solver 前置机制验证：神圣盾魔法免疫坐实（G5/G6/J13 解决）+ snow 持有守卫 + 单测——80 单测 + 46 检查点全绿（2026-06-05）

> 玩家裁定"先不开始 solver，两个机制必须先坐实+验证（solver 会探索 route 外路线，否则必错）"。本段把神圣盾免疫与 snow 坐实落盘+单测，零回归。详见 `mechanics_51.md §C.8 / §K.2`、`memory/mechanics_status.md`。

- **神圣盾 = 区域伤（魔法）免疫**（G5/G6/J13 解决）：神圣盾 = `shield5` = tile44，唯一在 **MT44(6,6)**（杀 (5,9)(7,9) 两 redGuard 开 specialDoor(6,8) 入中心拾）。引擎 itemEffect = `def+=100 + setFlag('魔法免疫',true)`；`flag:魔法免疫` 免疫**领域15/夹击16/阻击18/激光24/伏击27 全部区域伤**（updateCheckBlock 源码）。
  - **本塔"地形伤"= 巫师领域/夹击**，神圣盾免之。**血网(lavaNet) 是另一套(护符 amulet)，本塔全图无 lavaNet、无 amulet → 空置**；lava(tile5) 是 noPass 障碍不扣血，由 snow 清。tiles.json tile5 旧误导注释已订正。
  - **sim 早已就绪**（拾取 set_flags :1242 + 免疫 `_apply_zone_damage` 入口 :930），**无需改产品代码**。本 route token4527 拾盾（DEF 204→304）；**持盾后大量过区域伤格被 checkpoint 4582/4723/5833/6066… 重度隐性验证**（非"无真值"）。单测 `tests/test_holy_shield.py`（6 测）。
- **snow 持有守卫**（玩家裁定必须，solver 正确性）：`_use_snow` 加 `if items['snow']<=0: return`（与 centerFly 同款）——**没拿到 snow 就用=no-op**。snow 只清**四正方向**相邻 lava（玩家实测确认不清对角），cls=constants 不消耗可重复。route snow 首入背包 tok5907(MT35 屠龙)、5 次使用均持有 → 守卫不破坏回放。单测 `tests/test_snow.py`（6 测）。
- **solver 前置状态**：神圣盾/snow/floodfill 化简/死亡剪枝/区域伤/被动加成等**均就绪**；**唯一挂着的前置 = bomb×coin 交互**（见下一里程碑【下一阶段】§，solver 若用 bomb+coin 组合策略前必须回引擎 afterBattle 源码坐实）。

---

## 🟢🎉 模拟器阶段完成：MT2 商人 3% 祝福 + MT49 削弱链/屠龙刀 + 圣水 switch 分支 + win 软终止——46/46 检查点全 PASS，token0→6353 通关全程逐 token 复现，终态 won=True HP=14382（2026-06-05，commit d026c80）

### 【里程碑：模拟器阶段完成】

- **忠实复现真实通关全程**：**6354 token（索引 0→6353）、51 层（MT0–MT50）、全部机制**，**46 检查点 + 68 单测全绿**。终态 **won=True、HP=14382**——这是**本 route 的成绩基准**，即 solver 要超越的数字。
- **双终止态**（solver 的失败/成功判定接口，二者置位后 `step()` 一切 token 冻结 no-op）：
  - `dead`（**死亡硬终止**）：HP≤0 即 game over，冻结于死亡点。
  - `won`（**通关软终止**）：杀 MT50(6,5) 魔王 → `afterBattle["6,5"]` 榜单 while 经 break 跳出 → 续跑循环外 `type:win` → won=True。此刻 `hero.hp` = 最优化目标值。
- **本段消掉最后 FAIL 的根因**（铁律：玩家裁定，非凑绿）：
  1. **圣水 tok6340 no-op（switch 分支）**：`_eval_single` 缺 `switch:` 读分支 → MT16 老人二段事件 `if switch:A` 恒 false → 永不进发圣水的 choices 支 → superPotion 未入包。补分支后 tok6340 HP **+6510**（=round(0.74×(507+373))×10）。
  2. **win 永不触发（break 吞 while 后指令）**：`afterBattle["6,5"]` = `[…, while{榜单choices}, type:win]`，tok6353=CHOICE:0 选"生命值"含 break 跳出 while。旧 CHOICE 恢复码把 break 当"丢弃整个 remaining"，连 type:win 一起吞掉 → won 永不置位。修复：break 时若 remaining 以 `_while_continue` 哨兵打头则只丢该哨兵、清 break、续跑循环【外】指令（商店 while 后无指令，对其 no-op）。
  3. **MT2 商人 3% 祝福（Math.round）**：`_eval_value_expr` 未翻译 JS `Math.round/floor/ceil` → eval 异常返回 0（祝福 +0）。补 Math.* → `_mround/_mfloor/_mceil`。tok6222 ATK 492→507、DEF 362→373。
  4. **锚点订正 tok6221→tok6222**（玩家 2026-06-05 主动裁定）：玩家记锚点时读的是接受祝福【后】属性 507/373；游戏内撞击/选择不直接反映在 token 记录，实际对应 sim CHOICE 生效那步。
- **MT50 魔王战算账**（终态 HP 来源）：setEnemy redKing hp5000/atk1580/def190；勇者 ATK507 vs 防190 → 每刀 317、⌈5000/317⌉=16 刀；受 boss ATK1580 vs 防373=1207/刀×(16−1)=**18105** → 32487−18105=**14382**。boss 金 500×幸运币 2 → +1000。

### 【下一阶段：求解器】

solver 设计见 `docs/solver-design.md`。已积累的 solver 相关资产：

- **自动模式 floodfill**（`_auto_floodfill`）= 天然**状态化简器**：可达零伤非事件怪自动秒杀 + 可达道具自动拾取，迭代到不动点（门挡=不可达，不穿门）。
- **死亡硬终止**（dead）= **剪枝**接口：探索"会死路线"时即时砍掉（普通战斗 damage≥hp 由 canBattle 拦截、永不致死；能致死的只有 force/事件/地形）。
- **centerFly 绕后清场** = MT40 零伤打法范式（瞬移锚点先清红门上 13 格再踩触发格，全程零损）。
- **被动加成**（非线性属性阈值来源之一）：coin（金币×2）、knife 屠龙刀（对 magicDragon ATK×2）、cross（对僵尸/吸血鬼 ATK×2）。
- **⚠ bomb×coin 交互待源码坐实 = solver 前置最后一个挂着的待确认**：bomb 炸杀金币当前**不**乘 coin×2（走 removeBlock 非 `_enemy_gold`），引擎 afterBattle 源码未抓取——**solver 若要用 bomb+coin 组合策略，启动前必须回 `core.useBomb`/炸弹结算 afterBattle 源码坐实**（mechanics §J16）。神圣盾/snow 已于上方里程碑解决，此为前置清单仅剩项。
- **N-b（真结局口径）不在求解范围**：按假结局(NE)对齐，通关 = 杀 MT50 boss 即终止。

---

## 🟢 提取 MT0 地下室 + 幸运金币×2 / bigKey 全开黄门 / 撞假墙 reveal 链（G7 解决）/ 圣水：token4723/4925 转绿，35/36 检查点 PASS（2026-06-05，commit 1377688）

- **进展**：**35/36 PASS**，**对齐至 token4925**（MT0 已通、coin 幸运金币已拾、reveal 链 / G7 已解决），**全部道具机制实现完毕**。本段消掉 tok4723/4925 两个 FAIL：
  1. **MT0 地下室落盘**：`data/games51/floors/MT0.json`（玩家 2026-06-04 引擎抓取）。地下室全空：(1,1)=87 上楼→MT1、(6,6)=53 coin，`events / afterBattle / firstArrive / autoEvent` 全空 = **无自定义事件需建模**；仅 downFly（从 MT1）可进、(1,1) 楼梯回 MT1。
  2. **幸运金币 ×2**：coin（tile 53，MT0(6,6) 拾）= 被动，持有后全程击杀金币翻倍，门控在 `_enemy_gold`（`hero.items.get("coin")>0`）。
  3. **bigKey 大黄门钥匙全开黄门**、**圣水（`item:` 前缀修复）**：随机制落 `mechanics_51.md`。
  4. **撞假墙 reveal 链（G7 解决）**——MT41 (10,2) 隐藏怪触发链（源码坐实，详见 `mechanics_51.md` §G7，**已实现**）：① 杀**左侧** (2,2) redWizard → `afterBattle["2,2"]` 设 `flag:41=1`；② 英雄到**右侧** (9,2)、`hasVisitedFloor('MT42')`、按 R 撞 (10,2)=330 假墙 → `events["10,2"]` 条件 `flag41==1 && status:x===9 && status:y===2 && visited(MT42)` 为真 → `setBlock 220 destruct` 放出 redWizard、`flag41=2`；③ 杀该怪 → `afterBattle["10,2"]` 封 (5,6)(6,6)(7,6)=yellowWall、开 (5,7)(7,7)、(6,5) 放 **downFly**、(7,1)=yellowWall、自毁。④ 英雄拾 downFly（tok4916）→ downFly 下飞 MT0（tok4921）→ 拾 coin → 回 MT1。**sim 改动**：撞 noPass/墙/特殊门时先按条件求值该格 `events`，成立则触发（英雄当步不移入）；setBlock 无 loc 默认事件自身格、放实体时清底层地形（假墙 330→0 怪才可被战斗走入）；条件求值器补 `===/!==` / `status:x,y` / `core.hasVisitedFloor`。**锚点全中**：tok4858 英雄已在 MT41(10,2)、tok4916 downFly=1。**零回归**：68 测试 + 33 检查点全 PASS。
- **唯一 FAIL — token5156**：真值 `MT?(9,6) HP=5193 ATK=217 DEF=314 黄=17 蓝=1 金=1768`（金 1768 = 幸运金币 ×2 的 oracle）；sim 停 `MT16(9,11) 金=1724`（**pos (9,11) vs (9,6)、金 1724 vs 1768 差 −44**；HP/ATK/DEF/黄/蓝/coin 全吻合）。窗口 **token[4925→5156]**，分叉在 **MT0 回 MT1 之后某处**；金差 −44 = coin×2 生效后某场战斗的金币差或漏打一只小怪，**逐笔账待玩家裁定**。
- **终局剩余**：**token5156→6360**——**pickaxe@5391**、**help 键@5315–26**（no-op）、**centerFly@6354**、**圣水@6346**、**MT26 公主设 flag → MT24 传送 MT50 → MT49 竞技场（/10 魔王）→ MT50 杀 boss = win**（杀死即重放终止）。

---

## 🟢 实现 earthquake/bomb/pickaxe/upFly-downFly + KEY 派发表 + MT44 隐藏层单向楼梯：token4417/4504/4528/4582 转绿，32/32 检查点全 PASS，对齐至 token4582(MT47)（2026-06-04，commit 1e84590）

- **进展**：**32/32 检查点全 PASS**，**模拟器逐 token 对齐至 token4582**，**全塔 50 层数据齐备**，**全部道具（snow / centerFly / earthquake / bomb / pickaxe / 飞翼 upFly-downFly）已实现**。本段把 KEY 快捷键体系坐实并补齐最后几个道具，消掉 token4417/4504/4528/4582 四个 FAIL：
  1. **KEY 派发表坐实（引擎硬编码 `onKeyUp`，非存档配置）**：49→pickaxe（破墙镐，无备选）、50→bomb（否则 hammer）、51→centerFly（瞬移面板）、52→[icePickaxe,freezeBadge,earthquake,upFly,downFly,…] 首个可用。50/52 两行与玩家实测（bomb/earthquake）吻合 → 表权威 → 49=pickaxe 一并坐实。绑定落 `data/games51/replay_keybindings.json`（`{"52":"earthquake","50":"bomb","49":"pickaxe"}`），sim 按表派发不硬编码。`decode_route.py` 补 `K<keyCode>`（无冒号）→ `KEY:<keycode>` 解析。
  2. **earthquake / bomb / pickaxe / upFly / downFly 五道具实现**：earthquake（§L.6，键'4'，清当前层全图 canBreak 墙 tile1/2，**这是 token4417 根因**——破 MT37 内墙后拾齐 11黄2蓝1红/ATK+16/DEF+16）；**bomb**（§I.7，键'2'，炸英雄四方向相邻 hp<500 的怪，**走 afterBattle 路径**与战斗死亡同——MT44(6,9) 一发炸 (5,9)(7,9) 两 redGuard→openDoor(6,8)→上取 redPotion×?/shield5，**这是 token4528 根因**）；**pickaxe**（§L.5，键'1'，破四方向相邻 canBreak 墙，earthquake 的相邻版）；**upFly/downFly**（§I.7，飞翼，唯一进入 MT44 隐藏层的手段）。
  3. **MT44 隐藏层单向楼梯**：`isHide:true` → `canFlyTo=false / canFlyFrom=true`，MT43↔MT45 楼梯直连跳过 MT44，MT44 楼梯只能**出**不能**进**，唯一入口 = upFly/downFly。修 `_copy_state` 漏传 `is_hide`（每步被重置 False）的根因 bug。
  4. **真值订正两处**（玩家主动裁定，非凑绿）：token4417 (2,4)→(2,3)、**token4582 层 MT44→MT47**（route 末两跳 FLOOR 显式飞 MT47 + 坐标/HP/属性/钥匙全吻合为证）。
- **剩余**：**终局段 token4582→6360（约 1800 token）无真值覆盖**。已知路上有：**tok5131 踩章鱼区**、**tok5315–5326 八个 help 键（no-op）**、**tok5391 KEY:49 用 pickaxe**、**tok6354 第三次 centerFly**、**MT26 公主设 flag**、**MT24 传送 MT50**、**MT49 竞技场（/10 魔王）**、**MT50 终局 boss（杀死即 win = 重放终止）**。**N-b（真假结局口径）按假结局对齐**。
- **下一步**：玩家提供终局段真值，对齐到通关。

---

## 🟢 扩层最后一批 MT42–MT50 + 四种地形伤 + MT44 隐藏层跳层 + MT31 商人：token4222/4350 转绿，28/32 检查点 PASS（2026-06-04，commit 7696385）

- **进展**：**28/32 检查点 PASS**，**MT1–MT47 段逐 token 推进中**，**全塔 50 层数据已提取完毕（各 0 处不一致）**。本段把最后九层（MT42–MT50）落盘并实现四类地形伤 + 两个跨层/商人机制，消掉 token4222/4350 两个 FAIL：
  1. **MT42–MT50 九层提取落盘**（raw capture + clean JSON）；附带 **tile 4 订正为 star**、**§N 终局 boss 链**、**whiteKing 核实订正**。全塔 50 层至此提取完毕。
  2. **四种地形伤实现**：魔法警卫**夹击**(special 16)、巫师**领域**(15)、**阻击**(18) 按格损血，外加既有血网/毒地，统一走 `_apply_zone_damage`。
  3. **MT44 隐藏层跳层**（数据驱动，非硬编码）：`isHide:true` → 楼梯 `:next/:before` 解析对隐藏层透明（`_resolve_floor_id` 跳过），**MT43↔MT45 楼梯直连**，MT44 只能用 upFly/downFly 进入（当前 sim 未建模，为后续待办）。新增 `FloorState.is_hide` 字段从 JSON `isHide` 读入。
  4. **MT31 商人落盘**（消 token4350 的 黄−2蓝−1 真因）：`shops.json` 补 MT31(1,11) 4黄+1蓝/1000金条目（之前只在 _note、catalog=None 早退致购买空转）；核实 `_handle_trader` 的 CHOICE:1 状态机正确（不置 flag、商人留存、可反复对话）、多件 give 循环正确（之前只验过 MT6 单件）。回归 68 测试 + 既有商人检查点零波及。
- **下一步**：从首个 FAIL token4417 起逐笔账（见下方剩余检查点 + ledger）。

---

## 🟡 首个 FAIL = token4417（下个会话起点）+ 剩余检查点 4504/4528/4582

- **首个 FAIL — token4417**：真值 `MT37(2,4) HP=1479 ATK=202 DEF=154 黄=11 蓝=2 红=1`，sim 停在 `MT38(2,1) HP=1479 ATK=186 DEF=138 黄=2 蓝=0 红=0`——**HP 吻合，但 ATK/DEF 各缺 16、黄缺 9 蓝缺 2 红缺 1**（大量道具/属性没拿到，且落错层）。窗口 **token[4350→4417] 逐笔全状态账**已写入 `checkpoint_ledger.txt`。
  - 窗口关键 Δ：tok4352 `ITEM:50` centerFly、tok4353 蓝1→0、tok4360 `CHOICE:0` 金−4000 +earthquake（**MT47 商人买地震卷轴已生效**）、tok4367 `L`→MT48、tok4368 `FLOOR:MT37`、tok4396 `U`→MT38，随后 **tok4396 起 tok4397–4416 在 MT37↔MT38 之间 L/U 楼梯横跳**（疑似机关，sim 当作普通楼梯反复弹）。
  - **玩家预告该段含三项已登记待确认**（铁律：不自行猜根因/改真值/改断言凑绿，待源码坐实）：
    - **J13 神圣盾免疫地形伤**（`docs/mechanics_51.md` §J）：神圣盾对应哪个 flag、获取楼层/条件待坐实；当前未实现免疫。
    - **J14 MT43 移动的魔法警卫**：whiteKing 是否每步/每回合移动、移动规则待坐实；sim 当前按静态怪处理，若实为移动怪则夹击触发格随之变。
    - ~~**J15 MT48 地震卷轴破 MT37 墙**~~ → ✅ **已解决（2026-06-04）**：商人在 **MT47(5,2)**（**非 MT48**，原笔误已订正 handoff/mechanics，shops.json 本就对）。tok4369 `KEY:52`（键'4'）= 使用 earthquake（玩家实测坐实，推翻旧推测"K52=upFly"），效果 = 清**当前层全图**所有 canBreak 墙（tile 1/2），不跨层。实现见 §L.6 + `_use_earthquake`。**这正是 token4417 根因**：破墙后 MT37 内部连通，tok4370–4417 拾齐 11黄2蓝1红/ATK+16/DEF+16，**token4417 属性全吻合**（旧"MT37↔MT38 横跳"消失，改为正常遍历内部）。残留仅终点坐标 sim(2,3) vs 真值(2,4) 差一步，待裁定（见下）。
- **剩余检查点（3 个，4417 之后）**：token **4504 / 4528 / 4582**，集中在 **DEF 暴涨段 134→204→309**（疑 shield5 / 大盾拾取）：
  ```
  token4504: MT43 (9,4)   HP=123  ATK=202 DEF=204  黄=5 蓝=2
  token4528: MT44 (6,5)   HP=623  ATK=202 DEF=304  黄=5 蓝=2
  token4582: MT44 (11,2)  HP=4723 ATK=202 DEF=309  黄=5 蓝=2
  ```
  注：token4528/4582 在 **MT44 隐藏层**内（验证 upFly/downFly 进入 + 层内战斗/拾取），token4504 HP=123 是极限态（损血精度验金石）。

---

## 🟢 MT40 boss存活才打(§M.7) + 死亡硬终止(§M.8) + changeFloor stair 落点：token4141 转绿，26/26 全 PASS，MT1–MT41 全程对齐（2026-06-04，commit eb1470e）

- **进展**：**26/26 检查点全 PASS**，**MT1–MT41 全程逐 token 对齐**，**68/68 测试全绿**（原 64 + 新增 4）。本段三件事，消掉最后一个 FAIL（4141）：
  1. **MT40 boss「存活才打」(§M.7)**：核对 events["6,7"] 内原始 JS（`extract/mt40_raw_capture.json`）——`for i=0..12: if core.getBlockId(a[i])!==null { move+battle }`，红门以上 13 格只对**还活着**的怪逐个先攻强制战斗，**已清格 getBlockId===null 跳过、零伤**。修：`MT40.json` 13 场 battle 各带 `loc:[x,y]`（顺序即源码 a 数组）；`simulator` battle 分支带 loc 走存活判断（entities==0 跳过、非 0 取该格怪 force 战斗后清格），**无 loc 保持无条件（MT32 不受波及）**。本路线 centerFly 瞬移 (2,1) 后先清光 13 格 → 踩 (6,7) 一场不打、HP 恒 262（吻合真值）。**纠正上一段误判**：旧 MT40.json 把 13 场展开为无条件 battle、丢了 getBlockId 存活判断——这才是 4141 HP 崖真因（非上段所列 (a)(b)(c)）。
  2. **死亡硬终止 (§M.8)**：`GameState.dead`，`step()` 入口 dead 则一切 token no-op（冻结死亡点）；任何 hp≤0 结算（强制战斗 / 事件扣血 setValue / step 末尾兜底）置 dead；强制战斗序列中途致死 → `_execute_event_list` 立即停剩余指令。重放路线不触发，**为 solver 探索"会死路线"准备**（普通战斗 damage≥hp 拦截不变、永不致死，能死的只有 force/事件/地形）。
  3. **changeFloor 支持 stair 字段落点（G7 解决）**：事件驱动的 `changeFloor` 原只认 `loc`、缺省 (0,0)；补上**有 loc 用 loc、无 loc 有 stair 查目标层 down_floor/up_floor、都无则报错**，与 fly 魔杖 / `_apply_stair_change` 同一套解析。MT40 events["6,1"] 的 `changeFloor :next stair:downFloor` → 落 MT41.downFloor=[6,2] → **token4141 转绿**。
- **新增 4 测试**（`test_force_battle_mt32.py`）：§M.7 全清零伤 / 部分清场只打存活；§M.8 死亡 token no-op 冻结 / 死亡中途停战。
- **G7（MT41 跨层机关）已解决**（= changeFloor stair 解析），从待确认移除。
- **下一步**：**MT42–MT50 待提取**（raw capture + 落盘 `data/`）；**营救公主终局待验证**（通关条件 + 终局事件）。MT1–MT41 已是逐 token 金标准基线。

---

## 🟢 多格boss footprint + setBlock跨层 + getBlock条件 + centerFly：token3371/3704/4012 转绿（2026-06-04，25/26 检查点 PASS，commit c33934d）

- **进展**：**25/26 检查点 PASS**，**MT1–MT40 基本逐 token 对齐**，**64/64 测试全绿**。本段实现四件事，一次性消掉级联 FAIL 中的三个（3371/3704/4012）：
  1. **多格 boss footprint（魔龙/章鱼九宫格占位）**：`monsters.json` 给 magicDragon(257)/octopus(258) 声明 `footprint`（3×3，锚点=怪实体格向上展开两行），活着时九格全 noPass，只能站锚点正下方朝上触发战斗；afterBattle `hide remove` 清占位（配套清 terrain 层）。`tiles.json` 把 17 订正回 `airwall`（noPass:true 保留）。→ **token3371/3704 转绿**。
  2. **setBlock 跨层**：`setBlock` 支持 `floorId` 指向非当前层（按需加载目标层），数字 / 字符串 number 两种写法（字符串走 `_id_to_tile_full` 解析），区分 entity / terrain 层落子。
  3. **getBlock 裸条件分支（口径已坐实）**：`_eval_single` 新增 `core.getBlock(x,y) ===/!== null`（区别于 getBlockId）。口径：blockObjs 收录所有 tile≠0 的格（墙/门/楼梯/地形/装饰/怪/道具），tile==0→null；双层模型映射为 **entity≠0 或 terrain≠0 即非 null**。用于 MT39 `autoEvent[8,4]` 九宫格条件（开两扇黄门后 (4,4) 黄门→`openDoor`+`setBlock centerFly3`）。
  4. **centerFly（ITEM:50 瞬移，消耗性已坐实）**：中心对称瞬移到 (W-1-x, H-1-y)，**不切层**；canUseItemEffect 校验对称点 `getBlockId ∈ {null,'none','airwall'}`（airwall 可落——与移动 noPass 是两套判定）；消耗性：`items.json` centerFly `cls=tools`，`_afterUseItem` 源码 tools 用即 −1（§K.2），故"配 3 用 3"是消耗必然、非巧合。centerFly3(331) 拾取给 3 个 centerFly。→ 勇者 tok3925 拿 centerFly3、tok4085 瞬移进 MT40 段、黄钥匙不多耗 → **token4012 转绿**。
- **唯一 FAIL — token4141**（下个会话起点）：真值 `MT41(6,2) HP=262 ATK=182 DEF=134 黄=7`，sim 停在 `MT40(7,9) HP=1246 黄=4`（连楼层都没进对）。新窗口 **token[4012→4141]（129 步）逐笔全状态账**已写入 `checkpoint_ledger.txt`。**三个待玩家裁定材料**（铁律：不自行猜根因 / 改真值 / 改断言凑绿）：
  - **(a) tok4075 `L` @ MT36(1,11) HP+800**：这 +800 是什么？（祭坛 / 祝福 / 血瓶？还是误算）
  - **(b) tok4084–4095 MT40↔MT39 往返**：`FLOOR:MT40` 后又 R 到 MT39(11,10)、再 D 回 MT40——这段楼梯 / 边界相邻行为 sim 是否建模对了？
  - **(c) 真值黄钥匙多 3 把（真值 7 vs sim 4）**：真实路线在 sim 未到达的格拿了 3 把黄钥匙 → sim 路线在窗口内提前分叉，没走到那些格。
- **G7（MT41 跨层机关 `events[10,2]`，setBlock destruct 语义 + hasVisitedFloor 条件）仍挂待确认**（详见 memory/mechanics_status.md）。token4141 要进 MT41，**大概率与 G7 相关**，下个会话处理。

---

## 🟢 扩层 MT34–MT41 + token2965 转绿（2026-06-04，22/26 检查点 PASS，commit 533c2e2）

- **进展**：**22/26 检查点 PASS**，**MT1–MT33 全程逐 token 对齐 + MT34–MT41 八层已提取**，**64/64 测试全绿**。本段三件事：扩层 MT34–MT41（含原始 raw capture 落盘）、实现 snow 冰魔法（§K）、补 MT40 骑士队长 boss 13 连战测试（§M）；核心是**修正 flower(168) 误标 noPass**。
- **token2965 根因 = flower(168) 提取误标 noPass，非"踩格机关"**（玩家原假设两处踩格事件，依铁律核对源码后报告差异）：
  - **机关二（踩(10,5)→关(10,4)/(10,8)机关门）源码里存在且 sim 早已正确实现**：`MT33.events["10,5"]`（setBlock specialDoor + flag:骑士剑机关 + getBlockCls 四角怪判断）配 `autoEvent["11,4"]` 开门半。
  - **机关一（踩(9,10)→(8,10)变墙）源码里查无此事件**（clean JSON、live raw capture、firstArrive/parallelDo/afterBattle 全空）。真因：`extract/blocksInfo_full.json['168']={id:flower,cls:terrains}` **无 noPass 字段** → h5mota terrains 默认可通行；旧 tiles.json 误标 `noPass:true` 把 (8,10) 当墙。
  - **玩家实测路线坐实**：tok2939–2942 自 (7,10) 连按 R 向右穿 (8,10)→(9,10)→(10,10) 取 sword3(+40 ATK)，sim 误判撞墙 → ATK 卡 114(真值 154，差正好 40)。
  - **修正** `tiles.json` 168 `noPass:false`（来源订正 blocksInfo + 路线印证）→ token2965 精确命中 `MT33(8,3) HP=6 ATK=154`，级联 token3212 同时转绿。（玩家所述"变墙"那条回退路最优路线未走，sim 不杜撰源码无据机制。）
- **当前 FAIL（4 个，级联自 3371）**：token **3371 / 3704 / 4012 / 4141**。
  - **首个 FAIL = token3371 MT32(1,4)：仅 HP sim=1006 vs 真值=606（差 +400），ATK/DEF/坐标/黄钥匙全吻合**——即 sim 在 [3212→3371] 这 159 步**少挨 400 HP**（与原 ATK 缺口无关，是另一回事）。
  - **首个 FAIL 窗口 token[3212→3371] 已逐笔摆账**（`checkpoint_ledger.txt`，跑分器自动生成）。HP 收支净 +100（906→1006），真值要 606。**三个待玩家裁定的可疑点**：
    - **(a) tok3253 `CHOICE:0` @ MT2(11,5)**：sim 只判"金币+1000"。这是什么选项事件？是否还应扣 400 HP 或别的代价？（语义未确认）
    - **(b) tok3301 蓝血瓶 @ MT35(5,5)**：sim 给 +800（base200×ratio4，数据模型自洽）。真实回血是 +800 还是 +400？
    - **(c) 五场战斗**（tok3228/3230/3333/3365/3367，损血 −220/−220/−260/−140/−260）某场是否带未建模 special 而少算损血。
  - **G7（MT41 跨层机关 `events[10,2]`）继续挂待确认**（详见 memory/mechanics_status.md），**token4141 依赖它**，按玩家指示先不动。
- **下个会话**：玩家裁定上述 (a)(b)(c) 三个可疑点后，据此定位并修复 token3371（铁律：不自行猜根因/改真值/改断言凑绿），随后看 3704/4012 级联，4141 待 G7 裁定。

---

## 🔵 扩层第三批 MT29–MT33（2026-06-04，20/21 检查点 PASS，唯 token2965 待查）

> ⚠ **本段 token2965 已于上方 🟢 段解决（根因 flower(168) 误标 noPass，commit 533c2e2）。以下保留作历史。**


- **进展**：**20/21 检查点 PASS，MT1–MT33 基本对齐**（差 token2965）。61/61 测试全绿，17 个 pytest 检查点 + token2400/2501/2804 端点全 PASS（commit 50af961）。
- **本段修复**（commit 50af961）：
  1. **删除「同步 move 后对话→拦截」死规则**：源码坐实纯文字对话 `\t[name]text` 在回放中一律不拦截（引擎 replayActions 无文字处理器，文字非 token 类型，仅 choices 读取选择 token）。该规则当年为 MT10 小偷事件所加，但 **MT10 自 f28ceab 起已不依赖它**（小偷事件靠 move+hide 独立成立），全塔扫描仅误伤 MT32 boss 演出（`extract/scan_sync_dialog.py` 证实仅 2 处，均 MT32）。删除后 token2501 的 HP/楼层/位置/DEF/黄钥匙全部对齐。
  2. **祭坛购买补金币守卫**：`data/games51/common_events.json` 商店三档购买（HP/ATK/DEF）各包一层 `if status:money >= flag:money1`——**钱不够→不成交、不扣钱、不加属性、times1 不变，但 CHOICE token 仍被消化**（玩家连按 7 次买攻击，第 4 次起金币 <440 被引擎拒绝）。修前无条件成交致 ATK 多加 +32（4 次）。修后 token2501 ATK=102、gold=401 逐项吻合。（times1 跨层累计经核对正确，入 MT32 时 =3，非 bug。）
- **§G.7.3 标注**：`docs/mechanics_51.md` §G.7.3 旧时间线已与现状不符，已标注「已过时，以 §M 和当前实现为准」（只加标注，未删原文）。
- **唯一 FAIL — token2965**（下个会话起点）：真值 `MT33(8,3) HP=6 ATK=154 DEF=70 黄2`，sim 出 `MT33(5,9) HP=226 ATK=114`。token2804(854/112) → 2965(6/154) 之间 **HP-848、ATK+42**——这段有恶战 + 大量加攻（宝石/祭坛?），新分叉待查。**HP=6 是极限态，意味着这一段每场战斗损血必须一字不差，是损血精度的验金石**。下个会话需用玩家中间真值夹逼定位第一处分叉。

---

## ✅ MT1–MT28 全程逐 token 对齐（2026-06-03，18/18 检查点全 PASS）

扩层第二批 MT21–MT28 已彻底对齐：**token2400 转绿（MT28(2,11) 1261/78/64 5黄），MT1–MT28 全程逐 token 忠实复现，54/54 测试全绿**（commit 95e1b4f）。第一批 MT15–MT20（tok[2000] 转绿，commit b11f9c2）见 §八(0)；第二批根因/发现见 §八(0.5)。进度详见 §一。

> 检查点口径：17 个在 `tests/test_checkpoints.py`（tok[100..2000]，pytest 套件内）；第 18 个 token2400 由 `extract/verify_tok2400.py` 独立验证（端点 MT28）。合计 **18/18** 金标准锚点全绿。

> 历史（已解决）：上一阶段曾长期卡在「模拟器重放未在 rle[69] 触发 MT3(5,9) 伏击」。根因是 MT1 早期路径走错（非伏击未发生）。现已修复——tok[100] 即为伏击后基线 ATK=10/DEF=10（正确触发），MT3 伏击机制详见 §二②。

### ⚠ 永久铁律（新会话必须遵守，禁止跳过）

1. **禁止**用模拟器输出推翻 `docs/mechanics_51.md §H` 或 `memory/project_overview.md` 里的既定事实。
2. **禁止**修改测试断言来"凑绿"——断言失败说明模拟器有 bug，应修模拟器。
3. **改任何文档（§H、memory）或测试断言之前，必须先得到人工确认。**
4. 模拟器结果与玩家实测矛盾时，结论只能是"模拟器有 bug"，永远不能是"事实有误"。

---

## 一、当前进度

| 模块 | 状态 | 关键 commit |
|------|------|------------|
| 战斗引擎（含 special 全套） | ✅ 已验证（oracle 比对） | 8afbd69 |
| MT10 单层重放真绿 | ✅ 全 148 token 逐格对齐 | b3b12fa |
| 机制文档 §A–§I（含飞行系统全貌） | ✅ 全部落盘 | 见最新 commit |
| 机制侦察阶段封板 | ✅ 完成 | 见最新 commit |
| 跨层模拟器 MT1–MT14 | ✅ 13/13 检查点全PASS，24/24测试全绿 | f28ceab |
| 商店/祭坛购买（insert/while/break/CHOICE） | ✅ 通用实现，MT12祭坛+商店验证 | f28ceab |
| MT12/MT13/MT14 楼层数据 | ✅ 已提取校验（各0处不一致） | f28ceab |
| **跨层模拟器 MT1–MT20（扩层第一批）** | ✅ **17/17 检查点全PASS，54/54 测试全绿，MT1–MT20 逐 token 对齐** | **b11f9c2** |
| 老人对话事件 + 打不过原地不动(canBattle) | ✅ 通用实现，tok[2000] 转绿 | b11f9c2 |
| 商人(trader)≠祭坛(blueShop)、cross双倍攻、祭坛格noPass | ✅ 见 §八(0) 根因 | e7f20d0 / 75687ea / b11f9c2 |
| **跨层模拟器 MT1–MT28（扩层第二批）** | ✅ **18/18 检查点全PASS，54/54 测试全绿，MT1–MT28 逐 token 对齐** | **95e1b4f** |
| MT14红钥匙(三僵尸autoEvent/getBlockCls)、MT28钥匙回收商人、MT26公主改写MT24、tiles.json修怪名 | ✅ 见 §八(0.5) 根因 | 95e1b4f |
| **跨层模拟器 MT1–MT33（扩层第三批）** | 🔵 **20/21 检查点 PASS（差 token2965），MT1–MT33 基本对齐** | **50af961** |
| 删除「同步move后对话拦截」死规则 + 祭坛购买金币守卫 | ✅ 见顶部🔵段 | 50af961 |

**路由起始状态**（来源：`data/games51/hero_init.json`，引擎直读）：

```
楼层：MT1  位置：(6, 11)  朝向：down
HP=1000  ATK=100  DEF=100  mdef=0  gold=0
持有道具：fly×1
flag：nowWeapon=sword5, nowShield=shield5, 魔法免疫=true
```

注：ATK=100 已含 sword5 buff，DEF=100 已含 shield5 buff；初始即有 fly魔杖（startText 给的）。

---

## 二、关键事实速查（新会话最易踩坑）

### ① 切层方式分类（共四类）

| 类型 | 路由表现 | 本存档计数 | 备注 |
|------|---------|-----------|------|
| **fly魔杖** | `fly:MTn` → `FMTn:` → `FLOOR:MTn` | **220 次** | 绝对主路径 |
| **走楼梯** | 无 token，UDLR 走到楼梯格自动触发 | 极少（需模拟器统计） | 包含 MT1→MT2 首次跨层 |
| **upFly/downFly** | `item:upFly`/`item:downFly` → `ITEM:{n}` | 若有则在 ITEM token 中 | 不产生 FLOOR token |
| **事件 changeFloor** | 无任何 token | 至少 2 次（MT3伏击→MT2；MT24事件→MT50） | 不产生任何 token |

`floor_transitions.json` 的 `type` 字段（"changeFloor"/"centerFly"/"keyboard_fly"）**全部错误**，不可信。

**"无 FMT token ≠ 走楼梯"**：无 FMT token 意味着"未被 fly魔杖 飞入"，可能是走楼梯、upFly/downFly（ITEM token）、事件 changeFloor（无 token）或根本未访问。禁止用排除法默认"走楼梯"。

### ② MT3 伏击（强制必经，永久改变基线）

玩家进入 MT3 并踩到 (5,9) 时触发伏击事件，**无法绕过**（(5,9) 是 MT3 唯一上行路径）：

```
setValue: hp=400, atk=10, def=10
setValue: flag:nowWeapon = null   （卸装，sword5 永久失去）
setValue: flag:nowShield = null   （卸装，shield5 永久失去）
setValue: flag:魔法免疫 = false
hide loc=(5,9) remove=true        （触发格变空地，不再触发）
changeFloor → MT2 (3,8)           ← 目标是 MT2，不是 MT1
```

**伏击后永久基线**：HP=400, ATK=10, DEF=10（无装备），魔法免疫消失。  
伏击前积累的 ATK/DEF 宝石全部清零（钥匙/金币不受影响）。

**模拟器必须实现**：`_execute_instruction()` 需支持 `changeFloor` 指令类型；`_set_value()` 需支持 `"null"` → Python `None`。

### ③ 双层地图（terrain + entity）

每个楼层有两张独立的 13×13 矩阵：

- `terrain[y][x]`：地形 tile（墙=1, 地板=0, 楼梯/毒地等各有 tile ID），静态
- `entities[y][x]`：实体（怪物/门/道具/机关门等），动态，战斗/事件后清零

`step()` 判断可通行：`terrain[ny][nx] != 1 且 entities[ny][nx] 不阻挡`。

### ④ 拦截型事件（intercepting events）

同步 move/generateMove 之后紧跟字符串对话 → 事件暂停，等待 CHOICE token 推进；英雄坐标不更新。事件脚本内的 `changeFloor` 指令触发后须立即设置 `pending_floor_change` 并停止当前事件执行。

### ⑤ 飞行道具规则（详见 §I）

| 道具 | 换层？ | 落点 | canFlyTo/From 生效？ | 实际限制 |
|------|-------|------|---------------------|---------|
| fly魔杖 | ✅ | 向上飞→目标层 downFloor；向下飞→目标层 upFloor | ✅ 均检查 | gate1+gate2；canFlyTo=false 层不可飞入（MT0/MT44/MT50） |
| centerFly | ❌ 不换层 | (12-x, 12-y) 对称格 | — | 对称格须为空（getBlockId==null） |
| upFly | ✅ | 当前英雄坐标 | ❌ **不检查** | **硬编码 `index >= 49` 封顶**（从 MT49 起飞被拒） |
| downFly | ✅ | 当前英雄坐标 | ❌ **不检查** | 硬编码 `index < 1` 地板；目标格须为空 |

**upFly/downFly 可用范围**：只能在 floorIds 下标 1–48 之间的楼层起飞（即 MT1–MT48），MT0/MT49/MT50 均受限（MT0 被 index<1 挡，MT49/MT50 被 index>=49 挡）。

gate1（楼梯连通检查）：不要求站在楼梯旁，只要当前层存在可达楼梯格的无阻碍路径即可飞。

### ⑥ MT50 完整进出规则（特殊，封板确认）

**进入 MT50：唯一路径是 MT24(6,2) 事件 changeFloor**

触发条件：踩 MT24(6,2) 且 `flag:营救公主=true`  
落点：MT50(6,7)  
原因：fly魔杖（canFlyTo=false 被 flyTo 拒）、upFly（index=49≥49 被硬编码拒）、downFly（MT51 不存在）均不可达。

**离开 MT50：打通关后游戏自动结束**

MT50 内战胜最终 BOSS 后触发 `win` 事件，游戏结束——这是**全塔重放的终止条件**，不需要模拟器处理离场。MT50 内 `canFlyFrom=false`（fly魔杖被拒），upFly（index=50≥49 被拒）；downFly 不检查 canFlyFrom，理论上可用于测试，但正常通关流程不需要离场。

**MT50 的模拟器处理**：重放到 MT50 内 BOSS afterBattle 的 `win` 指令时，终止重放，记录最终状态。

---

## 三、跨层模拟器要解决的核心问题

### (a) 每层持久状态

38 个楼层被 fly魔杖 多次进入（MT31 最多 11 次）。每层的 `entities[y][x]` 必须跨访问持久化。  
`FloorState` 升级为 `dict[floorId, FloorState]`（全局 registry）+ `current_floor: str`。

### (b) 切层的四条路径（扩展自两条）

```
路径 A：走楼梯
  触发条件：UDLR 移动后目标格在 change_floor 字典中
  落点：change_floor[loc_key]["loc"] 坐标 + floorId
  实现：step() 设 pending_floor_change，外层循环完成切层

路径 B：fly魔杖（FLOOR:MTn token）
  触发条件：action 以 "FLOOR:" 开头
  落点：fromIndex≤toIndex → 目标层 downFloor；否则 upFloor
  实现：step() 内直接切层 + 更新 visited_floors

路径 C：事件 changeFloor 指令
  触发条件：事件脚本执行到 {"type":"changeFloor",...} 指令
  落点：指令内 loc 字段（如 MT3伏击→MT2(3,8)；MT24事件→MT50(6,7)）
  实现：_execute_instruction() 新增 changeFloor 分支，设 pending_floor_change

路径 D：upFly / downFly（ITEM:{n} token）
  触发条件：ITEM token 对应 upFly 或 downFly 道具
  落点：当前英雄坐标（不用 upFloor/downFloor 字段）
  目标层：floorIds[currentIndex ± 1]
  实现：step() 处理 ITEM token 时识别，直接切层
```

### (c) changeFloor 从"冻结"升级为"移交控制权"

楼梯/事件 changeFloor → 设 `pending_floor_change`（含目标 floorId+坐标）+ `_exited=True`，外层循环检测后切层，后续 token 在新层处理。fly魔杖/upFly/downFly → step() 内直接完成切层。

---

## 四、待办分类

### 不阻塞跨层开发——靠全程 route 重放自然解决

| 编号 | 问题 | 为何不阻塞 |
|------|------|-----------|
| J8 | `flag:fly` 的设置条件（gate1 开关） | 重放时 flag 状态由 route 事件自然更新；全程跑通后从状态快照读出 |
| J9 | ✅ **已解决**：`flag:营救公主` 在 MT26(6,6) 公主事件设置 | 详见 §八(0.5)；该事件还 setBlock 改写 MT24 第6列(6,2/6,3/6,4)打通——跨层动态建图点，待走 MT50 流程端到端验证 |
| J10 | MT21–MT23、MT27–MT30 是否实际访问 | 普通层无特殊机制；全程模拟器跑通后统计访问记录即可 |
| J11 | MT24(6,2) 事件可否重复触发 | 无 `hide/remove=true`，理论可重复；重放中若多次踩格则自然测试到 |

### 仍需查证（不紧急）

| 编号 | 问题 | 优先级 |
|------|------|--------|
| J5 | 护身符（amulet）获取楼层和条件 | 中 |
| J6 | 魔法免疫 flag 的其他设置条件（MT3 伏击已确认为移除路径） | 中 |
| J12 | MT50 实际离场坐标（正常通关不需要，仅调试用） | 低 |

---

## 五、目录结构速查

```
data/games51/
  hero_init.json        路由起始状态（引擎直读）
  floors/MT1–MT10.json  已提取（含 map/terrain/events/changeFloor）
  floors/MT11+          待提取（跨层开发第一步）
  monsters.json         怪物属性
  items.json            道具定义
  shops.json            商店/祭坛
  floor_transitions.json 220条切层记录（type字段全部错误，不可信）
  mt10_route_trace.json  MT10段路由（148 tokens，已验证）

docs/
  mechanics_51.md       §A–§I 机制全文（唯一事实来源）
  handoff.md            本文件
  solver-design.md      求解器设计待办

sim/
  simulator.py          GameState/HeroState/FloorState/step()（MT10已验证）

extract/
  gen_floors.py         提取楼层数据（需扩展到 MT11–MT50）
  mt24_raw_events.txt   MT24 events 原始数据（含 MT50 特殊传送脚本）
  mt49_mt50_raw.txt     MT49/MT50 连通结构原始数据
  upfly_downfly_src.txt upFly/downFly/fly魔杖 canUseItemEffect 源码对比
```

---

## 七、检查点回归状态（2026-06-03 更新，17/17 PASS ✅）

> 注：本节的 13 点指 tok[100..1300]，仍全 PASS。扩层新增 4 点 tok[1400/1500/1603/2000] **全部转绿**（commit e7f20d0、75687ea、b11f9c2）。**合计 17/17 PASS，MT1–MT20 全程逐 token 对齐**；本批根因与待办见 §八(0)。

### 已确认结论

#### (a) ⚠ 检查点口径：tokens[:N+1]，tokens[0] 为不计步的初始化事件

**这是之前所有"差一格/晚1步"位置偏差的总根因，已修正（2026-06-02）。**

`tokens[0] = CHOICE:1` 是初始化事件，不计入玩家步数。  
玩家的 tok[N] = 执行完第 N 步之后的状态 = 处理完 `tokens[0..N]`（共 N+1 个 token）。  
正确口径：`tokens[:N+1]`，**禁止改回 `tokens[:N]`**。

- `tokens[:N]` 每点少跑 1 步，坐标偏 1 格，但因相邻步通常无属性变化，大多数点碰巧 PASS 而遮盖偏差。
- 只有当第 N+1 步恰好跨越属性事件（如血瓶拾取）时才暴露（tok[500] 就是典型案例）。

验证：改为 `tokens[:N+1]` 后，原 8 个 PASS 点仍全部 PASS，tok[500] 从 FAIL 转 PASS。

#### (b) Route 记录撞墙 token（纠正旧假设）

旧假设（"route 不记录撞墙，撞墙不产生 token"）**已被推翻**。

实测：tok[259-263] 对应原始字符串 `R5`（5步向右），在 MT4 上因墙被模拟器处理为 BLOCKED，英雄位置/HP/钥匙全部无变化。这些 token 存在于解析列表中，模拟器正确处理为无操作，checkpoint 索引不受影响。

**结论**：route 会记录撞墙移动；模拟器 BLOCKED 处理正确；checkpoint 定位无误。

#### (c) 解析器 Bug：`M<n>:` 后数字被丢弃（已记录，暂不修）

**现象**：原始 route 中存在 `M<mapID>:<count>` 格式的 token（如 `M8:4U`、`M10:1R`）。  
解析器将 `M8:` 解析为 `UNKNOWN:M8:`，然后紧随的数字 `4` 因不是字母而落入 `else: i += 1`，**被静默丢弃**。

**影响范围**：
- 第一个丢弃字符在原始串 raw[1544]，对应 tok[2200]
- 共丢弃 149 个字符，全部在 tok[2200]..tok[6138] 区间
- 当前 13 个检查点（tok[100]..tok[1300]）**完全不受影响**

**待办**：`M<n>:<count>` 的语义待确认（可能是商店/飞行道具调用），修复解析器前需先理解其含义。当前优先级：低（不阻塞 tok[1300] 以内的回归）。

#### (d) MT10(6,11) 楼梯门 enable 机制（已修复，commit 335dfd8）

**根因**：`show` 指令在模拟器里是 no-op，导致 MT10(6,11) 的 `events["6,11"].enable` 永远不从 `false` 变为 `true`。同时 `_apply_stair_change` 不检查 `enable`，导致楼梯在 Visit4 之前（tok[946]、tok[992]）就错误触发，英雄过早进入 MT11。

**修复内容**（simulator.py，commit 335dfd8）：
1. `show` 指令实现：遍历 `loc` 列表，将 `events[loc_key]["enable"]` 置为 `True`。
2. `_apply_stair_change` 新增 enable 检查：若 `events[loc_key].get("enable") is False`，则返回 False（楼梯不触发）。

**连锁效果**：tok[1000]（floor 从 MT11→MT10）、tok[1100]、tok[1200] 均因此转绿。

#### (e) 当前 PASS/FAIL 状态（2026-06-02，commit f28ceab）

| 检查点 | 状态 | 备注 |
|--------|------|------|
| tok[100..900]（9个） | ✅ PASS | 原有全部保持，无回归 |
| tok[1000] | ✅ PASS | enable修复后转绿（floor=MT10, HP=304, ATK=27） |
| tok[1100] | ✅ PASS | 连锁转绿 |
| tok[1200] | ✅ PASS | 连锁转绿 |
| tok[1300] | ✅ PASS | floor=MT14, HP=785, ATK=42, DEF=30（2026-06-02 转绿） |

**总计：13/13 PASS。MT1–MT14 全程逐 token 对齐完毕。24/24 回归测试全绿（含 test_checkpoints.py + test_replay_mt1_mt11.py）。**

#### (f) generateMove 异步误判修复（2026-06-02，commit f28ceab）

**根因**：`_execute_event_list` 对 `generateMove` 设置了 `had_sync_anim=True`，导致 MT10 events["6,9"]（小偷事件）后续对话字符串被误升级为拦截型事件，英雄卡在 MT10(6,9) 无法前往 MT11。

**源码证据**（`extract/mt6_10_raw.txt`）：
- events["6,5"]（埋伏）：所有 `generateMove` 均有 `"async":true` + `waitAsync`——需同步时**显式声明**
- events["6,9"]（小偷）：`generateMove` 无 `async` 字段、无 `waitAsync`——纯异步/fire-and-forget

**修复**（simulator.py 556行）：`t in ("move", "generateMove")` → `t == "move"`

**效果**：tok[1300] 从 `floor=MT10` 推进到 `floor=MT11`；原有 12 个 PASS 全部保持，无回归。

#### (g) tok[1300] 转绿：MT12-MT14 提取 + 商店/祭坛购买实现（2026-06-02）

**已提取并校验**：MT12.json / MT13.json / MT14.json，各楼层逐格比对 0 处不一致。

**已实现（simulator.py + common_events.json）**：
- `insert:"商店"` → 展开公共事件 `商店`（while+choices 循环）
- `while` / `break` / `CHOICE:n` token 推进选项分支
- 祭坛 `setValue flag:ratio` + 商店 `setValue +=` 通用实现（按 §D 公式，不硬编码）
- NPC 交互后英雄坐标移动到 NPC 格（修复旧 bug：hero 卡在相邻格）
- fly魔杖 MT12→MT13→MT12→MT13→MT14 双弹跳路径正确执行

**tok[1300] 验证值**：floor=MT14, HP=785, ATK=42(30+4×3), DEF=30。

---

## 八、下个会话待办（已知，先别动）

### (0) ✅ 扩层第一批 MT15–MT20 全部对齐（2026-06-03）：tok[2000] 转绿，17/17 全 PASS

**重大进展**：17/17 检查点全 PASS，**MT1–MT20 全程逐 token 忠实复现**，54/54 测试全绿（commit b11f9c2）。

**本批修复的根因（供参考）**：

1. **商人(trader 122) ≠ 祭坛(blueShop 131)**（commit 75687ea）：两套独立脚本。商人卖钥匙/二次对话消失，**不**共用祭坛"商店"commonEvent。
2. **祭坛格 7/8 标 noPass + 模拟器数据驱动读 noPass**（commit e7f20d0）：不再硬编码 WALL_TILES，塔特有装饰墙由 `tiles.json` 的 `noPass:true` 声明。
3. **互动格统一不移入**：fakeWall / 祭坛 / 激活 NPC / 商人 / 老人——触发互动后英雄**当步不移入**，需再按一次同向 token 才走入（与引擎"撞 NPC 不移动"一致）。
4. **cross 双倍攻击**：持十字架对兽人/吸血鬼 ATK ×2（cross 由 MT19 门事件给予，到达后生效）。
5. **打不过的怪原地不动（canBattle 规则）**（commit b11f9c2）：`_fight_monster` 中 `if result.damage >= hero.hp: return`——损血 ≥ 当前 HP（战后 HP≤0）则引擎 `canBattle(damage<hp)` 拒战，英雄原地不动（等同撞墙），HP/坐标/钥匙/金币全不变。
6. **老人对话事件（commonEvent "对话"）**（commit b11f9c2）：撞 oldman(121) → `systemEvents.oldman` → `insert "对话" args=[楼层号,x,y,0]` → 按 `flag:arg1`(楼层号) `switch` 显示提示 → 末尾 `hide loc=[arg2,arg3] remove` 老人消失。MT18 老人(纯提示)即此机制；通用支持 `insert args`(设 flag:arg1..arg4)、`switch/caseList`、`hide` 动态 loc 表达式。MT2(给1000金币)/MT3(给手册)的老人因 `有选择的对话=true` 会挂 choices 拦截，由路线中的 CHOICE token 消化（本路线仅 MT3 老人在 tok[115] 被撞、tok[116]=CHOICE:0 干净消化；MT2 老人全程未撞）。

**金标准锚点（全部已对齐 ✅）**：

```
tok[1809]: MT15 (7,10)  HP=179 ATK=68 DEF=54   ✅  （恶战 599→179，损 420）
tok[1902]: MT20 (6,10)  HP=591 ATK=68 DEF=54   ✅
tok[2000]: MT16 (3, 1)  HP=618 ATK=72 DEF=56   ✅  （区域内累计 ATK 68→72、DEF 54→56）
```

MT18 老人逐 token 吻合玩家真值：tok[1827]→(2,2)、tok[1828] 撞老人对话→老人消失/原地不动、tok[1829]→走入(3,2)。

**两个待办（都不影响当前结果）**：

- **(待办α) `item:` 前缀在 `_set_value` 是 no-op**：涉及 `item:book`(MT3老人手册)、`item:superPotion`(MT16)、`item:yellowKey`(MT12) 三处 setValue。当前 no-op 下 17/17 全绿（含受检的 HP/yk），说明现口径不依赖这三处生效。**若改为生效会同时激活 superPotion(动 HP)/yellowKey(动 yk) 两个受检字段，有打破现绿之险**；故将来确需时再单独评估 + 跑全回归，绝不顺手加。
- **(待办β) 解析器 `M<n>:` 吞数字 / snow 冰冻 / 矿镐(pickaxe)** 待实现：分别在扩到 tok[2200+]、MT35、用 `ITEM:47` 时才需要，当前不阻塞（详见 §八(b)(c)）。

**第二批 MT21–MT28 已完成**（✅ 端点验证 token2400 转绿，详见 §八(0.5)）。

复现脚本：`diag_1730_2000.py`（逐 token 打印 tok[1730..2000] + 锚点对比；注：脚本内 tok[1680] 锚点坐标 (1,11) 系手记写反，sim 实为 (11,1)、属性全对，非检查点、不影响结论）。第二批端点脚本：`extract/verify_tok2400.py`。

### (0.5) ✅ 扩层第二批 MT21–MT28 全部对齐（2026-06-03）：token2400 转绿，18/18 全 PASS

**重大进展**：18/18 检查点全 PASS，**MT1–MT28 全程逐 token 忠实复现**，54/54 测试全绿（commit 95e1b4f）。token2400 端点 = MT28(2,11) HP1261/ATK78/DEF64/5黄，逐项吻合。

**本批关键发现（已提取落盘 data/，模拟器已实现）**：

1. **MT14 红钥匙 = 三僵尸全灭的 autoEvent**：`MT14.autoEvent["1,3"]`，条件 `core.getBlockCls(1,1)!=='enemys' && (3,1) && (2,2)`（三只 zombieKnight 全杀），动作 `openDoor(1,3)`(清 330 不可破坏墙) + `setBlock redKey (1,3)`(放红钥匙)。英雄 tok2173 捡到，tok2322 飞 MT20 后开 (6,9) 红门上行。
   - **根因**：`_eval_single` 原不认 `core.getBlockCls(x,y) ===/!== 'enemys'` → 三子条件全 False → autoEvent 永不触发 → redKey 恒 0 → MT20 红门封死、英雄卡底排倒退回 MT19、token2400 停在 MT21（而非 MT28）。
   - **修复**（simulator.py `_eval_single`，commit 95e1b4f）：加 getBlockCls 分支，数据驱动判该格 entity 是否在 `_tile_to_enemy`（tiles.json enemys 段），不硬编码楼层。全 50 层仅 MT14 一处用 getBlockCls，影响面孤立。零回归。

2. **J9 解决：`flag:营救公主` 在 MT26(6,6) 公主(princess 132)事件设置**：撞公主 → events[6,6] false 分支设 `flag:营救公主=true` + setBlock 改写 **MT24 第6列**(6,2)/(6,3)/(6,4) 为地板——**跨层动态建图点**（一层事件改另一层地图，呼应建图铁律"地图连通性是动态的"）。此后踩 MT24(6,2) 且 flag 成立 → changeFloor MT50(6,7)。已完整提取，待英雄实走 MT50 流程端到端验证。

3. **MT28 钥匙回收商人（specialTrader 124，(8,4)）**：while+choices 循环，卖 1 黄钥匙 −1黄/+100金；"卖5把"/"全卖了" 受 `flag:额外功能开关` 门控；"下次再说"=break。已通用实现。

4. **tiles.json 怪物错名修正**（依据 `extract/blocksInfo_full.json` 引擎权威，commit 95e1b4f）：230/234/238–243/245–251/258 共 **16 个 enemys** id+_monster 重生成 + 补 **npcs 132=princess**（commit message 计为"17 处"）。MT15(6,6) 那只由错名 steelRock 正为 octopus（大章鱼 hp1200/atk180/def20）。零回归（MT15 那只本路线未打）。**仍缺** 9 个引擎正确怪名未进 monsters.json（yellowKing/poisonSkeleton/skeletonKing/skeletonWizard/redSkeletonCaption/demon/demonPriest/whiteHornSlime/badPrincess），当前已提取楼层均未用到，第三批若遇到再补。

**下一步：扩层第三批 MT29+**。玩家已给新检查点金标准真值：

```
token2501: MT32 (6,10)  HP=143  ATK=102 DEF=64  黄=5
token2804: MT33 (7,11)  HP=854  ATK=112 DEF=68  黄=3
token2965: MT33 (8,3)   HP=6    ATK=154 DEF=70  黄=2   ← HP 仅剩 6，极限状态
```

注意：**MT32 有祭坛**（§D 加点公式，搜索/重放须含祭坛加点）；token2965 HP=6 是极限状态（多损一格地形/战斗即致死，最易暴露损血/地形 bug）。token2400 已穿过 tok[2200]（§七c 解析器 `M<n>:` 吞数字起点）仍逐项对齐，说明 tok≤2400 解析无碍；但 §八(b) 的 `M<n>:` bug 影响区延伸到 tok[6138]，扩到 token2501+ 前需确认该区间是否出现 `M<n>:` token，必要时先修解析器。

### (a) ✅ test_replay_mt10.py — 7个埋伏机关测试已升级（commit 48693c1）

7 个隔离测试已全部替换为全路径真实重放验证（从 hero_init 出发，使用完整 51_*.h5route）。
MT10 埋伏访问为 Visit 5（tok[1168]），关键时刻：tok[1190]触发(6,3)→85；tok[1204]autoEvent(6,3)→0；tok[1250]进入MT11(HP=701,gold=305,RK=0)。
50/50 全测试全绿。

### (b) 解析器 Bug：`M<n>:` 后数字被吞（tok[2200] 后）

已记录于 §七(c)。`M<mapID>:<count>` 格式语义待确认，修复前须先理解含义。影响范围 tok[2200..6138]，当前检查点全部在 tok[1300] 以内，不阻塞。

### (c) 冰冻徽章 snow 机制（模拟器待实现）

机制已记入 `docs/mechanics_51.md §K`（数据来源：源码）。模拟器尚未实现。扩展到 MT35 层时再实现，当前不阻塞。

---

## 六、跨层开发建议起点

1. **提取 MT1–MT50 所有楼层数据**（扩展 gen_floors.py），重点补充 `upFloor`/`downFloor`/`canFlyTo`/`canFlyFrom` 字段。
2. **修复 simulator.py 的两个已知 bug**：
   - `_execute_instruction()` 新增 `changeFloor` 指令分支（MT3伏击/MT24事件均依赖）
   - `_set_value()` 补充 `"null"` → `None` 映射（伏击卸装依赖）
3. **扩展 GameState**：`floor` → `floors: dict[str, FloorState]` + `current_floor: str` + `pending_floor_change: Optional[...]` + `visited_floors: set[str]`。
4. **实现四条切层路径**（见三(b)），以 fly魔杖（220次）为主路径优先验证。
5. **全程回放验证**：以 hero_init 为起点，回放完整 route；终止条件 = MT50 内 `win` 指令触发。
