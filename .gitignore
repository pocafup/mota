# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# 临时诊断输出
*.txt
!docs/*.md

# Playwright MCP 日志
.playwright-mcp/

# h5route 存档（原始录像，体积大且含个人数据）
*.h5route

# 编辑器
.vscode/
.idea/
*.swp

# Claude Code 本地配置（不入库）
.claude/

# navigate_to 持久化缓存桶（跨 run 复用·体积大·按版本指纹自动失效重建·不入仓）
cache/

# 搜索/重放临时产物（每次跑都生成、不入仓）
*.log
*.jsonl
# json 仅窄域挡 extract/ 下路线/段出口/胜利路径产物，
# 绝不挡 data/ 事实源与 extract/mt*_raw_capture.json / audit_*.json / blocksInfo_full.json
extract/*route*.json
extract/seg_exit_*.json
extract/winning_path_*.json
